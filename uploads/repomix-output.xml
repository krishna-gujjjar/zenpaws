This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
.zed/
  settings.json
assets/
  pets/
    cat/
      sprites/
        away.png
        celebrate.png
        fight.png
        idle.png
        jump.png
        notification.png
        offline.png
        play.png
        run.png
        sleep.png
        typing.png
        walk.png
      reference.png
    dog/
      sprites/
        away.png
        celebrate.png
        fight.png
        idle.png
        jump.png
        notification.png
        offline.png
        play.png
        run.png
        sleep.png
        typing.png
        walk.png
      reference.png
    fox/
      sprites/
        away.png
        celebrate.png
        fight.png
        idle.png
        jump.png
        notification.png
        offline.png
        play.png
        run.png
        sleep.png
        typing.png
        walk.png
      reference.png
    ghost/
      sprites/
        away.png
        celebrate.png
        fight.png
        idle.png
        jump.png
        notification.png
        offline.png
        play.png
        run.png
        sleep.png
        typing.png
        walk.png
      reference.png
    parrot/
      sprites/
        away.png
        celebrate.png
        fight.png
        idle.png
        jump.png
        notification.png
        offline.png
        play.png
        run.png
        sleep.png
        typing.png
        walk.png
      reference.png
    rabbit/
      sprites/
        away.png
        celebrate.png
        fight.png
        idle.png
        jump.png
        notification.png
        offline.png
        play.png
        run.png
        sleep.png
        typing.png
        walk.png
      reference.png
    slime/
      sprites/
        away.png
        celebrate.png
        fight.png
        idle.png
        jump.png
        notification.png
        offline.png
        play.png
        run.png
        sleep.png
        typing.png
        walk.png
      reference.png
docs/
  00_PROJECT_CONSTITUTION.md
  01_PROJECT_OVERVIEW.md
  02_PRODUCT_REQUIREMENTS_DOCUMENT.md
  03_PROJECT_SCOPE.md
  04_SYSTEM_ARCHITECTURE.md
  05_TECHNICAL_DECISIONS.md
  06_NETWORK_ARCHITECTURE.md
  07_MESSAGE_PROTOCOL.md
  08_FILE_TRANSFER_PROTOCOL.md
  09_DATABASE_SCHEMA.md
  10_PET_ENGINE_SPECIFICATION.md
  11_UI_UX_GUIDELINES.md
  12_SECURITY_MODEL.md
  13_PERFORMANCE_GUIDELINES.md
  14_DEVELOPMENT_PHASES.md
  15_AI_AGENT_RULES.md
  16_PROJECT_MEMORY.md
  17_CHANGELOG.md
  18_BACKLOG.md
  19_KNOWN_ISSUES.md
  20_TESTING_STRATEGY.md
  21_DEPLOYMENT_GUIDE.md
  22_CONTRIBUTING_GUIDE.md
  23_ASSET_LICENSES.md
  24_API_REFERENCE.md
  25_EVENT_BUS_ARCHITECTURE.md
  26_STORAGE_STRATEGY.md
  27_SYNC_REPLICATION_MODEL.md
  28_PET_SPRITE_ASSET_GENERATION_PROMPTS.md
src/
  app/
    app-shell.tsx
    app.provider.tsx
    app.tsx
  features/
    chat/
      components/
        message-composer.tsx
        message-item.tsx
        message-list.tsx
        room-tabs.tsx
      utils/
        room.ts
      chat-shell.tsx
  hooks/
    use-app-status.ts
    use-peer-presence.ts
    use-typing-events.ts
  queries/
    message-status.ts
    messages.ts
  styles/
    global.css
  main.tsx
  vite-env.d.ts
src-tauri/
  capabilities/
    main-window.json
    pet-window.json
  crates/
    zenpaws-database/
      migrations/
        0001_init.sql
        0002_messages_fts.sql
        0003_peer_certificates.sql
      src/
        acks.rs
        lib.rs
        messages.rs
        tests.rs
        trust.rs
      Cargo.toml
    zenpaws-network/
      src/
        backoff.rs
        discovery.rs
        lib.rs
        protocol.rs
        service.rs
        session.rs
        state.rs
        tls.rs
        transport.rs
        udp.rs
      Cargo.toml
    zenpaws-notifications/
      src/
        lib.rs
      Cargo.toml
    zenpaws-pets/
      src/
        behavior.rs
        engine.rs
        lib.rs
        manifest.rs
        registry.rs
        state.rs
      Cargo.toml
    zenpaws-settings/
      src/
        identity.rs
        lib.rs
      Cargo.toml
    zenpaws-shared/
      src/
        lib.rs
        trust.rs
      Cargo.toml
    zenpaws-storage/
      src/
        lib.rs
      Cargo.toml
    zenpaws-transfer/
      src/
        lib.rs
      Cargo.toml
  icons/
    128x128.png
    128x128@2x.png
    256x256.png
    32x32.png
    512x512.png
    icon.icns
    icon.ico
    icon.png
  src/
    commands/
      chat.rs
      mod.rs
      network.rs
      pets.rs
    lib.rs
    main.rs
  .gitignore
  build.rs
  Cargo.toml
  rustfmt.toml
  tauri.conf.json
.gitignore
biome.json
index.html
MASTER_BUILD_PROMPT.md
package.json
README.md
tsconfig.json
tsconfig.node.json
tsconfig.node.tsbuildinfo
tsconfig.tsbuildinfo
vite.config.ts
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="src/features/chat/components/message-item.tsx">
import { useQueryClient } from "@tanstack/react-query";
import { invoke } from "@tauri-apps/api/core";
import { useState } from "react";
import { useMessageStatus } from "../../../queries/message-status";
import type { ChatMessage } from "../../../queries/messages";

interface MessageItemProps {
  message: ChatMessage;
  onReply: (message: ChatMessage) => void;
  room: string;
}

export function MessageItem({ message, onReply, room }: MessageItemProps) {
  const [editing, setEditing] = useState(false);
  const [body, setBody] = useState(message.body);
  const client = useQueryClient();
  const status = useMessageStatus(message.id, room);

  async function react() {
    await invoke("add_reaction", { emoji: "👍", messageId: message.id });
    await client.invalidateQueries({ queryKey: ["messages", room] });
  }

  async function remove() {
    await invoke("delete_message", { id: message.id });
    await client.invalidateQueries({ queryKey: ["messages", room] });
  }

  async function copy() {
    await navigator.clipboard.writeText(message.body);
  }

  async function saveEdit() {
    await invoke("edit_message", { body, id: message.id });
    await client.invalidateQueries({ queryKey: ["messages", room] });
    setEditing(false);
  }

  return (
    <article>
      <strong>{message.authorId}</strong>
      {editing ? (
        <>
          <textarea
            aria-label="Edit message"
            onChange={(event) => setBody(event.target.value)}
            value={body}
          />
          <button onClick={saveEdit} type="button">
            Save
          </button>
        </>
      ) : (
        <p>{message.body}</p>
      )}
      {message.replyTo ? <small>Replying to {message.replyTo}</small> : null}
      <div aria-label="Message actions">
        <button onClick={copy} type="button">
          Copy
        </button>
        <button onClick={() => onReply(message)} type="button">
          Reply
        </button>
        <button onClick={() => setEditing(true)} type="button">
          Edit
        </button>
        <button onClick={react} type="button">
          React
        </button>
        <button onClick={remove} type="button">
          Delete
        </button>
        <span>
          {room === "shared"
            ? "Sent"
            : status.data?.read
              ? "Read"
              : status.data?.delivered
                ? "Delivered"
                : "Sent"}
        </span>
      </div>
    </article>
  );
}
</file>

<file path="src/features/chat/components/room-tabs.tsx">
import { roomLabel } from "../utils/room";

interface RoomTabsProps {
  onSelect: (room: string) => void;
  rooms: readonly string[];
  selectedRoom: string;
}

export function RoomTabs({ onSelect, rooms, selectedRoom }: RoomTabsProps) {
  return (
    <nav aria-label="Chat rooms">
      {rooms.map((room) => (
        <button
          aria-pressed={room === selectedRoom}
          key={room}
          onClick={() => onSelect(room)}
          type="button"
        >
          {roomLabel(room)}
        </button>
      ))}
    </nav>
  );
}
</file>

<file path="src/features/chat/utils/room.ts">
const SHARED_ROOM = "shared";

export function directRoom(peerId: string): string {
  return `dm:${peerId}`;
}

export function isDirectRoom(room: string): boolean {
  return room.startsWith("dm:") && room.length > 3;
}

export function roomLabel(room: string): string {
  return room === SHARED_ROOM ? "Shared room" : "Direct message";
}

export { SHARED_ROOM };
</file>

<file path="src/hooks/use-peer-presence.ts">
import { listen } from "@tauri-apps/api/event";
import { useEffect, useState } from "react";

interface PeerStateEvent {
  PeerStateChanged: {
    peer_id: string;
    state: "Connecting" | "Connected" | "Degraded" | "Disconnected";
  };
}

export function usePeerPresence() {
  const [peers, setPeers] = useState<ReadonlyMap<string, string>>(new Map());

  useEffect(() => {
    let unlisten: (() => void) | undefined;
    void listen<PeerStateEvent>("zenpaws://event", (event) => {
      const peer = event.payload.PeerStateChanged;
      if (!peer) {
        return;
      }
      setPeers((current) => new Map(current).set(peer.peer_id, peer.state));
    }).then((dispose) => {
      unlisten = dispose;
    });
    return () => unlisten?.();
  }, []);

  return peers;
}
</file>

<file path="src/hooks/use-typing-events.ts">
import { listen } from "@tauri-apps/api/event";
import { useEffect, useRef, useState } from "react";

interface TypingEvent {
  TypingChanged: {
    is_typing: boolean;
    peer_id: string;
    room: string;
  };
}

export function useTypingEvents(room: string) {
  const [typingPeers, setTypingPeers] = useState<ReadonlySet<string>>(new Set());
  const expiry = useRef(new Map<string, number>());

  useEffect(() => {
    let unlisten: (() => void) | undefined;
    void listen<TypingEvent>("zenpaws://event", (event) => {
      const typing = event.payload.TypingChanged;
      if (!typing || typing.room !== room) {
        return;
      }
      setTypingPeers((peers) => {
        const next = new Set(peers);
        if (typing.is_typing) {
          const previous = expiry.current.get(typing.peer_id);
          if (previous !== undefined) {
            window.clearTimeout(previous);
          }
          next.add(typing.peer_id);
          expiry.current.set(
            typing.peer_id,
            window.setTimeout(() => {
              setTypingPeers((active) => {
                const updated = new Set(active);
                updated.delete(typing.peer_id);
                return updated;
              });
            }, 3000),
          );
        } else {
          const timeout = expiry.current.get(typing.peer_id);
          if (timeout !== undefined) {
            window.clearTimeout(timeout);
          }
          next.delete(typing.peer_id);
        }
        return next;
      });
    }).then((dispose) => {
      unlisten = dispose;
    });
    return () => unlisten?.();
  }, [room]);

  return typingPeers;
}
</file>

<file path="src/queries/message-status.ts">
import { useQuery } from "@tanstack/react-query";
import { invoke } from "@tauri-apps/api/core";
import { isDirectRoom } from "../features/chat/utils/room";

export interface MessageStatus {
  delivered: boolean;
  read: boolean;
}

export function useMessageStatus(messageId: string, room: string) {
  const peerId = isDirectRoom(room) ? room.slice(3) : null;
  return useQuery({
    enabled: peerId !== null,
    queryFn: () => invoke<MessageStatus>("message_status", { messageId, peerId }),
    queryKey: ["message-status", messageId, peerId],
    staleTime: Number.POSITIVE_INFINITY,
  });
}
</file>

<file path="src-tauri/crates/zenpaws-database/src/tests.rs">
use std::env;

    use super::{Database, MessageRecord};
    use uuid::Uuid;
    use zenpaws_shared::PeerId;

    #[test]
    fn applies_initial_migration_and_writes_a_peer() {
        let database = Database::open_in_memory().expect("database opens");
        database
            .upsert_peer(PeerId::new(), "peer", &[7; 32], 1)
            .expect("peer writes");

        let peer_id = PeerId::new();
        database
            .upsert_peer(peer_id, "author", &[8; 32], 2)
            .expect("author writes");
        database
            .insert_message(&MessageRecord {
                author: peer_id,
                body: "searchable local message".to_owned(),
                created_at: 2,
                id: Uuid::new_v4(),
                lamport_counter: 1,
                lamport_peer: peer_id,
                reply_to: None,
                room: "shared".to_owned(),
            })
            .expect("message writes");

        assert_eq!(database.migration_count().expect("migrations query"), 3);
        assert_eq!(
            database
                .search_messages("searchable", 10)
                .expect("fts query")
                .len(),
            1
        );
        database
            .set_setting("theme", "\"dark\"")
            .expect("setting writes");
        assert_eq!(
            database.setting("theme").expect("setting reads"),
            Some("\"dark\"".to_owned())
        );
    }

    #[test]
    fn database_can_be_owned_by_tauri_mutex_state() {
        fn assert_send<T: Send>() {}
        assert_send::<Database>();
    }

    #[test]
    fn configures_wal_and_reuses_migrations_on_disk() {
        let path = env::temp_dir().join(format!("zenpaws-db-{}.sqlite", Uuid::new_v4()));
        let database = Database::open(&path).expect("database opens");
        let journal_mode: String = database
            .connection
            .query_row("PRAGMA journal_mode", [], |row| row.get(0))
            .expect("journal mode reads");
        assert_eq!(journal_mode, "wal");
        drop(database);

        let reopened = Database::open(&path).expect("database reopens");
        assert_eq!(reopened.migration_count().expect("migrations query"), 3);
        drop(reopened);
        std::fs::remove_file(&path).expect("database file is removable");
        let _ = std::fs::remove_file(path.with_extension("sqlite-wal"));
        let _ = std::fs::remove_file(path.with_extension("sqlite-shm"));
    }
</file>

<file path="tsconfig.node.tsbuildinfo">
{"fileNames":["./node_modules/typescript/lib/lib.d.ts","./node_modules/typescript/lib/lib.es5.d.ts","./node_modules/typescript/lib/lib.dom.d.ts","./node_modules/typescript/lib/lib.webworker.importscripts.d.ts","./node_modules/typescript/lib/lib.scripthost.d.ts","./node_modules/typescript/lib/lib.decorators.d.ts","./node_modules/typescript/lib/lib.decorators.legacy.d.ts","./node_modules/vite/types/hmrpayload.d.ts","./node_modules/vite/dist/node/chunks/modulerunnertransport.d.ts","./node_modules/vite/types/customevent.d.ts","./node_modules/@types/estree/index.d.ts","./node_modules/rollup/dist/rollup.d.ts","./node_modules/rollup/dist/parseast.d.ts","./node_modules/vite/types/hot.d.ts","./node_modules/vite/dist/node/module-runner.d.ts","./node_modules/esbuild/lib/main.d.ts","./node_modules/vite/types/internal/terseroptions.d.ts","./node_modules/source-map-js/source-map.d.ts","./node_modules/postcss/lib/previous-map.d.ts","./node_modules/postcss/lib/input.d.ts","./node_modules/postcss/lib/css-syntax-error.d.ts","./node_modules/postcss/lib/declaration.d.ts","./node_modules/postcss/lib/root.d.ts","./node_modules/postcss/lib/warning.d.ts","./node_modules/postcss/lib/lazy-result.d.ts","./node_modules/postcss/lib/no-work-result.d.ts","./node_modules/postcss/lib/processor.d.ts","./node_modules/postcss/lib/result.d.ts","./node_modules/postcss/lib/document.d.ts","./node_modules/postcss/lib/rule.d.ts","./node_modules/postcss/lib/node.d.ts","./node_modules/postcss/lib/comment.d.ts","./node_modules/postcss/lib/container.d.ts","./node_modules/postcss/lib/at-rule.d.ts","./node_modules/postcss/lib/list.d.ts","./node_modules/postcss/lib/postcss.d.ts","./node_modules/postcss/lib/postcss.d.mts","./node_modules/vite/types/internal/csspreprocessoroptions.d.ts","./node_modules/vite/types/internal/lightningcssoptions.d.ts","./node_modules/vite/types/importglob.d.ts","./node_modules/vite/types/metadata.d.ts","./node_modules/vite/dist/node/index.d.ts","./node_modules/@babel/types/lib/index.d.ts","./node_modules/@types/babel__generator/index.d.ts","./node_modules/@babel/parser/typings/babel-parser.d.ts","./node_modules/@types/babel__template/index.d.ts","./node_modules/@types/babel__traverse/index.d.ts","./node_modules/@types/babel__core/index.d.ts","./node_modules/@vitejs/plugin-react/dist/index.d.ts","./vite.config.ts","./node_modules/@types/esrecurse/index.d.ts","./node_modules/@types/json-schema/index.d.ts","./node_modules/@types/react/global.d.ts","./node_modules/csstype/index.d.ts","./node_modules/@types/react/index.d.ts","./node_modules/@types/react-dom/index.d.ts"],"fileIdsList":[[43],[43,44,45,46,47],[43,45],[55],[53,54],[42,48],[33],[31,33],[22,30,31,32,34,36],[20],[23,28,33,36],[19,36],[23,24,27,28,29,36],[23,24,25,27,28,36],[20,21,22,23,24,28,29,30,32,33,34,36],[36],[18,20,21,22,23,24,25,27,28,29,30,31,32,33,34,35],[18,36],[23,25,26,28,29,36],[27,36],[28,29,33,36],[21,31],[12,41,42],[11,12],[8],[8,9,10,12,13,15,16,17,37,38,39,40,41,42],[8,9,10,14],[10],[12,42],[42,49]],"fileInfos":[{"version":"a7297ff837fcdf174a9524925966429eb8e5feecc2cc55cc06574e6b092c1eaa","impliedFormat":1},{"version":"69684132aeb9b5642cbcd9e22dff7818ff0ee1aa831728af0ecf97d3364d5546","affectsGlobalScope":true,"impliedFormat":1},{"version":"092c2bfe125ce69dbb1223c85d68d4d2397d7d8411867b5cc03cec902c233763","affectsGlobalScope":true,"impliedFormat":1},{"version":"80e18897e5884b6723488d4f5652167e7bb5024f946743134ecc4aa4ee731f89","affectsGlobalScope":true,"impliedFormat":1},{"version":"cd034f499c6cdca722b60c04b5b1b78e058487a7085a8e0d6fb50809947ee573","affectsGlobalScope":true,"impliedFormat":1},{"version":"8e7f8264d0fb4c5339605a15daadb037bf238c10b654bb3eee14208f860a32ea","affectsGlobalScope":true,"impliedFormat":1},{"version":"782dec38049b92d4e85c1585fbea5474a219c6984a35b004963b00beb1aab538","affectsGlobalScope":true,"impliedFormat":1},{"version":"a7ca8df4f2931bef2aa4118078584d84a0b16539598eaadf7dce9104dfaa381c","impliedFormat":1},{"version":"10073cdcf56982064c5337787cc59b79586131e1b28c106ede5bff362f912b70","impliedFormat":99},{"version":"72950913f4900b680f44d8cab6dd1ea0311698fc1eefb014eb9cdfc37ac4a734","impliedFormat":1},{"version":"751764bb94219b4ce8f5475dc35d3de2e432fea01a0c9610cd7f69ad05e398c6","impliedFormat":1},{"version":"ee70b8037ecdf0de6c04f35277f253663a536d7e38f1539d270e4e916d225a3f","affectsGlobalScope":true,"impliedFormat":1},{"version":"a660aa95476042d3fdcc1343cf6bb8fdf24772d31712b1db321c5a4dcc325434","impliedFormat":1},{"version":"36977c14a7f7bfc8c0426ae4343875689949fb699f3f84ecbe5b300ebf9a2c55","impliedFormat":1},{"version":"ff0a83c9a0489a627e264ffcb63f2264b935b20a502afa3a018848139e3d8575","impliedFormat":99},{"version":"161c8e0690c46021506e32fda85956d785b70f309ae97011fd27374c065cac9b","affectsGlobalScope":true,"impliedFormat":1},{"version":"f582b0fcbf1eea9b318ab92fb89ea9ab2ebb84f9b60af89328a91155e1afce72","impliedFormat":1},{"version":"402e5c534fb2b85fa771170595db3ac0dd532112c8fa44fc23f233bc6967488b","impliedFormat":1},{"version":"52dcc257df5119fb66d864625112ce5033ac51a4c2afe376a0b299d2f7f76e4a","impliedFormat":1},{"version":"e5bab5f871ef708d52d47b3e5d0aa72a08ee7a152f33931d9a60809711a2a9a3","impliedFormat":1},{"version":"e16dc2a81595736024a206c7d5c8a39bfe2e6039208ef29981d0d95434ba8fcf","impliedFormat":1},{"version":"cc4a4903fb698ca1d961d4c10dce658aa3a479faf40509d526f122b044eaf6a4","impliedFormat":1},{"version":"19ee8416e6473ed6c7adb868fa796b5653cf0fa2a337658e677eaa0d134388c3","impliedFormat":1},{"version":"1328ab4e442614b28cdb3d4b414cf68325c0da0dca07287a338d0654b7a00261","impliedFormat":1},{"version":"a039dc21f045919f3cbee2ec13812cc6cc3eebc99dae4be00973230f468d19a6","impliedFormat":1},{"version":"3fbe57af01460e49dcd29df55d6931e1672bc6f1be0fb073d11410bc16f9037d","impliedFormat":1},{"version":"f760be449e8562ec5c09bb5187e8e1eabf3c113c0c58cddda53ef8c69f3e2131","impliedFormat":1},{"version":"44325ed13294fce6ab825b82947bbeed2611db7dad9d9135260192f375e5a189","impliedFormat":1},{"version":"e392e8fb5b514eafc585601c1d781485aa6dd6a320e75daf1064a4c6918a1b45","impliedFormat":1},{"version":"46e4a36e8ddbdfb4e7330e11c81c970dc8b218611df9183d39c41c5f8c653b55","impliedFormat":1},{"version":"370bde134aa8c2abc926d0e99d3a4d5d5dba65c6ee65459137e4f02670cbf841","impliedFormat":1},{"version":"6332f565867cf4a740a70e30f31cefba37ef7cebcf74f22eab8d744fde6d193e","impliedFormat":1},{"version":"2977b7884aedc895a1d0c9c210c7cf3272c29d6959a08a6fa3ff71e0aff08175","impliedFormat":1},{"version":"17f2922d41ddd032830a91371c948cd9ce903b35c95adca72271a54584f19b0b","impliedFormat":1},{"version":"3eed76ede2a1a14d7c9bb0a642041282dcc264811139d3dd275c9fe14efc9840","impliedFormat":1},{"version":"354a7f8e1287d9d6b7561bc97fdd8cbc2f7c1dd79e4cb37b942e8a5cfaff1085","impliedFormat":1},{"version":"8d369483f0c2b9ee388129cfdb6a43bc8112b377e86a41884bd06e19ce04f4c1","impliedFormat":99},{"version":"960bd764c62ac43edc24eaa2af958a4b4f1fa5d27df5237e176d0143b36a39c6","affectsGlobalScope":true,"impliedFormat":1},{"version":"4ec16d7a4e366c06a4573d299e15fe6207fc080f41beac5da06f4af33ea9761e","impliedFormat":1},{"version":"59f8dc89b9e724a6a667f52cdf4b90b6816ae6c9842ce176d38fcc973669009e","affectsGlobalScope":true,"impliedFormat":1},{"version":"e4af494f7a14b226bbe732e9c130d8811f8c7025911d7c58dd97121a85519715","impliedFormat":1},{"version":"cbb1c5ba5dbabe42c19ca31b83e48fec95895484fe1d1a8fb649b69ea224c5b8","impliedFormat":99},{"version":"556ccd493ec36c7d7cb130d51be66e147b91cc1415be383d71da0f1e49f742a9","impliedFormat":1},{"version":"b6d03c9cfe2cf0ba4c673c209fcd7c46c815b2619fd2aad59fc4229aaef2ed43","impliedFormat":1},{"version":"95aba78013d782537cc5e23868e736bec5d377b918990e28ed56110e3ae8b958","impliedFormat":1},{"version":"670a76db379b27c8ff42f1ba927828a22862e2ab0b0908e38b671f0e912cc5ed","impliedFormat":1},{"version":"13b77ab19ef7aadd86a1e54f2f08ea23a6d74e102909e3c00d31f231ed040f62","impliedFormat":1},{"version":"069bebfee29864e3955378107e243508b163e77ab10de6a5ee03ae06939f0bb9","impliedFormat":1},{"version":"26e0ffceb2198feb1ef460d5d14111c69ad07d44c5a67fd4bfeb74c969aa9afb","impliedFormat":99},"52f3ecd9baa285cb65f870eaffa276bd4f0dbf915a2f1bae0d68d101252ce736",{"version":"d5799bcf7fe4e6de3063abf4e321c14b051706f03cf5716d8a19f22fe1f69519","impliedFormat":1},{"version":"f3d8c757e148ad968f0d98697987db363070abada5f503da3c06aefd9d4248c1","impliedFormat":1},{"version":"7e29f41b158de217f94cb9676bf9cbd0cd9b5a46e1985141ed36e075c52bf6ad","affectsGlobalScope":true,"impliedFormat":1},{"version":"ac51dd7d31333793807a6abaa5ae168512b6131bd41d9c5b98477fc3b7800f9f","impliedFormat":1},{"version":"dc782ff85b2cb10075ecffc158af7bfb27ff97bf8491c917efea0c3d622d5ac4","impliedFormat":1},{"version":"be1cc4d94ea60cbe567bc29ed479d42587bf1e6cba490f123d329976b0fe4ee5","impliedFormat":1}],"root":[50],"options":{"allowSyntheticDefaultImports":true,"composite":true,"module":99,"noImplicitAny":true,"skipLibCheck":true,"strict":true,"strictNullChecks":true},"referencedMap":[[45,1],[48,2],[44,1],[46,3],[47,1],[56,4],[55,5],[49,6],[34,7],[32,8],[33,9],[21,10],[22,8],[29,11],[20,12],[25,13],[26,14],[31,15],[37,16],[36,17],[19,18],[27,19],[28,20],[23,21],[30,7],[24,22],[13,23],[12,24],[9,25],[42,26],[15,27],[10,25],[14,28],[41,29],[50,30]],"affectedFilesPendingEmit":[[50,17]],"emitSignatures":[50],"version":"5.8.3"}
</file>

<file path="tsconfig.tsbuildinfo">
{"root":["./src/main.tsx","./src/vite-env.d.ts","./src/app/app-shell.tsx","./src/app/app.provider.tsx","./src/app/app.tsx","./src/features/chat/chat-shell.tsx","./src/features/chat/components/message-composer.tsx","./src/features/chat/components/message-item.tsx","./src/features/chat/components/message-list.tsx","./src/features/chat/components/room-tabs.tsx","./src/features/chat/utils/room.ts","./src/hooks/use-app-status.ts","./src/hooks/use-peer-presence.ts","./src/hooks/use-typing-events.ts","./src/queries/message-status.ts","./src/queries/messages.ts"],"errors":true,"version":"5.8.3"}
</file>

<file path="docs/00_PROJECT_CONSTITUTION.md">
# 00 - Project Constitution

This is the condensed, always-in-force ruleset for building ZenPaws. The
full rationale lives in `MASTER_BUILD_PROMPT.md` at the repo root; this file
is the quick-reference version every phase is checked against.

## What ZenPaws is

A production-grade, cross-platform, LAN-only peer-to-peer desktop messenger
with interactive desktop pets, built on Tauri v2 + Rust + React 19.

## Coding discipline - the ponytail ladder

Before writing any code, stop at the first rung that holds:

1. Does this need to exist at all?
2. Is it already in this codebase?
3. Does the Rust/TS standard library do it?
4. Does the OS or Tauri do it natively?
5. Does an already-installed dependency do it?
6. Can it be done in one line?
7. Only then: the minimum new code that correctly solves the problem.

Never trimmed by this ladder: security, correctness, reliability,
accessibility, type safety.

## Code quality bar (applies from Phase 1 onward)

- **No file over 200 lines** for any code file (`.rs`, `.ts`, `.tsx`). Split
  by responsibility into components/hooks/utils/modules - see
  `15_AI_AGENT_RULES.md` for the exact decomposition rules.
- **No `any`** in TypeScript, anywhere, including generics defaults and
  catch clauses. Biome's `noExplicitAny` is set to `error`, not `warn`.
- Every dependency is checked against its own current documentation before
  use - not assumed from training data - since APIs and package names
  change (e.g. `framer-motion` → `motion`).
- Rust: `cargo clippy -- -D warnings` and `cargo fmt --check` must pass.
  TypeScript: `biome check .` must pass. Both are wired into CI from Phase 1.

## Process rules

- Never generate the entire project in one response; one phase at a time.
- Wait for explicit approval before starting the next phase.
- Never generate placeholder/mocked/TODO-only implementations unless
  explicitly requested - a crate that legitimately has no logic yet (because
  its phase hasn't started) stays an honestly empty, compiling stub with a
  doc comment, not a fake implementation.
- If a requirement is ambiguous or two requirements conflict, stop and ask.
- After every phase: update `16_PROJECT_MEMORY.md`, `17_CHANGELOG.md`,
  `18_BACKLOG.md`, `19_KNOWN_ISSUES.md`, and commit the phase as one atomic
  git commit.
- Architecture quality over implementation speed - but "quality" here means
  correctly minimal per the ladder above, not maximal abstraction.

## Non-goals

No WAN/internet relay, no mobile clients, no multi-workspace topology, no
resistance to a nation-state-level adversary on the LAN. See
`03_PROJECT_SCOPE.md` for the full boundary.
</file>

<file path="docs/01_PROJECT_OVERVIEW.md">
# 01 - Project Overview

## What is ZenPaws

ZenPaws is a desktop application for LAN-only, serverless group chat with an
interactive desktop-pet layer on top. It's aimed at small trusted networks -
an office, a LAN party, a home network - where installing a chat server isn't
worth it, but people still want presence, chat, file sharing, and a bit of
personality on their desktop.

## The three systems

1. **Messenger** - shared-room and direct-message chat, search, file and
   image sharing.
2. **Desktop Pet Platform** - transparent, always-on-top animated companions
   that react to chat activity and the user's cursor.
3. **LAN Networking Platform** - peer discovery, hostless replication,
   messaging transport, and file transfer that the other two systems sit on.

These are loosely coupled through an event bus (`25_EVENT_BUS_ARCHITECTURE.md`)
so the pet platform keeps working even if chat is disabled.

## Target users

Small trusted LANs, up to roughly 50 concurrent peers. No system
administrator, no server to stand up - every participant just runs the app.

## Supported platforms

Windows, Linux, macOS - one codebase, via Tauri v2.

## Target hardware

Dual-core Intel i3-equivalent CPU, 4GB RAM, HDD, integrated GPU. See
`13_PERFORMANCE_GUIDELINES.md` for the concrete budgets this implies.

## What this document set is

Each numbered doc in `docs/` owns one concern. When in doubt about a
decision, its rationale lives in exactly one of these files - check
`04_SYSTEM_ARCHITECTURE.md` if unsure which one.
</file>

<file path="docs/02_PRODUCT_REQUIREMENTS_DOCUMENT.md">
# 02 - Product Requirements Document

Each requirement below is written so it's testable. "Support X" from the
original brief is expanded into what "supported" means to verify.

## Identity & presence

- A user sets a username and avatar on first launch; a UUID is generated
  and persisted locally as their durable identity.
- Peers on the LAN appear in a presence list within a few seconds of
  joining (mDNS discovery latency, not polling).
- Presence states: online, away, offline - driven by the connection-state
  machine in `06_NETWORK_ARCHITECTURE.md`, not by manual toggling only.

## Messaging

- Shared room + 1:1 direct messages.
- Text messages support: edit, delete, reply-threading, emoji reactions,
  @mentions, typing indicators, sent/delivered/read status (semantics
  defined in `07_MESSAGE_PROTOCOL.md`), full-text search (FTS5), copy,
  right-click context menu.
- A room with 100,000+ messages scrolls smoothly (virtualized list,
  paginated fetch) on the target hardware in `13_PERFORMANCE_GUIDELINES.md`.

## Images

- Paste (Ctrl+V), clipboard, and drag-and-drop (browser/desktop/file
  explorer) all produce an attached image.
- Multiple images per message; preview, zoom, fullscreen, save-to-disk.
- Images auto-transfer to peers (thumbnail first, lazy-loaded original) -
  the only file type that auto-transfers content, not just metadata.

## Files

- Any file type is attachable. Non-image files transfer **metadata only**
  on send; content transfers only when the receiver clicks download.
- Transfer supports pause/resume/cancel, progress, and integrity
  verification (SHA-256) before a downloaded file is considered complete.

## Desktop pets

- Seven initial pets (cat, dog, fox, rabbit, slime, ghost, parrot), each a
  transparent always-on-top window, data-driven from
  `assets/pets/<pet>/manifest.json` - installable without a code change.
- Pets react to chat activity (typing, new message, celebration) and to
  cursor behavior (chase, react to rapid movement).
- Click-through mode is toggleable per pet.

## Notifications

- Native OS notification + pet speech-bubble on new message/mention.
- Clicking a notification focuses the relevant chat.
- No message content in the notification preview by default.

## Settings

- Username, avatar, theme (light/dark/system), notification prefs,
  download location, auto-download, startup behavior, font size, pet
  settings, performance mode (Full vs. Performance).

## System integration

- System tray with background operation; window state persists across
  restarts; clipboard copy/paste for text and images.

## Non-functional requirements

See `13_PERFORMANCE_GUIDELINES.md` (performance budgets) and
`12_SECURITY_MODEL.md` (threat model) - both are requirements, not
aspirations, and Phase 10 testing verifies against them explicitly.
</file>

<file path="docs/03_PROJECT_SCOPE.md">
# 03 - Project Scope

## In scope (this build)

- LAN-only chat (shared room + DMs), images, arbitrary file transfer.
- Seven data-driven desktop pets with discrete-event state sync.
- Hostless replication among peers (no server).
- Windows/Linux/macOS via Tauri v2.
- TLS + TOFU transport security on the LAN.

## Explicitly out of scope (non-goals)

- **No WAN/internet relay, NAT traversal, or cloud component of any kind.**
  If a future need for cross-network chat emerges, it's a new project
  decision, not an extension bolted onto this one.
- **No mobile clients.** Desktop only.
- **No multi-workspace / multi-server topology.** One flat LAN room plus
  DMs - not a Slack-style architecture with channels-of-channels.
- **No resistance to a nation-state-level adversary already on the LAN.**
  The security model (TLS + TOFU) protects against casual snooping and
  identity spoofing by other LAN devices, not a sophisticated attacker who
  already has a foothold on the same subnet. This line is deliberate - see
  `12_SECURITY_MODEL.md` for the full threat model.

## Deferred to a later iteration (not this build, but designed for)

- **One pet per LAN user**, broadcast via the same discrete-event sync
  already built for local pets - the sync protocol in
  `09_PET_ENGINE_SPECIFICATION.md` is designed so this is additive, not a
  rework.

## MVP vs. full build

Phases 0–8 (through Notification System) constitute a usable MVP. Phases 9
(Optimization) and 10 (Testing & Hardening) are required before this is
"production-grade" in the sense the constitution uses that word - they are
not optional polish.
</file>

<file path="docs/04_SYSTEM_ARCHITECTURE.md">
# 04 - System Architecture

## Top-level shape

```
┌─────────────────────────────┐        ┌──────────────────────────────────┐
│   React 19 Frontend (src/)   │ Tauri  │   Rust Backend (src-tauri/)       │
│  Zustand + TanStack Query    │◄──IPC─►│  thin commands/ → crates/*        │
│  Motion animations           │ events │  tokio runtime                    │
└─────────────────────────────┘        └──────────────────────────────────┘
```

The frontend never talks to the network, filesystem, or database directly -
only through Tauri commands exposed by `src-tauri/src/commands/`, which
delegate to the internal crates. This is what makes the per-window
capability scoping in `12_SECURITY_MODEL.md` meaningful: a window can only
reach what its capability file grants, regardless of what the frontend code
tries to call.

## Internal crate graph

```mermaid
graph TD
    UI[React Frontend] -->|Tauri IPC| CMD[commands/]
    CMD --> SHARED[zenpaws-shared: types, errors, Event Bus]
    CMD --> NET[zenpaws-network]
    CMD --> DB[zenpaws-database]
    CMD --> PETS[zenpaws-pets]
    CMD --> XFER[zenpaws-transfer]
    CMD --> NOTIF[zenpaws-notifications]
    CMD --> STORE[zenpaws-storage]
    CMD --> SETTINGS[zenpaws-settings]

    NET -->|events| SHARED
    XFER -->|events| SHARED
    SHARED -->|events| PETS
    SHARED -->|events| NOTIF
    DB -.->|read/write| NET
    DB -.->|read/write| XFER
```

Solid arrows: direct calls. Dashed: data dependency, not control coupling.
Note that `NET`, `PETS`, `XFER`, and `NOTIF` never call each other directly
- only through the Event Bus in `zenpaws-shared`, per
`25_EVENT_BUS_ARCHITECTURE.md`.

## Why a Cargo workspace instead of Tauri "modules"

Tauri v2 doesn't have a first-class module system beyond plugins; the
idiomatic way to get real separation of concerns in a Tauri app of this size
is a Cargo workspace with the binary crate (`src-tauri`) as a thin shell
over internal library crates. Each `zenpaws-*` crate is independently unit-
testable without a Tauri runtime. See `05_TECHNICAL_DECISIONS.md` for the
alternatives considered.

## Where things live

| Concern | Location |
|---|---|
| UI components/hooks/state | `src/` |
| IPC surface (thin) | `src-tauri/src/commands/` |
| Business logic | `src-tauri/crates/zenpaws-*` |
| Tauri app config, window defs | `src-tauri/tauri.conf.json` |
| Per-window permissions | `src-tauri/capabilities/*.json` |
| Pet assets | `assets/pets/<pet>/` (added in Phase 4) |

## Data flow example: sending a text message

1. UI calls the `send_message` Tauri command with a typed payload.
2. `commands/chat.rs` validates shape, calls `zenpaws-database` to persist
   locally (source of truth is local-first - see `27_SYNC_REPLICATION_MODEL.md`),
   then calls `zenpaws-network` to broadcast the message envelope.
3. `zenpaws-network` publishes a `MessageSent` event on the Event Bus.
4. `zenpaws-pets` subscribes and may trigger a "celebrate" pet state;
   `zenpaws-notifications` subscribes for the receiving peers' side.
5. The frontend receives a Tauri event and updates the TanStack Query cache
   optimistically, reconciled once the local DB write is confirmed.
</file>

<file path="docs/06_NETWORK_ARCHITECTURE.md">
# 06 - Network Architecture

LAN only. No cloud, no relay, no external servers.

## Discovery

`mdns-sd` advertises each peer as `_zenpaws._tcp.local` with TXT records for
`uuid`, `username`, and protocol version. Discovery is push-based (mDNS
announcements + queries), not polling. UDP broadcast is the fallback path
for networks where multicast is filtered.

## Identity

UUID + username + avatar, generated on first launch and persisted in
`zenpaws-settings`. **IP address is a transport detail only** - DHCP can
reassign it at any time, so it is never used as a peer identity key
anywhere in the codebase (database keys, UI display, dedup logic, etc.).

## Transport

TCP with a binary protocol (see `07_MESSAGE_PROTOCOL.md`) rather than
WebSocket - this is a native Rust-to-Rust peer link with no browser
involved, so WebSocket's HTTP upgrade handshake and framing overhead buys
nothing here.

## Connection state machine

```mermaid
stateDiagram-v2
    [*] --> Connecting
    Connecting --> Connected: TLS handshake + TOFU check pass
    Connecting --> Disconnected: handshake fails
    Connected --> Degraded: heartbeat missed
    Degraded --> Connected: heartbeat recovers
    Degraded --> Disconnected: retry budget exhausted
    Disconnected --> Connecting: mDNS re-announces peer
```

- **Heartbeat**: periodic ping over the open TCP connection; missing N
  consecutive heartbeats moves the peer to `Degraded`.
- **Retry with backoff**: exponential backoff on reconnect attempts, capped,
  to avoid hammering a peer that's genuinely offline.
- No polling beyond the heartbeat interval; no port scanning - peers are
  only ever contacted at addresses learned via mDNS.

## Protocol versioning

Every message envelope carries a 1-byte protocol version, independent of
the per-message Lamport logical-clock versioning used for replication
(`27_SYNC_REPLICATION_MODEL.md`). A peer on an older protocol version is
detected at handshake time and shown as "incompatible version" rather than
silently misinterpreting frames.

## Delivery semantics

Given hostless replication (no server to be the source of truth for
"delivered"), these are defined explicitly rather than left implicit:

- **Sent**: the local peer has persisted the message and queued it for
  broadcast.
- **Delivered**: acknowledged by at least one other currently-connected
  peer (not necessarily the final recipient, in the shared-room case).
- **Read**: supported for direct messages only, acknowledged by the other
  recipient. Shared-room messages do not send or display read receipts.

This owner-approved definition is locked for Phase 2 implementation.

## LAN scale target

Up to ~50 concurrent peers. The heartbeat interval, retry backoff caps, and
mDNS query interval are tuned in `13_PERFORMANCE_GUIDELINES.md` to keep
aggregate LAN traffic low at that scale.
</file>

<file path="docs/07_MESSAGE_PROTOCOL.md">
# 07 - Message Protocol

## Wire format

```
┌──────────────┬───────────────────┬──────────────────────────┐
│ length (u32) │ protocol_ver (u8) │ bincode(serde enum body)  │
│ 4 bytes, LE  │ 1 byte            │ `length` bytes            │
└──────────────┴───────────────────┴──────────────────────────┘
```

Length-prefixed framing over the TCP stream from `06_NETWORK_ARCHITECTURE.md`.
`bincode` chosen over JSON for this payload - see `05_TECHNICAL_DECISIONS.md`
for why.

## Envelope body (conceptual shape - refined in Phase 5 implementation)

```rust
enum Envelope {
    Handshake { peer_uuid: Uuid, username: String, pubkey_fingerprint: [u8; 32] },
    Heartbeat,
    Message(MessagePayload),
    Ack { message_id: Uuid, kind: AckKind }, // Delivered | Read
    PetState(PetStateEvent),                  // see 09_PET_ENGINE_SPECIFICATION.md
    TransferControl(TransferMessage),          // see 08_FILE_TRANSFER_PROTOCOL.md
    SyncRequest { since_clock: LamportClock }, // see 27_SYNC_REPLICATION_MODEL.md
}

struct MessagePayload {
    id: Uuid,
    room: RoomTarget,       // SharedRoom | Direct(Uuid)
    author: Uuid,
    body: MessageBody,      // Text | Edit | Delete | Reaction
    clock: LamportClock,    // (peer_uuid, counter) - see 27
    created_at: i64,        // unix millis, local clock - not trusted for ordering
    reply_to: Option<Uuid>,
    mentions: Vec<Uuid>,
}
```

## Delivery/read status

Implements the semantics defined in `06_NETWORK_ARCHITECTURE.md` via the
`Ack` variant above. `Read` acknowledgements are valid for direct messages
only; shared-room messages do not have per-peer read state.

## Edit / delete / conflict handling

An edit or delete is itself a `MessagePayload` referencing the original
`id`. When two peers edit the same message concurrently (rare on a small
LAN room, but possible), the Lamport-clock last-write-wins rule from
`27_SYNC_REPLICATION_MODEL.md` decides which edit wins; the losing edit is
kept in local history (not silently dropped) so it can be surfaced if ever
needed for debugging, but not shown in the primary thread.

## Search

Full-text search is local-only (SQLite FTS5 against the local replica of
message history) - it is never a network operation. See
`09_DATABASE_SCHEMA.md`.
</file>

<file path="docs/08_FILE_TRANSFER_PROTOCOL.md">
# 08 - File Transfer Protocol

Implemented by the dedicated `zenpaws-transfer` crate - never coupled to
chat logic (the chat system only ever sees file *metadata*; the transfer
system is what moves bytes).

## Metadata (sent automatically with the message)

Stored in SQLite (`09_DATABASE_SCHEMA.md`) and included in the
`MessagePayload`: `fileId`, `messageId`, `senderId`, `mimeType`, `size`,
`checksum` (SHA-256), `createdAt`, `originalFilename`, `storedFilename`
(UUID-based).

## Content transfer flow (non-image files)

```mermaid
sequenceDiagram
    participant R as Receiver
    participant S as Sender
    R->>S: TransferMessage::Request { file_id }
    S->>R: TransferMessage::Offer { total_chunks, chunk_size }
    loop each chunk
        S->>R: TransferMessage::Chunk { index, bytes }
        R->>S: TransferMessage::ChunkAck { index }
    end
    R->>R: verify SHA-256 against stored checksum
    R->>S: TransferMessage::Complete | TransferMessage::Failed
```

Images differ only in that the `Request` is sent automatically by the
receiver on message arrival (auto-transfer, up to the 25MB threshold in
`05_TECHNICAL_DECISIONS.md`) instead of waiting for a user click.

## Chunking

Fixed chunk size (tuned in Phase 7 against real LAN throughput; default
starting point 256KB). Each chunk is acknowledged individually, which is
what makes pause/resume possible - resume simply re-requests from the last
acknowledged chunk index rather than restarting the whole transfer.

## Pause / resume / cancel

- **Pause**: receiver stops sending `ChunkAck`; sender stops sending new
  chunks after a short in-flight window, keeps the send state alive.
  **Resume**: receiver re-sends the last acked index, sender continues.
- **Cancel**: either side sends `TransferMessage::Cancel`; partial data on
  the receiver's side is deleted, not left as a truncated file.

## Integrity validation

Full-file SHA-256 checked after the last chunk lands, before the file is
moved out of the temporary transfer staging area (`26_STORAGE_STRATEGY.md`)
into its final downloads location. A checksum mismatch triggers
`TransferMessage::Failed`, not a silently-corrupt file.

## Upload limit

2 GB per file (from `05_TECHNICAL_DECISIONS.md`). The sender rejects an
attach attempt above this before any transfer begins, with a clear reason
shown to the user - not a silent truncation.
</file>

<file path="docs/10_PET_ENGINE_SPECIFICATION.md">
# 10 - Pet Engine Specification

## Window architecture

Each active pet is its own transparent, always-on-top, click-through-capable
Tauri window (`pet-<slug>-<instance>`), matched against the `pet-*` capability
scope in `12_SECURITY_MODEL.md` - minimal IPC surface, no filesystem/network
access from that window's webview context.

The pet engine (`zenpaws-pets`) must keep running even if the chat system is
disabled - it subscribes to chat-originated events via the Event Bus but has
no compile-time or runtime dependency on `zenpaws-network`'s internals.

## Data-driven pet definition

```
assets/pets/<pet>/
  manifest.json
  sprites/
  sounds/
  LICENSE.txt          -- required; see 23_ASSET_LICENSES.md
```

`manifest.json` shape (refined during Phase 4 implementation):

```json
{
  "id": "cat",
  "displayName": "Cat",
  "states": {
    "idle": { "sprite": "sprites/idle.png", "frameCount": 4, "fps": 6 },
    "walk": { "sprite": "sprites/walk.png", "frameCount": 6, "fps": 10 }
  },
  "sounds": { "notification": "sounds/notify.wav" },
  "defaultScale": 1.0
}
```

No hardcoded pets in application code - adding a pet is adding a directory
that satisfies this schema, validated at load time.

## States

`idle`, `walk`, `run`, `jump`, `sleep`, `typing`, `notification`,
`celebrate`, `fight`, `play`, `away`, `offline`.

## Behaviors

Chase cursor, react to rapid cursor movement, interact with other local
pets, celebrate on new messages, display chat notifications, idle
wandering. All behaviors are local-only in this build - see Synchronization
below for what is and isn't networked.

## Synchronization (future: one pet per LAN user)

**Never synchronized**: coordinates, frame positions, per-frame animation
state - this would be bandwidth-expensive and pointless at 50 peers.

**Synchronized** (discrete events only): `typing`, `playing`, `sleeping`,
`celebrating`, `notification`, `away`, `offline` - carried as
`PetStateEvent` in the message envelope (`07_MESSAGE_PROTOCOL.md`).

**Rate limit: max 2 broadcasts/sec/pet**, with debounce (don't re-send the
same state repeatedly) and deduplication (a receiving peer ignores a
duplicate event) implemented in `zenpaws-pets`, not left to network-layer
throttling alone.

## Performance budget

< 1% CPU per active pet on the target hardware (`13_PERFORMANCE_GUIDELINES.md`).
Performance Mode (`26_STORAGE_STRATEGY.md`'s sibling concept in
`13_PERFORMANCE_GUIDELINES.md`) reduces pet FPS and effect complexity when
enabled.
</file>

<file path="docs/11_UI_UX_GUIDELINES.md">
# 11 - UI/UX Guidelines

## Component architecture

React function components only, typed props (no `any`, no untyped
`children: React.ReactNode` left implicit - always declared). Structure per
`15_AI_AGENT_RULES.md`'s decomposition rules: a component file stays under
200 lines by extracting hooks (`useX.ts`), pure helpers (`utils/`), and
sub-components rather than growing one file.

## Styling

Utility-first CSS (Tailwind) is the working assumption for velocity, kept
consistent with Motion for anything that's actually animated rather than
just transitioned via CSS. This is revisited concretely once Phase 1's
frontend shell exists - recorded as a decision to confirm, not yet locked.

## Motion usage

- Use `motion/react`'s declarative `animate`/`variants` API for state-driven
  UI motion (message send/receive, panel open/close, pet reactions bridging
  into UI, e.g. a notification toast).
- Respect `useReducedMotion()` - every non-essential animation must check it
  and fall back to an instant or minimal transition.
- Prefer one orchestrated moment over scattered micro-animations; excessive
  ambient motion both hurts the low-end-hardware CPU budget and reads as
  noisy rather than polished.

## Accessibility

Keyboard focus must be visible everywhere; every interactive element
reachable by keyboard; color contrast meets WCAG AA at minimum; screen
reader labels on icon-only buttons (e.g. reaction picker, context menu
triggers).

## Virtualized list UX

The 100k-message list must not visibly "pop" content in - use skeleton
placeholders or a fade-in tied to `useReducedMotion`-aware Motion, sized to
avoid layout shift as rows mount.

## Empty and error states

Per the interface's own voice, not a person's: explain what happened and
what to do next, plainly. An empty DM list says what a person can do next
(start a conversation with an online peer), not just "No messages yet."

## Detailed visual design (colors, type, layout)

Deferred to when actual screens are built (Phase 5+) rather than speculated
here - a full token system, aesthetic direction, and asset production
happens against real content, not this planning document, following the
`frontend-design` guidance available in the build environment.
</file>

<file path="docs/12_SECURITY_MODEL.md">
# 12 - Security Model

## Threat model - explicit boundary

**Protects against**: peer identity spoofing on the LAN, casual traffic
snooping by other devices on the same subnet, path traversal / malicious
filenames from a peer, oversized/malformed transfer abuse.

**Does not protect against** (see `03_PROJECT_SCOPE.md` non-goals): a
sophisticated attacker who already has a privileged foothold on the LAN or
on a peer's machine itself; this is a trusted-LAN tool, not a
zero-trust-network product.

## Transport security

TLS via `rustls`, with **TOFU (Trust On First Use)** peer-identity pinning:

1. On first contact with a peer UUID, the connecting peer records that
   peer's certificate/public-key fingerprint (`peers.pubkey_fingerprint` in
   `09_DATABASE_SCHEMA.md`).
2. On every subsequent connection, the presented fingerprint must match the
   pinned one - a mismatch is surfaced to the user as a warning (possible
   impersonation or a peer that reinstalled/regenerated identity), not
   silently accepted or silently rejected.
3. This is how "UUID isn't IP" identity claims get verified - a UUID+
   username alone is just an assertion; the pinned key is what makes it
   costly to spoof after first contact.

## File handling

- **Filename sanitization**: `originalFilename` is display-only, never used
  as a filesystem path; `storedFilename` is always a UUID assigned by the
  receiver.
- **Path traversal protection**: all file writes are canonicalized and
  checked to stay within the configured storage root before any write
  occurs.
- **Checksum validation**: SHA-256, verified before a transferred file is
  considered complete (`08_FILE_TRANSFER_PROTOCOL.md`).
- **Upload limits**: 2 GB per file, enforced before transfer begins.

## Tauri v2 capability scoping (per window)

Tauri v2's per-window capability files (`src-tauri/capabilities/*.json`)
replace v1's single global allowlist - this is used for real least-privilege
separation:

- **`main-window`**: the full legitimate command surface - filesystem
  access scoped to the app's own data/downloads directories, dialog,
  clipboard, notification, tray.
- **`pet-*` windows**: minimal - window positioning/transparency/click-
  through and the specific pet-state IPC commands. No filesystem, network,
  or dialog access, since pet windows render frequently-updated asset
  content and don't need it.

The exact permission-string list in each capability file is finalized
against Tauri's generated schema (`src-tauri/gen/schemas/`, produced on
first `cargo tauri dev` once plugins are registered) rather than guessed -
the Phase 1 capability files ship a first-pass list to be verified then.

Whenever a new `#[tauri::command]` is added in any phase, its reachability
from a given window is re-checked against that window's capability file -
a command existing in Rust does not make it callable; the capability grant
does.

## Database access

All SQL is parameterized/prepared statements through `zenpaws-database`'s
typed functions. The frontend never sends raw SQL - this was also a factor
in choosing `rusqlite` over `tauri-plugin-sql` in `05_TECHNICAL_DECISIONS.md`.

## Local data at rest

Not encrypted at rest in this build (LAN-trust threat model, see above). If
that changes, it's a new decision recorded here, not an assumption.
</file>

<file path="docs/13_PERFORMANCE_GUIDELINES.md">
# 13 - Performance Guidelines

## Target hardware

Dual-core Intel i3-equivalent CPU, 4GB RAM, HDD supported, integrated GPU.

## Budgets

| Metric | Target |
|---|---|
| Idle RAM | < 200 MB |
| Idle CPU | < 2% |
| Pet engine | < 1% CPU per active pet |
| 100,000-message history | Smooth scrolling (virtualized, no jank) |
| Large image history | Lazy loaded, never all-at-once |

These are requirements verified in Phase 10, not aspirations - Phase 9
exists specifically to close any gap between where Phases 1–8 land and
these numbers.

## How each requirement is met

- **Idle RAM/CPU**: no polling loops; mDNS and heartbeat are event/timer
  driven with conservative intervals tuned for the 50-peer LAN scale
  (`06_NETWORK_ARCHITECTURE.md`); the frontend doesn't re-render on data it
  doesn't display (see Zustand/TanStack Query selector discipline below).
- **Pet engine CPU**: sprite-sheet animation at a capped FPS (data-driven
  per pet, `10_PET_ENGINE_SPECIFICATION.md`), Performance Mode reduces this
  further; pet windows are otherwise idle (no continuous redraw when a pet
  is in `idle` state beyond its own low-fps animation).
- **100k-message scrolling**: `@tanstack/react-virtual` renders only visible
  rows; SQLite pagination is keyset-based (`created_at` cursor, not
  `OFFSET`) per `09_DATABASE_SCHEMA.md`; FTS5 search is indexed, not a table
  scan.
- **Lazy image loading**: thumbnail-first, original fetched only when the
  user actually views it full-size (`08_FILE_TRANSFER_PROTOCOL.md`'s
  auto-transfer threshold already limits what arrives unprompted).

## Frontend render discipline

- Zustand selectors are narrow - a component subscribes to the smallest
  slice of state it needs, not the whole store, to avoid unnecessary
  re-renders on unrelated state changes.
- TanStack Query cache keys are structured so a chat-list update doesn't
  invalidate unrelated queries (e.g. settings, pet state).
- Motion animations respect `useReducedMotion` and are scoped to elements
  actually changing - no blanket page-level animation wrappers.

## Performance Mode

A user-facing toggle (`02_PRODUCT_REQUIREMENTS_DOCUMENT.md`) between Full
(max animation/interaction fidelity) and Performance (reduced pet FPS,
reduced effects, lower-frequency UI animation) - implemented as a single
setting read by both `zenpaws-pets` and the frontend's Motion usage, not two
separately-maintained code paths.

## Measuring, not guessing

Phase 9 and Phase 10 use `cargo bench`/`criterion` for Rust-side hot paths
(message insert/query, chunked transfer throughput) and manual profiling
(browser devtools performance tab via the webview, plus OS-level CPU/RAM
monitoring on the actual target-spec hardware where feasible) - see
`20_TESTING_STRATEGY.md`.
</file>

<file path="docs/15_AI_AGENT_RULES.md">
# 15 - AI Agent Rules

Binding rules for whoever (human or AI) implements each phase.

## The ponytail ladder

See `00_PROJECT_CONSTITUTION.md`. Applied before writing any new code, every
time, no exceptions except security/correctness/reliability/accessibility/
type-safety, which are never trimmed for simplicity.

## Dependency documentation check

Before adding or using any dependency, its **current** documentation is
checked - package name, import path, current API shape - not recalled from
training data. This project has already hit one real example of why: the
animation library renamed from `framer-motion` to `motion` with a changed
import path (`motion/react`); using stale knowledge here would have shipped
a deprecated package. Record the check's outcome in `05_TECHNICAL_DECISIONS.md`
if it's a new dependency, or note it inline in the relevant phase's memory
entry if it's confirming an existing one.

## File size limit

**No code file (`.rs`, `.ts`, `.tsx`) exceeds 200 lines.** When a file
approaches this, split by responsibility:

- **React components**: extract sub-components, custom hooks (`useX.ts`),
  and pure presentational pieces before a component file grows past the
  limit. A component that both fetches data and renders a complex tree is
  two files, not one.
- **Rust**: split a module into submodules (`mod.rs` + `handlers.rs` +
  `types.rs`, etc.) along natural seams - request handling vs. domain
  types vs. error types are typically separate files even within one
  Tauri command group.
- **Utils/helpers**: a `utils.ts` that's becoming a junk drawer is a sign
  it needs to become `utils/format.ts`, `utils/validation.ts`, etc. - named
  by what the functions do, not a generic catch-all.

## Component/utility structure convention

```
src/
  app/            # app shell, providers, routing (if any)
  features/       # one folder per feature area (chat/, pets/, transfers/, settings/)
    chat/
      components/
      hooks/
      utils/
  components/     # cross-feature shared UI primitives
  hooks/          # cross-feature shared hooks
  utils/          # cross-feature pure helpers (no side effects, no React)
  stores/         # Zustand stores
  queries/        # TanStack Query hooks/keys
```

A helper used by exactly one feature lives in that feature's `utils/`; a
helper used by two or more lives in the top-level `utils/`. Don't
pre-emptively generalize a single-use helper into the shared folder.
always use kebab-case for file & folder names. don't ever use "—", "…", or any other special characters.

## Type safety

- `never use any` - literally: no `any`, no `as any` escape hatches, no
  implicit `any` from an unannotated parameter. Biome's
  `linter.rules.suspicious.noExplicitAny` is `error`, and `tsconfig.json`
  has `strict: true` plus explicit `noImplicitAny: true`.
- When a type is genuinely unknown at a boundary (e.g. a Tauri event
  payload), use `unknown` and narrow it with a type guard or a validation
  step - never assert past the boundary with `any`.
- Rust: `#![deny(clippy::all)]`-equivalent via the workspace lint table in
  `05_TECHNICAL_DECISIONS.md`; avoid `unwrap()`/`expect()` outside tests and
  truly-infallible cases - propagate `Result` with `thiserror`/`anyhow`
  instead.

## Linters/formatters - must pass before a phase is considered done

- `biome check .` (lint + format check)
- `cargo clippy --all-targets -- -D warnings`
- `cargo fmt --check`
- `tsc -b --noEmit` (typecheck)

## No placeholder implementations

A crate or module that legitimately has no logic yet, because its phase
hasn't started, stays an honest, compiling stub with a doc comment pointing
to the spec it will implement - never a fake/mocked version of the feature
it will eventually be.
</file>

<file path="docs/21_DEPLOYMENT_GUIDE.md">
# 21 - Deployment Guide

Placeholder-appropriate for the current stage (Phase 1 complete, no
distributable build yet) - filled in with real steps starting Phase 9/10.

## Bundler targets (Tauri v2)

- **Windows**: MSI and/or NSIS installer.
- **macOS**: `.app` bundle, DMG.
- **Linux**: `.deb`, AppImage (broadest compatibility across distros).

Configured via `tauri.conf.json`'s `bundle` section - left at defaults in
Phase 1, tuned per-platform in Phase 10.

## Versioning

Semantic versioning (`major.minor.patch`), starting at `0.1.0` through the
phased build; `1.0.0` reserved for the first build that passes Phase 10's
full testing checklist.

## Code signing

Out of scope for the phased build itself - noted here as a real requirement
for any actual public distribution (unsigned installers trigger OS security
warnings), to be revisited before any release beyond internal testing.

## What's deferred

Auto-update (`tauri-plugin-updater`) is not part of this build's scope -
LAN peers are expected to be manually kept in sync on the same app version
for now; a mismatched protocol version is detected and surfaced per
`06_NETWORK_ARCHITECTURE.md` rather than silently breaking.
</file>

<file path="docs/22_CONTRIBUTING_GUIDE.md">
# 22 - Contributing Guide

## Workflow

- One phase = one atomic commit (`phase-N: <summary>`), per
  `00_PROJECT_CONSTITUTION.md`. Within a phase, smaller commits are fine as
  long as the phase's final state is a clean, buildable commit.
- After Phase 10, ordinary work follows Conventional Commits
  (`feat:`, `fix:`, `refactor:`, `docs:`, `test:`) since the phase-gate
  discipline is specific to the initial build.

## Before every commit

```bash
# Frontend
bun run check
bun run typecheck

# Backend (run from src-tauri/)
cargo fmt --check
cargo clippy --all-targets -- -D warnings
cargo nextest run
```

All four must pass. This isn't optional CI-will-catch-it - run it locally
first.

## Code review checklist

- [ ] No file over 200 lines (see `15_AI_AGENT_RULES.md` for how to split).
- [ ] No `any` in TypeScript, no unexplained `unwrap()`/`expect()` in Rust.
- [ ] New dependency? Its current docs were checked, and the decision (with
      rationale) is recorded in `05_TECHNICAL_DECISIONS.md`.
- [ ] New Tauri command? Its capability-file reachability was updated in
      `12_SECURITY_MODEL.md`'s per-window scoping.
- [ ] Touches the DB schema? A new migration file was added - an existing
      one was never edited.
- [ ] Docs (`16_PROJECT_MEMORY.md`, `17_CHANGELOG.md`, and any spec doc the
      change affects) updated in the same PR, not deferred.

## Ponytail self-check

Before opening a PR, answer in the description: what did this change decide
NOT to build (YAGNI), what did it reuse instead of adding, and what actually
needed new code? This mirrors the phase-level self-review in
`14_DEVELOPMENT_PHASES.md`'s Definition of Done.
</file>

<file path="docs/23_ASSET_LICENSES.md">
# 23 - Asset Licenses

Tracks the license of every pet sprite/sound asset. **No pet ships without
an entry here and a matching `LICENSE.txt` in its own
`assets/pets/<pet>/LICENSE.txt`** - this is enforced, not a suggestion, per
`00_PROJECT_CONSTITUTION.md`'s rule against copying art assets from any
reference project without permission.

## Status: empty - no assets created yet (Phase 0/1 complete, Phase 4 not started)

| Pet | Asset type | Source | License | Attribution required |
|---|---|---|---|---|
| cat | - | - | - | - |
| dog | - | - | - | - |
| fox | - | - | - | - |
| rabbit | - | - | - | - |
| slime | - | - | - | - |
| ghost | - | - | - | - |
| parrot | - | - | - | - |

Rows are filled in as each pet's assets are produced or sourced in Phase 4.
"Source" must name either "original for this project" or a specific,
checkable license source (e.g. a named asset pack and its license terms) -
never left blank once a pet ships.
</file>

<file path="docs/24_API_REFERENCE.md">
# 24 - API Reference (Tauri Commands & Events)

Filled in progressively as each phase implements real commands - empty at
Phase 1 since no feature commands exist yet beyond app scaffolding. This is
the canonical list; `12_SECURITY_MODEL.md`'s capability files must stay in
sync with whatever's added here.

## Format for each entry (used from Phase 2 onward)

```
### command_name
- **Window scope**: main-window | pet-* | both
- **Payload**: <TypeScript type>
- **Returns**: <TypeScript type>
- **Errors**: <enumerated error cases>
- **Added in**: Phase N
```

## Commands (none yet)

_(Phase 2+ will populate this as `zenpaws-network`, `zenpaws-database`, etc.
expose their first real commands.)_

## Events (none yet)

_(Tauri events emitted from backend to frontend - e.g. `peer:connected`,
`message:received` - documented here once Phase 2/5 introduce them.)_
</file>

<file path="docs/25_EVENT_BUS_ARCHITECTURE.md">
# 25 - Event Bus Architecture

## Why

`zenpaws-network`, `zenpaws-pets`, `zenpaws-transfer`, and
`zenpaws-notifications` never call each other's functions directly - they
publish and subscribe through an event bus owned by `zenpaws-shared`. This
is what lets the pet engine keep working with chat disabled, and what keeps
any one crate's internals from leaking into another's.

## Backend implementation

A typed `EventBus` wrapping `tokio::sync::broadcast`, defined once in
`zenpaws-shared`:

```rust
pub enum ZenPawsEvent {
    PeerConnected { peer_uuid: Uuid },
    PeerDisconnected { peer_uuid: Uuid },
    MessageReceived { message_id: Uuid, room: RoomTarget },
    MessageSent { message_id: Uuid },
    TransferProgress { file_id: Uuid, percent: u8 },
    TransferComplete { file_id: Uuid },
    PetStateChanged { pet_instance: Uuid, state: PetState },
}
```

Each crate that needs to react to events holds a `Receiver<ZenPawsEvent>` and
matches on the variants it cares about - no crate needs to know who
published an event, only the shared enum shape.

## Frontend bridge

The Rust side re-emits relevant `ZenPawsEvent` variants as Tauri events
(`app.emit("zenpaws://event", payload)`); the frontend has one typed event
registry (`src/app/events.ts`) mapping each Tauri event name to its exact
TypeScript payload type - no `any` on either side of that bridge, per
`15_AI_AGENT_RULES.md`.

## Example flows

```mermaid
graph LR
    NET[zenpaws-network] -->|MessageReceived| BUS[EventBus]
    BUS --> PETS[zenpaws-pets: maybe celebrate]
    BUS --> NOTIF[zenpaws-notifications: native + speech bubble]
    BUS --> TAURI[Tauri emit → frontend]
```

## What the bus is not

Not a general-purpose message queue with persistence or replay - it's an
in-process pub/sub for decoupling modules within one running app instance.
Cross-peer messaging is the LAN protocol (`07_MESSAGE_PROTOCOL.md`), a
separate concern entirely.
</file>

<file path="docs/26_STORAGE_STRATEGY.md">
# 26 - Storage Strategy

Prevents uncontrolled disk growth on a long-running install. Enforced by
`zenpaws-storage`, with limits configurable in Settings but shipping with
sane defaults.

## Quotas (defaults - user-adjustable in Settings, Phase 8)

| Category | Default limit | Eviction policy |
|---|---|---|
| Image cache (originals) | 2 GB | LRU eviction once over limit |
| Thumbnail cache | 500 MB | LRU eviction; thumbnails are cheap to regenerate |
| Temporary in-progress transfers | Bounded by free disk space + the 2GB per-file limit (`08_FILE_TRANSFER_PROTOCOL.md`) | Cleared immediately on cancel/failure, never left as orphaned partial files |
| Logs | 50 MB total, rotated | Oldest rotated out first |

## Directory layout (under the OS-appropriate app data dir)

```
<app-data>/
  zenpaws.sqlite            # WAL mode - also produces zenpaws.sqlite-wal/-shm
  downloads/                # completed, user-facing file downloads
  cache/
    images/
    thumbnails/
  transfers/tmp/            # in-progress chunked transfers, never user-visible
  logs/
```

## Why this matters for the low-end-hardware target

Unbounded caches on a 4GB-RAM/HDD target machine degrade the whole system,
not just ZenPaws - quotas here are a correctness requirement for the
performance budgets in `13_PERFORMANCE_GUIDELINES.md`, not just tidiness.

## Cleanup triggers

Quota checks run on app startup and after each completed transfer/image
receive - not on a background timer that would itself cost idle CPU,
consistent with the "avoid unnecessary polling" rule in
`06_NETWORK_ARCHITECTURE.md`.
</file>

<file path="docs/27_SYNC_REPLICATION_MODEL.md">
# 27 - Sync & Replication Model

## Hostless replication

There is no server. Every peer stores the full shared-room history (subject
to whatever local retention the user configures - unbounded by default),
its DM histories, and its own settings. This is what "hostless" means here:
no peer is more authoritative than another by role, only by clock ordering
(below).

## Message versioning: Lamport logical clocks

Each peer maintains a local counter. Every message/edit/delete carries
`(peer_uuid, counter)` as its Lamport clock, incremented on every local
event and updated to `max(local, received) + 1` on receiving a remote
event - the standard Lamport rule, giving a total, consistent ordering
across peers without synchronized wall clocks.

## Conflict resolution: last-write-wins by Lamport clock

When two peers concurrently edit or delete the same message, the edit with
the higher `(counter, peer_uuid)` tuple (peer_uuid as tiebreaker) wins and
becomes the visible state. The losing edit is retained in local storage,
not deleted - it's simply not what's rendered as current - so a future
"edit history" feature isn't foreclosed, even though it's not in this
build's scope.

**Why LWW and not a CRDT**: messages here are simple field-level edits
(text content, deleted flag), not concurrently-merged structured data like
collaborative text. A CRDT would solve a merge problem this app doesn't
have - per the ponytail ladder, the simpler mechanism that correctly
handles the actual conflict shape wins.

## Sync-on-reconnect (late join / catch-up)

```mermaid
sequenceDiagram
    participant A as Rejoining Peer
    participant B as Already-Connected Peer
    A->>B: SyncRequest { since_clock: A's last known clock per room }
    B->>A: stream of MessagePayload for anything newer than since_clock
    A->>A: apply in Lamport order, resolving conflicts via LWW
```

A rejoining peer sends its last-known clock per room to every peer it
reconnects to; each connected peer streams back anything newer. If multiple
peers respond with overlapping history, duplicates are detected by message
`id` and simply not reapplied - idempotent by construction.

## What this deliberately doesn't solve

A peer that's offline for a very long time relative to how much history
other peers have pruned (if/when retention limits are added later) may have
gaps; this build ships with unbounded local retention specifically to avoid
that edge case for now - revisit this doc first if retention limits are
ever introduced.
</file>

<file path="docs/28_PET_SPRITE_ASSET_GENERATION_PROMPTS.md">
# 28 - Pet Sprite Asset Generation Prompts

Companion to `10_PET_ENGINE_SPECIFICATION.md` and `23_ASSET_LICENSES.md`.

This is a working prompt kit for producing the sprite sheets Phase 4 needs. It is asset guidance, not application code.

## Read this before generating anything

General-purpose image generators are not well suited to independent animation frames: proportions, palette, pose style, and alignment drift between generations. Use a sprite-sheet consistency tool that locks a character reference and generates animation states against it, such as PixelLab or a comparable current tool. Check current pricing, terms of service, and commercial-use terms before committing to a tool.

Recommended workflow:

1. Generate one character reference sheet per pet.
2. Use that locked reference as image input for every state animation.
3. Touch up palette, frame alignment, and loop seams in Aseprite, Piskel, or the sprite tool editor.
4. Record provenance and commercial-use terms in `23_ASSET_LICENSES.md` before shipping.

If a dedicated sprite tool is unavailable, use the character reference as image-to-image input for every subsequent generation. Never generate animation frames text-only.

## Global style bible

Paste this verbatim into every prompt:

```text
Style: cute chibi 2D game sprite, flat cel-shading, thin 1px dark outline,
soft rounded shapes, big expressive eyes, minimal shading with a single
top-left light source. NOT photorealistic, NOT painterly, NOT 3D-rendered.

Side-view profile facing right, full body visible, centered in frame.
Background: fully transparent alpha channel.
Canvas: 128x128px per frame. Character is drawn on an internal 32x32 pixel
grid then upscaled with nearest-neighbor. Crisp pixel edges, no blur,
anti-aliasing artifacts, or smooth gradients.
Palette: warm, friendly, moderately saturated and consistent across all pets.
```

## Character reference prompts

Generate one reference before any state frames. These are proposals, not locked designs.

### Cat

```text
[GLOBAL STYLE BIBLE]
Subject: small round chibi cat, orange tabby stripes, cream belly, short
triangular ears, long loosely curled tail, bright green eyes, closed-mouth
content expression, sitting neutral pose facing right.
```

### Dog

```text
[GLOBAL STYLE BIBLE]
Subject: small chibi shiba-inu-style dog, cream and tan fur, one ear perked
and one slightly folded, curled tail over back, dark round eyes, open friendly
smile, sitting neutral pose facing right.
```

### Fox

```text
[GLOBAL STYLE BIBLE]
Subject: small chibi fox, bright orange fur, white chest and tail tip, large
pointed ears, bushy tail, amber eyes, sly but friendly closed-mouth smile,
standing neutral pose facing right.
```

### Rabbit

```text
[GLOBAL STYLE BIBLE]
Subject: small chibi rabbit, soft grey-white fur, one long ear standing and
one flopped, round fluffy tail, pink nose, large dark eyes, sitting neutral
pose facing right.
```

### Slime

```text
[GLOBAL STYLE BIBLE]
Subject: small translucent teal-green gel blob, glossy upper-left highlight,
two simple black dot eyes, no mouth or a tiny curved smile, rounded teardrop
body, resting neutral pose with no legs.
```

### Ghost

```text
[GLOBAL STYLE BIBLE]
Subject: small pastel lavender-white ghost, rounded head, wavy scalloped
bottom edge, simple round dot eyes, small open o mouth or gentle smile,
slight forward-lean floating neutral pose facing right.
```

### Parrot

```text
[GLOBAL STYLE BIBLE]
Subject: small chibi parrot, vivid red-blue-green macaw-style plumage, short
curved beak, round black eyes, perched neutral pose on both feet facing right,
wings folded at sides.
```

## Per-state animation prompt template

Use the locked reference as an image input.

```text
[GLOBAL STYLE BIBLE]
Using the attached character reference, generate a looping animation sequence
for the "<STATE>" action: <STATE DESCRIPTION>.
Frame count: <N>. Keep proportions, palette, outline weight, and silhouette
identical to the reference in every frame. Only the pose changes.
```

## State animation guide

| State | Frames | FPS | Motion |
|---|---:|---:|---|
| `idle` | 4 | 6 | Subtle breathing or bob, occasional blink, loop. |
| `walk` | 6 | 10 | Side-view walk loop. Parrot uses a hop cycle. |
| `run` | 6 | 14 | Faster exaggerated walk. Parrot uses wing-flutter glide. |
| `jump` | 5 | 10 | Crouch, launch, apex, falling, land; non-looping. |
| `sleep` | 3 | 2 | Curled up, slow rise/fall, optional alternating Zzz; loop. |
| `typing` | 4 | 8 | Tiny alternating paw or wing taps; loop. |
| `notification` | 3 | 8 | Head or ear perk alert; non-looping. |
| `celebrate` | 6 | 12 | Joyful bounce or spin, wings or arms up. |
| `fight` | 5 | 10 | Playful non-violent cartoon mock-battle only. |
| `play` | 5 | 10 | Batting or chasing an implied toy; loop. |
| `away` | 3 | 3 | Slow distracted look-around, slightly desaturated; loop. |
| `offline` | 1 | - | Static greyscale or desaturated pose. |

## Export and naming convention

```text
assets/pets/<pet>/
  manifest.json
  sprites/
    idle.png
    walk.png
    run.png
    jump.png
    sleep.png
    typing.png
    notification.png
    celebrate.png
    fight.png
    play.png
    away.png
    offline.png
  sounds/
  LICENSE.txt
```

Export one transparent PNG per state as a horizontal sprite strip: N frames, each 128px wide and 128px tall, ordered frame 1 through N left to right. The `frameCount` in `manifest.json` controls slicing.

## Before any pet ships

Fill in the pet row in `23_ASSET_LICENSES.md`, including tool commercial-use terms and any manual Aseprite or Piskel edits.
</file>

<file path="src/app/app.provider.tsx">
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { type ReactNode, useState } from "react";

interface AppProvidersProps {
  children: ReactNode;
}

/**
 * Wraps the app in the providers every feature will need. Kept as its own
 * file per docs/15_AI_AGENT_RULES.md's structure convention - App.tsx stays
 * focused on layout, this file owns provider setup.
 */
export function AppProviders({ children }: AppProvidersProps): ReactNode {
  const [queryClient] = useState(
    () =>
      new QueryClient({
        defaultOptions: {
          queries: {
            // Local Tauri-IPC "queries" aren't network calls with latency
            // to hide - no need for aggressive background refetching.
            refetchOnWindowFocus: false,
            retry: 1,
          },
        },
      }),
  );

  return <QueryClientProvider client={queryClient}>{children}</QueryClientProvider>;
}
</file>

<file path="src/app/app.tsx">
import type { ReactNode } from "react";
import { AppProviders } from "./app.provider";
import { AppShell } from "./app-shell";

/**
 * Root component. Deliberately minimal at Phase 1 - a real layout, routing
 * (if any), and feature areas (chat/, pets/, transfers/, settings/) are
 * added starting Phase 2 onward, per docs/15_AI_AGENT_RULES.md's structure
 * convention. This is not a placeholder feature; it's the honest current
 * state of the app shell.
 */
export function App(): ReactNode {
  return (
    <AppProviders>
      <AppShell />
    </AppProviders>
  );
}
</file>

<file path="src/features/chat/components/message-composer.tsx">
import { useQueryClient } from "@tanstack/react-query";
import { invoke } from "@tauri-apps/api/core";
import { type FormEvent, useRef, useState } from "react";

interface MessageComposerProps {
  onCancelReply: () => void;
  replyTo: string | null;
  room: string;
}

export function MessageComposer({ onCancelReply, replyTo, room }: MessageComposerProps) {
  const [body, setBody] = useState("");
  const [error, setError] = useState<string | null>(null);
  const client = useQueryClient();
  const typingTimeout = useRef<number | null>(null);

  function updateTyping(nextBody: string) {
    setBody(nextBody);
    void invoke("set_typing", { isTyping: nextBody.trim().length > 0, room });
    if (typingTimeout.current !== null) {
      window.clearTimeout(typingTimeout.current);
    }
    typingTimeout.current = window.setTimeout(() => {
      void invoke("set_typing", { isTyping: false, room });
    }, 2000);
  }

  async function submit(event: FormEvent<HTMLFormElement>) {
    event.preventDefault();
    try {
      await invoke("send_message", { body, replyTo, room });
      setBody("");
      void invoke("set_typing", { isTyping: false, room });
      setError(null);
      await client.invalidateQueries({ queryKey: ["messages", room] });
    } catch (reason: unknown) {
      setError(reason instanceof Error ? reason.message : "Could not send message.");
    }
  }

  return (
    <form onSubmit={submit}>
      {replyTo ? (
        <p>
          Replying to {replyTo}{" "}
          <button onClick={onCancelReply} type="button">
            Cancel reply
          </button>
        </p>
      ) : null}
      <label htmlFor="message-body">Message</label>
      <textarea
        id="message-body"
        onBlur={() => void invoke("set_typing", { isTyping: false, room })}
        onChange={(event) => updateTyping(event.target.value)}
        value={body}
      />
      {error ? <p role="alert">{error}</p> : null}
      <button disabled={body.trim().length === 0} type="submit">
        Send
      </button>
    </form>
  );
}
</file>

<file path="src/features/chat/components/message-list.tsx">
import { useVirtualizer } from "@tanstack/react-virtual";
import { useRef } from "react";
import { type ChatMessage, useInfiniteMessages } from "../../../queries/messages";
import { MessageItem } from "./message-item";

interface MessageListProps {
  onReply: (message: ChatMessage) => void;
  room: string;
}

export function MessageList({ onReply, room }: MessageListProps) {
  const scrollRef = useRef<HTMLDivElement>(null);
  const messages = useInfiniteMessages(room);
  const items = messages.data?.pages.flat() ?? [];
  const rows = useVirtualizer({
    count: items.length,
    estimateSize: () => 52,
    getScrollElement: () => scrollRef.current,
    overscan: 8,
  });

  if (messages.isError) {
    return <p role="alert">Could not load messages.</p>;
  }

  return (
    <div aria-label="Messages" ref={scrollRef} style={{ height: "100%", overflow: "auto" }}>
      {messages.hasNextPage ? (
        <button onClick={() => void messages.fetchNextPage()} type="button">
          Load older messages
        </button>
      ) : null}
      <div style={{ height: rows.getTotalSize(), position: "relative" }}>
        {rows.getVirtualItems().map((item) => {
          const message = items[item.index];
          if (!message) {
            return null;
          }
          return (
            <div
              key={message.id}
              style={{ left: 0, position: "absolute", top: item.start, width: "100%" }}
            >
              <MessageItem message={message} onReply={onReply} room={room} />
            </div>
          );
        })}
      </div>
    </div>
  );
}
</file>

<file path="src/features/chat/chat-shell.tsx">
import { useState } from "react";
import { usePeerPresence } from "../../hooks/use-peer-presence";
import { useTypingEvents } from "../../hooks/use-typing-events";
import type { ChatMessage } from "../../queries/messages";
import { MessageComposer } from "./components/message-composer";
import { MessageList } from "./components/message-list";
import { RoomTabs } from "./components/room-tabs";
import { directRoom, SHARED_ROOM } from "./utils/room";

export function ChatShell() {
  const [reply, setReply] = useState<ChatMessage | null>(null);
  const [room, setRoom] = useState(SHARED_ROOM);
  const typingPeers = useTypingEvents(room);
  const peers = usePeerPresence();
  const rooms = [
    SHARED_ROOM,
    ...[...peers]
      .filter(([, state]) => state !== "Disconnected")
      .map(([peerId]) => directRoom(peerId)),
  ];

  return (
    <main style={{ display: "grid", gridTemplateRows: "auto minmax(0, 1fr)", height: "100vh" }}>
      <header style={{ padding: "1rem" }}>
        <h1 style={{ margin: 0 }}>Shared room</h1>
        <RoomTabs onSelect={setRoom} rooms={rooms} selectedRoom={room} />
      </header>
      {typingPeers.size > 0 ? <p>{typingPeers.size} peer typing...</p> : null}
      <MessageList onReply={setReply} room={room} />
      <MessageComposer
        onCancelReply={() => setReply(null)}
        replyTo={reply?.id ?? null}
        room={room}
      />
    </main>
  );
}
</file>

<file path="src/hooks/use-app-status.ts">
import { useQuery } from "@tanstack/react-query";
import { invoke } from "@tauri-apps/api/core";

/** Exact shape of the Rust `AppStatus` struct - kept in sync by hand until
 * Phase 2+ introduces generated bindings. No `any`, no unchecked cast: this
 * is the one place that asserts the IPC payload's shape, and it does so
 * with a declared interface, not a type assertion. */
export interface AppStatus {
  name: string;
  version: string;
}

/**
 * Phase 1 smoke-test hook verifying the frontend↔backend IPC bridge works
 * end-to-end. Matches the temporary `app_status` command in
 * `src-tauri/src/commands/mod.rs` - both are removed/replaced once Phase 2
 * introduces the first real feature command.
 */
export function useAppStatus() {
  return useQuery({
    queryFn: () => invoke<AppStatus>("app_status"),
    queryKey: ["app-status"],
  });
}
</file>

<file path="src/queries/messages.ts">
import { type InfiniteData, useInfiniteQuery, useQuery } from "@tanstack/react-query";
import { invoke } from "@tauri-apps/api/core";

export interface ChatMessage {
  authorId: string;
  body: string;
  createdAt: number;
  id: string;
  replyTo: string | null;
  room: string;
}

export interface ChatCursor {
  createdAt: number;
  id: string;
}

interface ListMessagesArgs extends Record<string, unknown> {
  cursor?: ChatCursor;
  limit: number;
  room: string;
}

export function useMessages(room: string, cursor?: ChatCursor) {
  return useQuery({
    queryFn: () =>
      invoke<ChatMessage[]>("list_messages", {
        cursor,
        limit: 50,
        room,
      }),
    queryKey: ["messages", room, cursor?.createdAt, cursor?.id],
    staleTime: Number.POSITIVE_INFINITY,
  });
}

export function useInfiniteMessages(room: string) {
  return useInfiniteQuery<
    ChatMessage[],
    Error,
    InfiniteData<ChatMessage[]>,
    readonly ["messages", string],
    ChatCursor | undefined
  >({
    getNextPageParam: (page) => {
      const last = page.at(-1);
      return last ? { createdAt: last.createdAt, id: last.id } : undefined;
    },
    initialPageParam: undefined as ChatCursor | undefined,
    queryFn: ({ pageParam }) =>
      invoke<ChatMessage[]>("list_messages", {
        cursor: pageParam,
        limit: 50,
        room,
      }),
    queryKey: ["messages", room],
    staleTime: Number.POSITIVE_INFINITY,
  });
}

export function useMessageSearch(query: string) {
  return useQuery({
    enabled: query.trim().length > 0,
    queryFn: () => invoke<ChatMessage[]>("search_messages", { limit: 50, query }),
    queryKey: ["message-search", query],
    staleTime: Number.POSITIVE_INFINITY,
  });
}

export async function listMessages(args: ListMessagesArgs): Promise<ChatMessage[]> {
  return await invoke<ChatMessage[]>("list_messages", args);
}
</file>

<file path="src/styles/global.css">
:root {
  color-scheme: light dark;
}

* {
  box-sizing: border-box;
}

body {
  min-height: 100vh;
  margin: 0;
}

/*
 * Deliberately minimal at Phase 1. Real design tokens (color, type,
 * layout) are established in Phase 5+ against actual screens - see
 * docs/11_UI_UX_GUIDELINES.md.
 */
</file>

<file path="src/main.tsx">
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import { App } from "./app/app";
import "./styles/global.css";

const rootElement = document.getElementById("root");
if (!rootElement) {
  throw new Error("Root element #root not found in index.html");
}

createRoot(rootElement).render(
  <StrictMode>
    <App />
  </StrictMode>,
);
</file>

<file path="src/vite-env.d.ts">
/// <reference types="vite/client" />
</file>

<file path="src-tauri/capabilities/main-window.json">
{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Capabilities for the main chat window - the full legitimate command surface. See docs/12_SECURITY_MODEL.md.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "core:window:allow-minimize",
    "core:window:allow-close",
    "core:window:allow-show",
    "core:window:allow-hide",
    "window-state:default"
  ]
}
</file>

<file path="src-tauri/capabilities/pet-window.json">
{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pet-capability",
  "description": "Minimal capabilities for transparent pet overlay windows - no filesystem, dialog, or shell access. Permission identifiers below are provisional; verify against the schema Tauri generates on first `cargo tauri dev` (see docs/19_KNOWN_ISSUES.md).",
  "windows": ["pet-*"],
  "permissions": [
    "core:event:default",
    "core:window:allow-set-ignore-cursor-events",
    "core:window:allow-set-always-on-top",
    "core:window:allow-set-position",
    "core:window:allow-set-size"
  ]
}
</file>

<file path="src-tauri/crates/zenpaws-database/migrations/0001_init.sql">
CREATE TABLE peers (
    uuid TEXT PRIMARY KEY,
    username TEXT NOT NULL,
    avatar_path TEXT,
    pubkey_fingerprint BLOB NOT NULL,
    last_seen_at INTEGER NOT NULL
);

CREATE TABLE messages (
    id TEXT PRIMARY KEY,
    room TEXT NOT NULL,
    author_uuid TEXT NOT NULL REFERENCES peers(uuid),
    body TEXT NOT NULL,
    reply_to TEXT REFERENCES messages(id),
    lamport_peer TEXT NOT NULL,
    lamport_counter INTEGER NOT NULL,
    created_at INTEGER NOT NULL,
    edited_at INTEGER,
    deleted_at INTEGER
);

CREATE VIRTUAL TABLE messages_fts USING fts5(
    body,
    content='messages',
    content_rowid='rowid'
);

CREATE TABLE reactions (
    message_id TEXT NOT NULL REFERENCES messages(id),
    peer_uuid TEXT NOT NULL REFERENCES peers(uuid),
    emoji TEXT NOT NULL,
    PRIMARY KEY (message_id, peer_uuid, emoji)
);

CREATE TABLE message_acks (
    message_id TEXT NOT NULL REFERENCES messages(id),
    peer_uuid TEXT NOT NULL REFERENCES peers(uuid),
    kind TEXT NOT NULL CHECK (kind IN ('delivered', 'read')),
    acked_at INTEGER NOT NULL,
    PRIMARY KEY (message_id, peer_uuid, kind)
);

CREATE TABLE files (
    id TEXT PRIMARY KEY,
    message_id TEXT NOT NULL REFERENCES messages(id),
    sender_uuid TEXT NOT NULL REFERENCES peers(uuid),
    mime_type TEXT NOT NULL,
    size_bytes INTEGER NOT NULL,
    checksum_sha256 BLOB NOT NULL,
    original_filename TEXT NOT NULL,
    stored_filename TEXT NOT NULL,
    created_at INTEGER NOT NULL
);

CREATE TABLE settings (
    key TEXT PRIMARY KEY,
    value TEXT NOT NULL
);

CREATE INDEX messages_room_created_at ON messages(room, created_at DESC, id DESC);
CREATE INDEX messages_lamport ON messages(lamport_peer, lamport_counter);
</file>

<file path="src-tauri/crates/zenpaws-database/migrations/0002_messages_fts.sql">
CREATE TRIGGER messages_ai AFTER INSERT ON messages BEGIN
    INSERT INTO messages_fts(rowid, body) VALUES (new.rowid, new.body);
END;

CREATE TRIGGER messages_ad AFTER DELETE ON messages BEGIN
    INSERT INTO messages_fts(messages_fts, rowid, body)
    VALUES ('delete', old.rowid, old.body);
END;

CREATE TRIGGER messages_au AFTER UPDATE OF body ON messages BEGIN
    INSERT INTO messages_fts(messages_fts, rowid, body)
    VALUES ('delete', old.rowid, old.body);
    INSERT INTO messages_fts(rowid, body) VALUES (new.rowid, new.body);
END;
</file>

<file path="src-tauri/crates/zenpaws-database/migrations/0003_peer_certificates.sql">
CREATE TABLE peer_certificates (
    peer_uuid TEXT PRIMARY KEY REFERENCES peers(uuid),
    certificate_der BLOB NOT NULL
);
</file>

<file path="src-tauri/crates/zenpaws-database/src/trust.rs">
use std::sync::{Arc, Mutex};

use rusqlite::params;
use zenpaws_shared::{CertificatePin, PeerId, PeerTrustStore, TrustStoreError, TrustedPeer};

use crate::Database;

/// TOFU trust storage backed by the local `SQLite` database.
#[derive(Clone)]
pub struct DatabaseTrustStore {
    database: Arc<Mutex<Database>>,
}

impl DatabaseTrustStore {
    /// Wraps Tauri-managed database state for network trust operations.
    #[must_use]
    pub const fn new(database: Arc<Mutex<Database>>) -> Self {
        Self { database }
    }
}

impl PeerTrustStore for DatabaseTrustStore {
    fn trusted_peer(&self, peer_id: PeerId) -> Result<Option<TrustedPeer>, TrustStoreError> {
        let result = self
            .database
            .lock()
            .map_err(|_| TrustStoreError::Storage("database lock poisoned".to_owned()))?
            .connection
            .query_row(
                "SELECT p.pubkey_fingerprint, c.certificate_der
             FROM peers AS p
             JOIN peer_certificates AS c ON c.peer_uuid = p.uuid
             WHERE p.uuid = ?1",
                [peer_id.as_uuid().to_string()],
                |row| Ok((row.get::<_, Vec<u8>>(0)?, row.get::<_, Vec<u8>>(1)?)),
            );
        match result {
            Ok((fingerprint, certificate_der)) => {
                let pin: [u8; 32] = fingerprint.try_into().map_err(|_| {
                    TrustStoreError::Storage("stored fingerprint has invalid length".to_owned())
                })?;
                Ok(Some(TrustedPeer::new(
                    peer_id,
                    certificate_der,
                    CertificatePin::new(pin),
                )))
            }
            Err(rusqlite::Error::QueryReturnedNoRows) => Ok(None),
            Err(error) => Err(TrustStoreError::Storage(error.to_string())),
        }
    }

    fn trust_first_use(&self, peer: TrustedPeer) -> Result<(), TrustStoreError> {
        if let Some(existing) = self.trusted_peer(peer.peer_id())? {
            return if existing.pin() == peer.pin() {
                Ok(())
            } else {
                Err(TrustStoreError::PinMismatch)
            };
        }
        let database = self
            .database
            .lock()
            .map_err(|_| TrustStoreError::Storage("database lock poisoned".to_owned()))?;
        let transaction = database
            .connection
            .unchecked_transaction()
            .map_err(|error| TrustStoreError::Storage(error.to_string()))?;
        transaction
            .execute(
                "INSERT INTO peers (uuid, username, pubkey_fingerprint, last_seen_at)
             VALUES (?1, 'Unknown', ?2, unixepoch())
             ON CONFLICT(uuid) DO UPDATE SET last_seen_at = unixepoch()",
                params![
                    peer.peer_id().as_uuid().to_string(),
                    peer.pin().as_bytes().to_vec()
                ],
            )
            .map_err(|error| TrustStoreError::Storage(error.to_string()))?;
        transaction
            .execute(
                "INSERT INTO peer_certificates (peer_uuid, certificate_der)
             VALUES (?1, ?2)
             ON CONFLICT(peer_uuid) DO NOTHING",
                params![peer.peer_id().as_uuid().to_string(), peer.certificate_der()],
            )
            .map_err(|error| TrustStoreError::Storage(error.to_string()))?;
        transaction
            .commit()
            .map_err(|error| TrustStoreError::Storage(error.to_string()))?;
        drop(database);
        Ok(())
    }
}

#[cfg(test)]
mod tests {
    use std::sync::{Arc, Mutex};

    use zenpaws_shared::{CertificatePin, PeerTrustStore, TrustedPeer};

    use super::{Database, DatabaseTrustStore, PeerId};

    #[test]
    fn persists_first_use_and_rejects_changed_pin() {
        let database = Arc::new(Mutex::new(
            Database::open_in_memory().expect("database opens"),
        ));
        let trust = DatabaseTrustStore::new(database);
        let peer_id = PeerId::new();
        let first = TrustedPeer::new(peer_id, vec![1], CertificatePin::new([1; 32]));
        let changed = TrustedPeer::new(peer_id, vec![2], CertificatePin::new([2; 32]));

        trust
            .trust_first_use(first.clone())
            .expect("first trust succeeds");
        assert_eq!(
            trust.trusted_peer(peer_id).expect("peer reads"),
            Some(first)
        );
        assert!(trust.trust_first_use(changed).is_err());
    }
}
</file>

<file path="src-tauri/crates/zenpaws-network/src/backoff.rs">
use std::time::Duration;

/// Bounded exponential reconnect delay without polling or busy retries.
#[derive(Clone, Copy, Debug)]
pub struct RetryBackoff {
    attempts: u32,
    initial: Duration,
    maximum: Duration,
}

impl RetryBackoff {
    /// Creates a bounded reconnect backoff policy.
    ///
    /// # Panics
    ///
    /// Panics when the initial delay is zero or greater than the maximum.
    #[must_use]
    pub fn new(initial: Duration, maximum: Duration) -> Self {
        assert!(!initial.is_zero(), "initial retry delay must be nonzero");
        assert!(initial <= maximum, "initial retry delay exceeds maximum");
        Self {
            attempts: 0,
            initial,
            maximum,
        }
    }

    /// Returns the next retry delay and increments the failed-attempt count.
    pub fn next_delay(&mut self) -> Duration {
        let multiplier = 1_u32.checked_shl(self.attempts.min(31)).unwrap_or(u32::MAX);
        self.attempts = self.attempts.saturating_add(1);
        self.initial.saturating_mul(multiplier).min(self.maximum)
    }

    /// Resets the policy after a successful peer connection.
    pub const fn reset(&mut self) {
        self.attempts = 0;
    }
}

#[cfg(test)]
mod tests {
    use std::time::Duration;

    use super::RetryBackoff;

    #[test]
    fn caps_and_resets_retry_delay() {
        let mut backoff = RetryBackoff::new(Duration::from_secs(1), Duration::from_secs(4));

        assert_eq!(backoff.next_delay(), Duration::from_secs(1));
        assert_eq!(backoff.next_delay(), Duration::from_secs(2));
        assert_eq!(backoff.next_delay(), Duration::from_secs(4));
        assert_eq!(backoff.next_delay(), Duration::from_secs(4));
        backoff.reset();
        assert_eq!(backoff.next_delay(), Duration::from_secs(1));
    }
}
</file>

<file path="src-tauri/crates/zenpaws-network/src/discovery.rs">
use base64::{Engine, engine::general_purpose::STANDARD};
use mdns_sd::{Receiver, ServiceDaemon, ServiceEvent, ServiceInfo};
use std::net::{SocketAddr, SocketAddrV4};
use thiserror::Error;
use zenpaws_shared::PeerId;

/// DNS-SD service type used by `ZenPaws` peers.
pub const SERVICE_TYPE: &str = "_zenpaws._tcp.local.";

/// A resolved peer that is ready for pinned outbound TLS.
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct DiscoveredPeer {
    /// Certificate acquired from the peer's mDNS TXT record.
    pub certificate_der: Vec<u8>,
    /// Resolved IPv4 TCP endpoint.
    pub endpoint: SocketAddr,
    /// Stable peer identity.
    pub peer_id: PeerId,
    /// Display name.
    pub username: String,
}

/// mDNS advertisement and browse service for the local LAN.
pub struct MdnsDiscovery {
    daemon: ServiceDaemon,
    events: Receiver<ServiceEvent>,
}

impl MdnsDiscovery {
    /// Starts a local DNS-SD daemon and browses for `ZenPaws` peers.
    ///
    /// # Errors
    ///
    /// Returns an error if the system cannot create the mDNS daemon or start
    /// browsing the `ZenPaws` service type.
    pub fn start() -> Result<Self, DiscoveryError> {
        let daemon = ServiceDaemon::new()?;
        let events = daemon.browse(SERVICE_TYPE)?;
        Ok(Self { daemon, events })
    }

    /// Announces this peer's TCP listener through mDNS.
    ///
    /// # Errors
    ///
    /// Returns an error if the service record is invalid or cannot be
    /// registered with the local mDNS daemon.
    pub fn advertise(
        &self,
        peer_id: PeerId,
        username: &str,
        port: u16,
        certificate_der: &[u8],
    ) -> Result<(), DiscoveryError> {
        let mut properties = std::collections::HashMap::from([
            ("uuid".to_owned(), peer_id.as_uuid().to_string()),
            ("username".to_owned(), username.to_owned()),
        ]);
        let certificate = STANDARD.encode(certificate_der);
        let chunks = certificate.as_bytes().chunks(200).collect::<Vec<_>>();
        properties.insert("certificate_parts".to_owned(), chunks.len().to_string());
        for (index, chunk) in chunks.into_iter().enumerate() {
            let value = std::str::from_utf8(chunk)
                .map_err(|_| DiscoveryError::InvalidCertificateEncoding)?;
            properties.insert(format!("certificate_{index}"), value.to_owned());
        }
        let instance_name = format!("zenpaws-{}", peer_id.as_uuid());
        let service = ServiceInfo::new(SERVICE_TYPE, &instance_name, "", "", port, properties)?;
        self.daemon.register(service)?;
        Ok(())
    }

    /// Waits for the next DNS-SD event produced by the local daemon.
    ///
    /// # Errors
    ///
    /// Returns an error when the mDNS daemon's event channel closes.
    pub async fn next_event(&self) -> Result<ServiceEvent, DiscoveryError> {
        self.events
            .recv_async()
            .await
            .map_err(|_| DiscoveryError::EventChannelClosed)
    }

    /// Waits for a resolved peer carrying the required TLS metadata.
    ///
    /// # Errors
    ///
    /// Returns an error for a closed event channel or malformed peer metadata.
    pub async fn next_peer(&self) -> Result<DiscoveredPeer, DiscoveryError> {
        loop {
            if let ServiceEvent::ServiceResolved(service) = self.next_event().await? {
                return parse_resolved_peer(&service);
            }
        }
    }

    /// Stops advertising and browsing before application shutdown.
    ///
    /// # Errors
    ///
    /// Returns an error if the mDNS daemon cannot be shut down cleanly.
    pub fn shutdown(&self) -> Result<(), DiscoveryError> {
        self.daemon.shutdown()?;
        Ok(())
    }
}

fn parse_resolved_peer(
    service: &mdns_sd::ResolvedService,
) -> Result<DiscoveredPeer, DiscoveryError> {
    let peer_id = service
        .get_property_val_str("uuid")
        .ok_or(DiscoveryError::MissingMetadata)?
        .parse()
        .map(PeerId::from_uuid)
        .map_err(|_| DiscoveryError::InvalidMetadata)?;
    let username = service
        .get_property_val_str("username")
        .ok_or(DiscoveryError::MissingMetadata)?
        .to_owned();
    let count = service
        .get_property_val_str("certificate_parts")
        .ok_or(DiscoveryError::MissingMetadata)?
        .parse::<usize>()
        .map_err(|_| DiscoveryError::InvalidMetadata)?;
    let mut encoded = String::new();
    for index in 0..count {
        encoded.push_str(
            service
                .get_property_val_str(&format!("certificate_{index}"))
                .ok_or(DiscoveryError::MissingMetadata)?,
        );
    }
    let certificate_der = STANDARD
        .decode(encoded)
        .map_err(|_| DiscoveryError::InvalidCertificateEncoding)?;
    let address = service
        .get_addresses_v4()
        .into_iter()
        .next()
        .ok_or(DiscoveryError::MissingIpv4Address)?;
    Ok(DiscoveredPeer {
        certificate_der,
        endpoint: SocketAddr::V4(SocketAddrV4::new(address, service.get_port())),
        peer_id,
        username,
    })
}

/// mDNS discovery failures.
#[derive(Debug, Error)]
pub enum DiscoveryError {
    #[error("mDNS operation failed")]
    Mdns(#[from] mdns_sd::Error),
    #[error("mDNS event channel closed")]
    EventChannelClosed,
    #[error("certificate encoding was invalid")]
    InvalidCertificateEncoding,
    #[error("peer mDNS metadata was invalid")]
    InvalidMetadata,
    #[error("peer mDNS metadata was incomplete")]
    MissingMetadata,
    #[error("peer has no resolved IPv4 address")]
    MissingIpv4Address,
}
</file>

<file path="src-tauri/crates/zenpaws-network/src/lib.rs">
//! LAN discovery, framed TCP protocol, and peer connection state.
//!
//! See `docs/06_NETWORK_ARCHITECTURE.md` and `docs/07_MESSAGE_PROTOCOL.md`.

mod backoff;
mod discovery;
mod protocol;
mod service;
mod session;
mod state;
mod tls;
mod transport;
mod udp;

pub use backoff::RetryBackoff;
pub use discovery::{DiscoveredPeer, DiscoveryError, MdnsDiscovery, SERVICE_TYPE};
pub use protocol::{AckKind, Envelope, FrameError, MAX_FRAME_BYTES, read_envelope, write_envelope};
pub use service::{NetworkService, PeerConnection, ServiceError};
pub use session::{HandshakeError, HandshakeIdentity, client_handshake, server_handshake};
pub use state::{ConnectionStateMachine, StateError};
pub use tls::{TlsError, TlsIdentity};
pub use transport::{TransportError, accept_tls, connect_tls};
pub use udp::{
    MAX_DISCOVERY_DATAGRAM_BYTES, UDP_DISCOVERY_PORT, UdpAdvertisement, UdpDiscovery, UdpError,
};
</file>

<file path="src-tauri/crates/zenpaws-network/src/protocol.rs">
use std::io;

use serde::{Deserialize, Serialize};
use thiserror::Error;
use tokio::io::{AsyncRead, AsyncReadExt, AsyncWrite, AsyncWriteExt};
use uuid::Uuid;
use zenpaws_shared::PeerId;

/// Independent protocol version for wire compatibility checks.
pub const PROTOCOL_VERSION: u8 = 1;
/// Upper bound for one framed control message.
pub const MAX_FRAME_BYTES: usize = 1024 * 1024;

/// Acknowledgement kinds permitted by the receipt policy.
#[derive(Clone, Copy, Debug, Deserialize, Eq, PartialEq, Serialize)]
pub enum AckKind {
    Delivered,
    Read,
}

/// The small control envelope used before chat payloads are added in Phase 5.
#[derive(Clone, Debug, Deserialize, Eq, PartialEq, Serialize)]
pub enum Envelope {
    Handshake { peer_id: PeerId, username: String },
    Heartbeat,
    Ack { message_id: Uuid, kind: AckKind },
}

/// Protocol framing and serialization failures.
#[derive(Debug, Error)]
pub enum FrameError {
    #[error("protocol version {received} is unsupported")]
    UnsupportedVersion { received: u8 },
    #[error("frame is {received} bytes, over the {maximum}-byte limit")]
    FrameTooLarge { received: usize, maximum: usize },
    #[error("failed to serialize or deserialize the frame")]
    Serialization(#[from] Box<bincode::ErrorKind>),
    #[error("I/O failed while framing a peer message")]
    Io(#[from] io::Error),
}

/// Writes one versioned, length-prefixed envelope.
///
/// # Errors
///
/// Returns an error if serialization fails, the encoded frame exceeds the
/// documented limit, or the stream cannot be written.
pub async fn write_envelope<W>(writer: &mut W, envelope: &Envelope) -> Result<(), FrameError>
where
    W: AsyncWrite + Unpin,
{
    let body = bincode::serialize(envelope)?;
    if body.len() > MAX_FRAME_BYTES {
        return Err(FrameError::FrameTooLarge {
            received: body.len(),
            maximum: MAX_FRAME_BYTES,
        });
    }

    writer
        .write_u32_le(
            u32::try_from(body.len()).map_err(|_| FrameError::FrameTooLarge {
                received: body.len(),
                maximum: MAX_FRAME_BYTES,
            })?,
        )
        .await?;
    writer.write_u8(PROTOCOL_VERSION).await?;
    writer.write_all(&body).await?;
    writer.flush().await?;
    Ok(())
}

/// Reads one versioned, length-prefixed envelope.
///
/// # Errors
///
/// Returns an error for unsupported peers, oversized input, malformed frames,
/// or a failed stream read.
pub async fn read_envelope<R>(reader: &mut R) -> Result<Envelope, FrameError>
where
    R: AsyncRead + Unpin,
{
    let length = reader.read_u32_le().await? as usize;
    if length > MAX_FRAME_BYTES {
        return Err(FrameError::FrameTooLarge {
            received: length,
            maximum: MAX_FRAME_BYTES,
        });
    }

    let version = reader.read_u8().await?;
    if version != PROTOCOL_VERSION {
        return Err(FrameError::UnsupportedVersion { received: version });
    }

    let mut body = vec![0; length];
    reader.read_exact(&mut body).await?;
    Ok(bincode::deserialize(&body)?)
}

#[cfg(test)]
mod tests {
    use tokio::io::duplex;
    use uuid::Uuid;

    use super::{AckKind, Envelope, read_envelope, write_envelope};

    #[tokio::test]
    async fn round_trips_an_envelope() {
        let (mut writer, mut reader) = duplex(1024);
        let envelope = Envelope::Ack {
            message_id: Uuid::new_v4(),
            kind: AckKind::Delivered,
        };

        let writer_task = tokio::spawn(async move { write_envelope(&mut writer, &envelope).await });
        let received = read_envelope(&mut reader).await.expect("frame is valid");
        writer_task
            .await
            .expect("writer task completes")
            .expect("frame writes");

        assert!(matches!(
            received,
            Envelope::Ack {
                kind: AckKind::Delivered,
                ..
            }
        ));
    }
}
</file>

<file path="src-tauri/crates/zenpaws-network/src/service.rs">
use std::{
    collections::HashSet,
    net::SocketAddr,
    sync::{Arc, Mutex},
    time::{Duration, Instant},
};

use rustls::pki_types::{CertificateDer, ServerName};
use thiserror::Error;
use tokio::net::{TcpListener, TcpStream};
use tokio_rustls::TlsStream;
use zenpaws_shared::{EventBus, PeerId, PeerTrustStore, TrustStoreError, ZenPawsEvent};

use crate::{
    ConnectionStateMachine, Envelope, FrameError, HandshakeError, HandshakeIdentity, MdnsDiscovery,
    TlsError, TlsIdentity, TransportError, UDP_DISCOVERY_PORT, UdpAdvertisement, UdpDiscovery,
    accept_tls, client_handshake, connect_tls, read_envelope, server_handshake, write_envelope,
};

/// Listener, mDNS advertisement, and inbound encrypted peer lifecycle.
pub struct NetworkService {
    connecting_peers: Mutex<HashSet<PeerId>>,
    discovery: MdnsDiscovery,
    udp: UdpDiscovery,
    udp_advertisement: UdpAdvertisement,
    heartbeat_timeout: Duration,
    local: HandshakeIdentity,
    listener: TcpListener,
    server_config: Arc<rustls::ServerConfig>,
    bus: EventBus,
}

impl NetworkService {
    /// Binds the TCP listener and advertises it through mDNS.
    ///
    /// # Errors
    ///
    /// Returns an error when identity validation, TCP binding, TLS setup, or
    /// mDNS advertising fails.
    pub async fn bind(
        bind_address: SocketAddr,
        peer_id: PeerId,
        username: String,
        identity: &TlsIdentity,
        bus: EventBus,
        heartbeat_timeout: Duration,
    ) -> Result<Self, ServiceError> {
        let local = HandshakeIdentity::new(peer_id, username)?;
        let listener = TcpListener::bind(bind_address).await?;
        let discovery = MdnsDiscovery::start()?;
        let tcp_port = listener.local_addr()?.port();
        let certificate = identity.certificate();
        discovery.advertise(peer_id, local.username(), tcp_port, certificate.as_ref())?;
        let udp = UdpDiscovery::bind(
            SocketAddr::from(([0, 0, 0, 0], UDP_DISCOVERY_PORT)),
            SocketAddr::from(([255, 255, 255, 255], UDP_DISCOVERY_PORT)),
        )
        .await?;
        let udp_advertisement = UdpAdvertisement::new(
            peer_id,
            local.username().to_owned(),
            tcp_port,
            certificate.as_ref().to_vec(),
        )?;
        Ok(Self {
            connecting_peers: Mutex::new(HashSet::new()),
            discovery,
            udp,
            udp_advertisement,
            heartbeat_timeout,
            local,
            listener,
            server_config: identity.server_config()?,
            bus,
        })
    }

    /// Returns the local stable peer identity advertised by this service.
    #[must_use]
    pub const fn local_peer_id(&self) -> PeerId {
        self.local.peer_id()
    }

    /// Returns the TCP listener address assigned to this service.
    ///
    /// # Errors
    ///
    /// Returns an error when the listener no longer has a local address.
    pub fn local_addr(&self) -> Result<SocketAddr, ServiceError> {
        Ok(self.listener.local_addr()?)
    }

    /// Accepts TLS and completes the server half of the application handshake.
    ///
    /// # Errors
    ///
    /// Returns an error when TCP, TLS, framing, or handshake validation fails.
    pub async fn accept_peer(&self) -> Result<PeerConnection, ServiceError> {
        let (mut stream, address) =
            accept_tls(&self.listener, Arc::clone(&self.server_config)).await?;
        let remote = server_handshake(&mut stream, &self.local).await?;
        let mut state = ConnectionStateMachine::new(remote.peer_id(), self.bus.clone());
        state.connected(self.heartbeat_timeout)?;
        Ok(PeerConnection {
            address,
            heartbeat_timeout: self.heartbeat_timeout,
            remote,
            state,
            stream,
        })
    }

    /// Connects to a discovered peer using TOFU-pinned TLS and application identity validation.
    ///
    /// # Errors
    ///
    /// Returns an error when pin validation, TCP/TLS connection, framing, or
    /// remote identity validation fails.
    pub async fn connect_peer<S>(
        &self,
        address: SocketAddr,
        server_name: ServerName<'static>,
        expected_peer: PeerId,
        presented_certificate: CertificateDer<'static>,
        trust_store: &S,
    ) -> Result<PeerConnection, ServiceError>
    where
        S: PeerTrustStore + ?Sized,
    {
        let client_config =
            TlsIdentity::trust_peer_certificate(trust_store, expected_peer, presented_certificate)?;
        let mut stream = connect_tls(address, server_name, client_config).await?;
        let remote = client_handshake(&mut stream, &self.local, expected_peer).await?;
        let mut state = ConnectionStateMachine::new(remote.peer_id(), self.bus.clone());
        state.connected(self.heartbeat_timeout)?;
        Ok(PeerConnection {
            address,
            heartbeat_timeout: self.heartbeat_timeout,
            remote,
            state,
            stream,
        })
    }

    /// Runs the inbound accept loop with bounded recovery after listener errors.
    pub async fn run_inbound(self: Arc<Self>, mut shutdown: tokio::sync::watch::Receiver<bool>) {
        let mut retry =
            crate::RetryBackoff::new(Duration::from_millis(250), Duration::from_secs(5));
        loop {
            tokio::select! {
                changed = shutdown.changed() => {
                    if changed.is_err() || *shutdown.borrow() {
                        break;
                    }
                }
                result = self.accept_peer() => match result {
                    Ok(connection) => {
                        retry.reset();
                        tokio::spawn(async move { connection.monitor().await });
                    }
                    Err(_) => tokio::time::sleep(retry.next_delay()).await,
                },
            }
        }
    }

    /// Connects to one resolved mDNS peer through the full TOFU lifecycle.
    ///
    /// # Errors
    ///
    /// Returns an error for self-discovery, invalid TLS naming, trust failure,
    /// transport failure, or application-handshake failure.
    pub async fn connect_discovered_peer<S>(
        &self,
        peer: crate::DiscoveredPeer,
        trust_store: &S,
    ) -> Result<PeerConnection, ServiceError>
    where
        S: PeerTrustStore + ?Sized,
    {
        if peer.peer_id == self.local.peer_id() {
            return Err(ServiceError::SelfConnection);
        }
        let name = ServerName::try_from(format!("zenpaws-{}.local", peer.peer_id.as_uuid()))
            .map_err(|_| ServiceError::InvalidServerName)?
            .to_owned();
        self.connect_peer(
            peer.endpoint,
            name,
            peer.peer_id,
            CertificateDer::from(peer.certificate_der),
            trust_store,
        )
        .await
    }

    /// Reacts to resolved mDNS peers without polling or port scanning.
    pub async fn run_discovery(
        self: Arc<Self>,
        trust_store: Arc<dyn PeerTrustStore>,
        mut shutdown: tokio::sync::watch::Receiver<bool>,
    ) {
        loop {
            let peer = tokio::select! {
                changed = shutdown.changed() => {
                    if changed.is_err() || *shutdown.borrow() {
                        break;
                    }
                    continue;
                }
                result = self.discovery.next_peer() => match result {
                    Ok(peer) => peer,
                    Err(_) => break,
                },
            };
            if peer.peer_id == self.local.peer_id() || !self.mark_connecting(peer.peer_id) {
                continue;
            }
            let service = Arc::clone(&self);
            let trust_store = Arc::clone(&trust_store);
            tokio::spawn(async move { service.connect_with_retry(peer, trust_store).await });
        }
    }

    /// Announces and receives the UDP discovery fallback at a low fixed rate.
    pub async fn run_udp(
        self: Arc<Self>,
        trust_store: Arc<dyn PeerTrustStore>,
        mut shutdown: tokio::sync::watch::Receiver<bool>,
    ) {
        loop {
            let _ = self.udp.announce(&self.udp_advertisement).await;
            let peer = tokio::select! {
                changed = shutdown.changed() => {
                    if changed.is_err() || *shutdown.borrow() {
                        break;
                    }
                    continue;
                }
                received = self.udp.receive() => match received {
                    Ok((advertisement, source)) => crate::DiscoveredPeer {
                        certificate_der: advertisement.certificate_der,
                        endpoint: SocketAddr::new(source.ip(), advertisement.tcp_port),
                        peer_id: advertisement.peer_id,
                        username: advertisement.username,
                    },
                    Err(_) => continue,
                },
                () = tokio::time::sleep(Duration::from_secs(30)) => continue,
            };
            if peer.peer_id == self.local.peer_id() || !self.mark_connecting(peer.peer_id) {
                continue;
            }
            let service = Arc::clone(&self);
            let trust_store = Arc::clone(&trust_store);
            tokio::spawn(async move { service.connect_with_retry(peer, trust_store).await });
        }
    }

    async fn connect_with_retry(
        self: Arc<Self>,

        peer: crate::DiscoveredPeer,
        trust_store: Arc<dyn PeerTrustStore>,
    ) {
        let mut retry =
            crate::RetryBackoff::new(Duration::from_millis(250), Duration::from_secs(5));
        loop {
            match self
                .connect_discovered_peer(peer.clone(), trust_store.as_ref())
                .await
            {
                Ok(connection) => {
                    connection.monitor().await;
                    break;
                }
                Err(ServiceError::Tls(TlsError::Trust(TrustStoreError::PinMismatch))) => {
                    self.bus.publish(ZenPawsEvent::PeerTrustViolation {
                        peer_id: peer.peer_id,
                    });
                    break;
                }
                Err(_) => tokio::time::sleep(retry.next_delay()).await,
            }
        }
        self.clear_connecting(peer.peer_id);
    }

    fn mark_connecting(&self, peer_id: PeerId) -> bool {
        self.connecting_peers
            .lock()
            .is_ok_and(|mut peers| peers.insert(peer_id))
    }

    fn clear_connecting(&self, peer_id: PeerId) {
        if let Ok(mut peers) = self.connecting_peers.lock() {
            peers.remove(&peer_id);
        }
    }

    /// Stops mDNS activity before the application exits.
    ///
    /// # Errors
    ///
    /// Returns an error when the mDNS daemon cannot shut down cleanly.
    pub fn shutdown(&self) -> Result<(), ServiceError> {
        self.discovery.shutdown()?;
        Ok(())
    }
}

/// One accepted, TLS-protected peer connection.
pub struct PeerConnection {
    address: SocketAddr,
    heartbeat_timeout: Duration,
    remote: HandshakeIdentity,
    state: ConnectionStateMachine,
    stream: TlsStream<TcpStream>,
}

impl PeerConnection {
    /// Returns the remote TCP endpoint.
    #[must_use]
    pub const fn address(&self) -> SocketAddr {
        self.address
    }

    /// Returns the identity validated by the application handshake.
    #[must_use]
    pub const fn peer_id(&self) -> PeerId {
        self.remote.peer_id()
    }

    /// Sends a heartbeat without allocating a polling task.
    ///
    /// # Errors
    ///
    /// Returns an error when encrypted framing fails.
    pub async fn send_heartbeat(&mut self) -> Result<(), ServiceError> {
        write_envelope(&mut self.stream, &Envelope::Heartbeat).await?;
        Ok(())
    }

    /// Receives one control envelope and refreshes heartbeat state when needed.
    ///
    /// # Errors
    ///
    /// Returns an error when encrypted framing fails or the peer state is invalid.
    pub async fn receive(&mut self) -> Result<Envelope, ServiceError> {
        let envelope = read_envelope(&mut self.stream).await?;
        if matches!(envelope, Envelope::Heartbeat) {
            self.state.heartbeat(self.heartbeat_timeout)?;
        }
        Ok(envelope)
    }

    /// Monitors inbound control frames until the peer disconnects or a frame fails.
    async fn monitor(mut self) {
        while self.receive().await.is_ok() {}
        let _ = self.state.disconnected();
    }

    /// Applies the configured heartbeat deadline.
    ///
    /// # Errors
    ///
    /// Returns an error when the connection state cannot transition.
    pub fn check_heartbeat(&mut self, now: Instant) -> Result<(), ServiceError> {
        self.state.check_heartbeat(now)?;
        Ok(())
    }
}

#[cfg(test)]
mod tests {
    use super::NetworkService;

    #[test]
    fn service_can_be_shared_with_background_tasks() {
        fn assert_send_sync<T: Send + Sync>() {}
        assert_send_sync::<NetworkService>();
    }
}

/// Network-service lifecycle failures.
#[derive(Debug, Error)]
pub enum ServiceError {
    #[error("TCP listener failed")]
    Io(#[from] std::io::Error),
    #[error("mDNS lifecycle failed")]
    Discovery(#[from] crate::DiscoveryError),
    #[error("UDP fallback discovery failed")]
    Udp(#[from] crate::UdpError),
    #[error("TLS configuration or handshake failed")]
    Tls(#[from] TlsError),
    #[error("TLS transport failed")]
    Transport(#[from] TransportError),
    #[error("application handshake failed")]
    Handshake(#[from] HandshakeError),
    #[error("peer protocol framing failed")]
    Frame(#[from] FrameError),
    #[error("peer state update failed")]
    State(#[from] crate::StateError),
    #[error("discovered peer attempted to connect to itself")]
    SelfConnection,
    #[error("discovered peer TLS server name was invalid")]
    InvalidServerName,
}
</file>

<file path="src-tauri/crates/zenpaws-network/src/session.rs">
use thiserror::Error;
use tokio::io::{AsyncRead, AsyncWrite};
use zenpaws_shared::PeerId;

use crate::{Envelope, FrameError, read_envelope, write_envelope};

/// Local identity fields sent after the TLS handshake completes.
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct HandshakeIdentity {
    peer_id: PeerId,
    username: String,
}

impl HandshakeIdentity {
    /// Validates fields that can safely appear in a LAN handshake.
    ///
    /// # Errors
    ///
    /// Returns an error for an empty, oversized, or control-character username.
    pub fn new(peer_id: PeerId, username: String) -> Result<Self, HandshakeError> {
        if username.is_empty() || username.len() > 32 || username.chars().any(char::is_control) {
            return Err(HandshakeError::InvalidUsername);
        }
        Ok(Self { peer_id, username })
    }

    /// Returns this peer's stable identity.
    #[must_use]
    pub const fn peer_id(&self) -> PeerId {
        self.peer_id
    }

    /// Returns the validated display name received during the handshake.
    #[must_use]
    pub fn username(&self) -> &str {
        &self.username
    }
}

/// Runs the initiator side of the encrypted application handshake.
///
/// # Errors
///
/// Returns an error for invalid protocol frames or unexpected remote identity.
pub async fn client_handshake<S>(
    stream: &mut S,
    local: &HandshakeIdentity,
    expected_peer: PeerId,
) -> Result<HandshakeIdentity, HandshakeError>
where
    S: AsyncRead + AsyncWrite + Unpin,
{
    send_handshake(stream, local).await?;
    receive_handshake(stream, expected_peer).await
}

/// Runs the accepting side of the encrypted application handshake.
///
/// # Errors
///
/// Returns an error for invalid protocol frames or an invalid remote username.
pub async fn server_handshake<S>(
    stream: &mut S,
    local: &HandshakeIdentity,
) -> Result<HandshakeIdentity, HandshakeError>
where
    S: AsyncRead + AsyncWrite + Unpin,
{
    let remote = receive_any_handshake(stream).await?;
    send_handshake(stream, local).await?;
    Ok(remote)
}

async fn send_handshake<S>(
    stream: &mut S,
    identity: &HandshakeIdentity,
) -> Result<(), HandshakeError>
where
    S: AsyncWrite + Unpin,
{
    write_envelope(
        stream,
        &Envelope::Handshake {
            peer_id: identity.peer_id,
            username: identity.username.clone(),
        },
    )
    .await?;
    Ok(())
}

async fn receive_handshake<S>(
    stream: &mut S,
    expected_peer: PeerId,
) -> Result<HandshakeIdentity, HandshakeError>
where
    S: AsyncRead + Unpin,
{
    let identity = receive_any_handshake(stream).await?;
    if identity.peer_id != expected_peer {
        return Err(HandshakeError::UnexpectedPeer);
    }
    Ok(identity)
}

async fn receive_any_handshake<S>(stream: &mut S) -> Result<HandshakeIdentity, HandshakeError>
where
    S: AsyncRead + Unpin,
{
    match read_envelope(stream).await? {
        Envelope::Handshake { peer_id, username } => HandshakeIdentity::new(peer_id, username),
        _ => Err(HandshakeError::ExpectedHandshake),
    }
}

/// Application handshake failures after TLS succeeds.
#[derive(Debug, Error)]
pub enum HandshakeError {
    #[error("handshake frame failed")]
    Frame(#[from] FrameError),
    #[error("handshake username is invalid")]
    InvalidUsername,
    #[error("expected a handshake envelope")]
    ExpectedHandshake,
    #[error("remote handshake peer identity did not match discovery")]
    UnexpectedPeer,
}

#[cfg(test)]
mod tests {
    use tokio::io::duplex;
    use zenpaws_shared::PeerId;

    use super::{HandshakeIdentity, client_handshake, server_handshake};

    #[tokio::test]
    async fn exchanges_identities_after_transport_security() {
        let client_id = PeerId::new();
        let server_id = PeerId::new();
        let client_identity =
            HandshakeIdentity::new(client_id, "client".to_owned()).expect("valid");
        let server_identity =
            HandshakeIdentity::new(server_id, "server".to_owned()).expect("valid");
        let (mut client_stream, mut server_stream) = duplex(4096);
        let server =
            tokio::spawn(
                async move { server_handshake(&mut server_stream, &server_identity).await },
            );

        let remote = client_handshake(&mut client_stream, &client_identity, server_id)
            .await
            .expect("valid remote identity");
        let received = server
            .await
            .expect("server task completes")
            .expect("server handshake");

        assert_eq!(remote.peer_id(), server_id);
        assert_eq!(received.peer_id(), client_id);
    }
}
</file>

<file path="src-tauri/crates/zenpaws-network/src/state.rs">
use std::time::{Duration, Instant};

use thiserror::Error;
use zenpaws_shared::{ConnectionState, EventBus, PeerId, ZenPawsEvent};

/// Tracks one peer's lifecycle without performing transport I/O.
#[derive(Debug)]
pub struct ConnectionStateMachine {
    bus: EventBus,
    heartbeat_deadline: Option<Instant>,
    peer_id: PeerId,
    state: ConnectionState,
}

impl ConnectionStateMachine {
    /// Creates a peer session in the connecting state.
    #[must_use]
    pub const fn new(peer_id: PeerId, bus: EventBus) -> Self {
        Self {
            bus,
            heartbeat_deadline: None,
            peer_id,
            state: ConnectionState::Connecting,
        }
    }

    /// Returns the current peer state.
    #[must_use]
    pub const fn state(&self) -> ConnectionState {
        self.state
    }

    /// Records a successful TLS and TOFU handshake.
    ///
    /// # Errors
    ///
    /// Returns an error when the current state cannot become connected.
    pub fn connected(&mut self, heartbeat_timeout: Duration) -> Result<(), StateError> {
        self.transition(ConnectionState::Connected)?;
        self.heartbeat_deadline = Some(Instant::now() + heartbeat_timeout);
        Ok(())
    }

    /// Records a heartbeat and refreshes the degradation deadline.
    ///
    /// # Errors
    ///
    /// Returns an error when the peer is disconnected or a transition fails.
    pub fn heartbeat(&mut self, heartbeat_timeout: Duration) -> Result<(), StateError> {
        if self.state == ConnectionState::Disconnected {
            return Err(StateError::HeartbeatWhileDisconnected);
        }
        self.heartbeat_deadline = Some(Instant::now() + heartbeat_timeout);
        if self.state == ConnectionState::Degraded {
            self.transition(ConnectionState::Connected)?;
        }
        Ok(())
    }

    /// Applies the heartbeat deadline to the state machine.
    ///
    /// # Errors
    ///
    /// Returns an error when the current state cannot become degraded.
    pub fn check_heartbeat(&mut self, now: Instant) -> Result<(), StateError> {
        if self
            .heartbeat_deadline
            .is_some_and(|deadline| now >= deadline)
        {
            self.transition(ConnectionState::Degraded)?;
        }
        Ok(())
    }

    /// Marks the peer unavailable after transport closure or retry exhaustion.
    ///
    /// # Errors
    ///
    /// Returns an error when the current state cannot become disconnected.
    pub fn disconnected(&mut self) -> Result<(), StateError> {
        self.heartbeat_deadline = None;
        self.transition(ConnectionState::Disconnected)
    }

    fn transition(&mut self, next: ConnectionState) -> Result<(), StateError> {
        if !is_valid_transition(self.state, next) {
            return Err(StateError::InvalidTransition {
                from: self.state,
                to: next,
            });
        }
        if self.state != next {
            self.state = next;
            self.bus.publish(ZenPawsEvent::PeerStateChanged {
                peer_id: self.peer_id,
                state: next,
            });
        }
        Ok(())
    }
}

fn is_valid_transition(from: ConnectionState, to: ConnectionState) -> bool {
    matches!(
        (from, to),
        (
            ConnectionState::Connecting | ConnectionState::Degraded,
            ConnectionState::Connected | ConnectionState::Disconnected
        ) | (
            ConnectionState::Connected,
            ConnectionState::Degraded | ConnectionState::Disconnected
        ) | (ConnectionState::Disconnected, ConnectionState::Connecting)
    ) || from == to
}

/// Invalid state machine operations.
#[derive(Debug, Error)]
pub enum StateError {
    #[error("cannot receive a heartbeat while disconnected")]
    HeartbeatWhileDisconnected,
    #[error("invalid state transition from {from:?} to {to:?}")]
    InvalidTransition {
        from: ConnectionState,
        to: ConnectionState,
    },
}

#[cfg(test)]
mod tests {
    use std::time::{Duration, Instant};

    use zenpaws_shared::{ConnectionState, EventBus, PeerId};

    use super::ConnectionStateMachine;

    #[test]
    fn heartbeat_timeout_degrades_a_connected_peer() {
        let mut session = ConnectionStateMachine::new(PeerId::new(), EventBus::new(1));
        session
            .connected(Duration::from_millis(1))
            .expect("valid transition");
        session
            .check_heartbeat(Instant::now() + Duration::from_secs(1))
            .expect("timeout check is valid");

        assert_eq!(session.state(), ConnectionState::Degraded);
    }
}
</file>

<file path="src-tauri/crates/zenpaws-network/src/tls.rs">
use std::sync::Arc;

use rcgen::generate_simple_self_signed;
use rustls::{
    ClientConfig, RootCertStore, ServerConfig,
    pki_types::{CertificateDer, PrivateKeyDer, PrivatePkcs8KeyDer},
};
use sha2::{Digest, Sha256};
use thiserror::Error;
use zenpaws_shared::{CertificatePin, PeerId, PeerTrustStore, TrustStoreError, TrustedPeer};

fn pin_from_certificate(certificate: &CertificateDer<'_>) -> CertificatePin {
    let digest = Sha256::digest(certificate.as_ref());
    let mut pin = [0; 32];
    pin.copy_from_slice(&digest);
    CertificatePin::new(pin)
}

/// A local self-signed certificate and its private key.
pub struct TlsIdentity {
    certificate: CertificateDer<'static>,
    private_key_der: Vec<u8>,
}

impl TlsIdentity {
    /// Generates a self-signed certificate for the supplied stable peer name.
    ///
    /// # Errors
    ///
    /// Returns an error when the certificate or key cannot be generated.
    pub fn generate(server_name: String) -> Result<Self, TlsError> {
        let certified = generate_simple_self_signed(vec![server_name])?;
        let certificate = certified.cert.der().clone();
        Ok(Self {
            certificate,
            private_key_der: certified.signing_key.serialize_der(),
        })
    }

    /// Returns this identity's certificate pin for TOFU persistence.
    #[must_use]
    pub fn pin(&self) -> CertificatePin {
        pin_from_certificate(&self.certificate)
    }

    /// Applies TOFU pinning and builds a client configuration for a peer.
    ///
    /// On first contact the certificate is recorded. On reconnect the
    /// presented certificate must produce the same stored SHA-256 pin.
    ///
    /// # Errors
    ///
    /// Returns an error for changed pins, unavailable persistent storage, or
    /// invalid TLS trust-anchor configuration.
    pub fn trust_peer_certificate<S>(
        trust_store: &S,
        peer_id: PeerId,
        presented_certificate: CertificateDer<'static>,
    ) -> Result<Arc<ClientConfig>, TlsError>
    where
        S: PeerTrustStore + ?Sized,
    {
        let pin = pin_from_certificate(&presented_certificate);
        let certificate = if let Some(existing) = trust_store.trusted_peer(peer_id)? {
            if existing.pin() != pin {
                return Err(TlsError::Trust(TrustStoreError::PinMismatch));
            }
            CertificateDer::from(existing.certificate_der().to_vec())
        } else {
            trust_store.trust_first_use(TrustedPeer::new(
                peer_id,
                presented_certificate.as_ref().to_vec(),
                pin,
            ))?;
            presented_certificate
        };
        Self::client_config_for_peer(certificate)
    }

    /// Builds a TLS server configuration for incoming peer connections.
    ///
    /// # Errors
    ///
    /// Returns an error when the certificate and private key cannot form a
    /// valid TLS server configuration.
    pub fn server_config(&self) -> Result<Arc<ServerConfig>, TlsError> {
        let config = ServerConfig::builder()
            .with_no_client_auth()
            .with_single_cert(
                vec![self.certificate.clone()],
                PrivateKeyDer::Pkcs8(PrivatePkcs8KeyDer::from(self.private_key_der.clone())),
            )?;
        Ok(Arc::new(config))
    }

    /// Restores a persisted self-signed certificate and PKCS#8 private key.
    #[must_use]
    pub fn from_der(certificate_der: Vec<u8>, private_key_der: Vec<u8>) -> Self {
        Self {
            certificate: CertificateDer::from(certificate_der),
            private_key_der,
        }
    }

    /// Returns a clone of this peer's public certificate.
    #[must_use]
    pub fn certificate(&self) -> CertificateDer<'static> {
        self.certificate.clone()
    }

    /// Returns a clone of the PKCS#8 private key for local persistence.
    #[must_use]
    pub fn private_key_der(&self) -> Vec<u8> {
        self.private_key_der.clone()
    }

    /// Builds a client configuration that trusts exactly one peer certificate.
    ///
    /// The caller supplies the certificate stored after an explicit first-use
    /// trust decision. No system root or arbitrary certificate is accepted.
    ///
    /// # Errors
    ///
    /// Returns an error when the pinned certificate cannot be added as a trust
    /// anchor.
    pub fn client_config_for_peer(
        peer_certificate: CertificateDer<'static>,
    ) -> Result<Arc<ClientConfig>, TlsError> {
        let mut roots = RootCertStore::empty();
        roots.add(peer_certificate)?;
        let config = ClientConfig::builder()
            .with_root_certificates(roots)
            .with_no_client_auth();
        Ok(Arc::new(config))
    }
}

/// Certificate generation and TLS configuration failures.
#[derive(Debug, Error)]
pub enum TlsError {
    #[error("self-signed certificate generation failed")]
    CertificateGeneration(#[from] rcgen::Error),
    #[error("TLS configuration failed")]
    Rustls(#[from] rustls::Error),
    #[error("TOFU trust validation failed")]
    Trust(#[from] TrustStoreError),
}

#[cfg(test)]
mod tests {
    use std::sync::Mutex;

    use zenpaws_shared::{PeerId, PeerTrustStore, TrustStoreError, TrustedPeer};

    use super::TlsIdentity;

    struct MemoryTrustStore(Mutex<Option<TrustedPeer>>);

    impl PeerTrustStore for MemoryTrustStore {
        fn trusted_peer(&self, peer_id: PeerId) -> Result<Option<TrustedPeer>, TrustStoreError> {
            Ok(self
                .0
                .lock()
                .expect("test lock is available")
                .as_ref()
                .filter(|peer| peer.peer_id() == peer_id)
                .cloned())
        }

        fn trust_first_use(&self, peer: TrustedPeer) -> Result<(), TrustStoreError> {
            *self.0.lock().expect("test lock is available") = Some(peer);
            Ok(())
        }
    }

    #[test]
    fn generated_identity_creates_tls_configs() {
        let identity = TlsIdentity::generate("zenpaws-test.local".to_owned())
            .expect("certificate generation succeeds");

        assert!(identity.server_config().is_ok());
        assert!(TlsIdentity::client_config_for_peer(identity.certificate()).is_ok());
    }

    #[test]
    fn tofu_rejects_a_changed_certificate() {
        let store = MemoryTrustStore(Mutex::new(None));
        let peer_id = PeerId::new();
        let first = TlsIdentity::generate("first.local".to_owned()).expect("first identity");
        let changed = TlsIdentity::generate("changed.local".to_owned()).expect("changed identity");

        TlsIdentity::trust_peer_certificate(&store, peer_id, first.certificate())
            .expect("first certificate is trusted");
        assert!(
            TlsIdentity::trust_peer_certificate(&store, peer_id, changed.certificate()).is_err()
        );
    }
}
</file>

<file path="src-tauri/crates/zenpaws-network/src/transport.rs">
use std::{net::SocketAddr, sync::Arc};

use rustls::{ClientConfig, ServerConfig, pki_types::ServerName};
use thiserror::Error;
use tokio::net::{TcpListener, TcpStream};
use tokio_rustls::{TlsAcceptor, TlsConnector, TlsStream};

/// Accepts one TCP connection and upgrades it to TLS.
///
/// # Errors
///
/// Returns an error when accepting the TCP socket or completing the TLS
/// handshake fails.
pub async fn accept_tls(
    listener: &TcpListener,
    server_config: Arc<ServerConfig>,
) -> Result<(TlsStream<TcpStream>, SocketAddr), TransportError> {
    let (stream, address) = listener.accept().await?;
    let stream = TlsAcceptor::from(server_config).accept(stream).await?;
    Ok((TlsStream::Server(stream), address))
}

/// Connects to a peer over TCP and completes a pinned TLS handshake.
///
/// # Errors
///
/// Returns an error when TCP connection, server-name validation, or the TLS
/// handshake fails.
pub async fn connect_tls(
    address: SocketAddr,
    server_name: ServerName<'static>,
    client_config: Arc<ClientConfig>,
) -> Result<TlsStream<TcpStream>, TransportError> {
    let stream = TcpStream::connect(address).await?;
    let stream = TlsConnector::from(client_config)
        .connect(server_name, stream)
        .await?;
    Ok(TlsStream::Client(stream))
}

/// Encrypted transport failures.
#[derive(Debug, Error)]
pub enum TransportError {
    #[error("TCP transport I/O failed")]
    Io(#[from] std::io::Error),
}

#[cfg(test)]
mod tests {
    use std::sync::Arc;

    use rustls::pki_types::ServerName;
    use tokio::net::TcpListener;

    use super::{accept_tls, connect_tls};
    use crate::TlsIdentity;

    #[tokio::test]
    async fn accepts_a_pinned_tls_connection() {
        let server_identity =
            TlsIdentity::generate("localhost".to_owned()).expect("server identity is generated");
        let server_config = server_identity
            .server_config()
            .expect("server config is valid");
        let client_config = TlsIdentity::client_config_for_peer(server_identity.certificate())
            .expect("client config is valid");
        let listener = TcpListener::bind("127.0.0.1:0")
            .await
            .expect("listener binds");
        let address = listener.local_addr().expect("listener has an address");
        let server = tokio::spawn(async move { accept_tls(&listener, server_config).await });
        let name = ServerName::try_from("localhost")
            .expect("server name is valid")
            .to_owned();

        let client = connect_tls(address, name, Arc::clone(&client_config)).await;
        let accepted = server.await.expect("server task completes");

        assert!(client.is_ok());
        assert!(accepted.is_ok());
    }
}
</file>

<file path="src-tauri/crates/zenpaws-network/src/udp.rs">
use std::net::SocketAddr;

use serde::{Deserialize, Serialize};
use thiserror::Error;
use tokio::net::UdpSocket;
use zenpaws_shared::PeerId;

use crate::protocol::PROTOCOL_VERSION;

/// Upper bound for one UDP discovery datagram.
pub const MAX_DISCOVERY_DATAGRAM_BYTES: usize = 1024;
/// Default LAN broadcast port for the mDNS fallback.
pub const UDP_DISCOVERY_PORT: u16 = 46_235;

/// Metadata announced when multicast DNS is unavailable.
#[derive(Clone, Debug, Deserialize, Eq, PartialEq, Serialize)]
pub struct UdpAdvertisement {
    /// Public self-signed certificate DER for TOFU pinning.
    pub certificate_der: Vec<u8>,
    /// Advertised TCP listener port.
    pub tcp_port: u16,
    /// Stable peer identity.
    pub peer_id: PeerId,
    /// Wire compatibility version.
    pub protocol_version: u8,
    /// Display name for discovery UI.
    pub username: String,
}

impl UdpAdvertisement {
    /// Creates a versioned advertisement after validating its display metadata.
    ///
    /// # Errors
    ///
    /// Returns an error for an empty, oversized, or control-character username.
    pub fn new(
        peer_id: PeerId,
        username: String,
        tcp_port: u16,
        certificate_der: Vec<u8>,
    ) -> Result<Self, UdpError> {
        if username.is_empty() || username.len() > 32 || username.chars().any(char::is_control) {
            return Err(UdpError::InvalidUsername);
        }
        Ok(Self {
            certificate_der,
            tcp_port,
            peer_id,
            protocol_version: PROTOCOL_VERSION,
            username,
        })
    }
}

/// Bounded UDP broadcast fallback for LAN peer discovery.
pub struct UdpDiscovery {
    destination: SocketAddr,
    socket: UdpSocket,
}

impl UdpDiscovery {
    /// Binds a socket and configures its broadcast destination.
    ///
    /// # Errors
    ///
    /// Returns an error if the socket cannot bind or enable broadcast mode.
    pub async fn bind(bind_address: SocketAddr, destination: SocketAddr) -> Result<Self, UdpError> {
        let socket = UdpSocket::bind(bind_address).await?;
        socket.set_broadcast(true)?;
        Ok(Self {
            destination,
            socket,
        })
    }

    /// Sends one bounded discovery datagram.
    ///
    /// # Errors
    ///
    /// Returns an error if serialization fails, the datagram is oversized, or
    /// the UDP send fails.
    pub async fn announce(&self, advertisement: &UdpAdvertisement) -> Result<(), UdpError> {
        let payload = bincode::serialize(advertisement)?;
        if payload.len() > MAX_DISCOVERY_DATAGRAM_BYTES {
            return Err(UdpError::DatagramTooLarge);
        }
        self.socket.send_to(&payload, self.destination).await?;
        Ok(())
    }

    /// Receives one discovery datagram and its source address.
    ///
    /// # Errors
    ///
    /// Returns an error if receiving or decoding the datagram fails, or when
    /// the peer uses an incompatible protocol version.
    pub async fn receive(&self) -> Result<(UdpAdvertisement, SocketAddr), UdpError> {
        let mut buffer = [0; MAX_DISCOVERY_DATAGRAM_BYTES];
        let (length, source) = self.socket.recv_from(&mut buffer).await?;
        let advertisement: UdpAdvertisement = bincode::deserialize(&buffer[..length])?;
        if advertisement.protocol_version != PROTOCOL_VERSION {
            return Err(UdpError::UnsupportedVersion);
        }
        Ok((advertisement, source))
    }

    /// Returns the local address selected by the operating system.
    ///
    /// # Errors
    ///
    /// Returns an error if the socket no longer has a local address.
    pub fn local_addr(&self) -> Result<SocketAddr, UdpError> {
        Ok(self.socket.local_addr()?)
    }
}

/// UDP fallback discovery failures.
#[derive(Debug, Error)]
pub enum UdpError {
    #[error("discovery username is invalid")]
    InvalidUsername,
    #[error("discovery datagram exceeds the configured limit")]
    DatagramTooLarge,
    #[error("peer uses an unsupported discovery protocol version")]
    UnsupportedVersion,
    #[error("UDP socket I/O failed")]
    Io(#[from] std::io::Error),
    #[error("discovery datagram could not be encoded or decoded")]
    Serialization(#[from] Box<bincode::ErrorKind>),
}

#[cfg(test)]
mod tests {
    use std::net::{Ipv4Addr, SocketAddr, SocketAddrV4};

    use zenpaws_shared::PeerId;

    use super::{UdpAdvertisement, UdpDiscovery};

    #[tokio::test]
    async fn sends_and_receives_a_loopback_advertisement() {
        let loopback = Ipv4Addr::LOCALHOST;
        let receiver = UdpDiscovery::bind(
            SocketAddr::V4(SocketAddrV4::new(loopback, 0)),
            SocketAddr::V4(SocketAddrV4::new(loopback, 0)),
        )
        .await
        .expect("receiver binds");
        let sender = UdpDiscovery::bind(
            SocketAddr::V4(SocketAddrV4::new(loopback, 0)),
            receiver.local_addr().expect("receiver address"),
        )
        .await
        .expect("sender binds");
        let advertisement = UdpAdvertisement::new(PeerId::new(), "peer".to_owned(), 5000, vec![1])
            .expect("advertisement is valid");

        sender
            .announce(&advertisement)
            .await
            .expect("datagram sends");
        let (received_advertisement, _) = receiver.receive().await.expect("datagram receives");

        assert_eq!(received_advertisement, advertisement);
    }
}
</file>

<file path="src-tauri/crates/zenpaws-network/Cargo.toml">
[package]
name = "zenpaws-network"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
base64.workspace = true
bincode.workspace = true
mdns-sd.workspace = true
rcgen.workspace = true
rustls.workspace = true
sha2.workspace = true
serde.workspace = true
thiserror.workspace = true
tokio.workspace = true
tokio-rustls.workspace = true
uuid.workspace = true
zenpaws-shared = { path = "../zenpaws-shared" }

[lints]
workspace = true
</file>

<file path="src-tauri/crates/zenpaws-notifications/src/lib.rs">
//! Native OS notifications and pet speech-bubble notification dispatch,
//! privacy-first by default (no message content in previews).
//!
//! Implemented in Phase 8 (Notification System). See
//! `docs/02_PRODUCT_REQUIREMENTS_DOCUMENT.md` for the requirement this
//! satisfies.
//!
//! Intentionally empty at Phase 1 - this is an honest stub, not a
//! placeholder implementation, per `docs/00_PROJECT_CONSTITUTION.md`.
</file>

<file path="src-tauri/crates/zenpaws-notifications/Cargo.toml">
[package]
name = "zenpaws-notifications"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]

[lints]
workspace = true
</file>

<file path="src-tauri/crates/zenpaws-pets/src/behavior.rs">
use std::time::Instant;

use crate::PetState;

/// Local-only cursor coordinates for pet behavior decisions.
#[derive(Clone, Copy, Debug, PartialEq)]
pub struct Point {
    pub x: f64,
    pub y: f64,
}

/// Runtime behavior quality setting.
#[derive(Clone, Copy, Debug, Eq, PartialEq)]
pub enum PerformanceMode {
    Full,
    Reduced,
}

/// Local behavior output. Coordinates are never synchronized.
#[derive(Clone, Copy, Debug, PartialEq)]
pub struct BehaviorDecision {
    pub state: PetState,
    pub target: Point,
}

/// Chooses local pet movement state from cursor distance and velocity.
#[derive(Debug)]
pub struct CursorBehavior {
    last_cursor: Option<(Point, Instant)>,
}

impl CursorBehavior {
    /// Creates a cursor behavior controller without any sampled cursor position.
    #[must_use]
    pub const fn new() -> Self {
        Self { last_cursor: None }
    }

    /// Produces the next local-only movement decision.
    #[must_use]
    pub fn update(
        &mut self,
        pet: Point,
        cursor: Point,
        now: Instant,
        mode: PerformanceMode,
    ) -> BehaviorDecision {
        let cursor_distance = distance(pet, cursor);
        let velocity = self.last_cursor.map_or(0.0, |(previous, sampled_at)| {
            let seconds = now.duration_since(sampled_at).as_secs_f64();
            if seconds == 0.0 {
                0.0
            } else {
                distance(previous, cursor) / seconds
            }
        });
        self.last_cursor = Some((cursor, now));

        let state = if cursor_distance < 8.0 {
            PetState::Idle
        } else if mode == PerformanceMode::Full && velocity > 1_200.0 {
            PetState::Run
        } else {
            PetState::Walk
        };
        BehaviorDecision {
            state,
            target: cursor,
        }
    }
}

impl Default for CursorBehavior {
    fn default() -> Self {
        Self::new()
    }
}

fn distance(left: Point, right: Point) -> f64 {
    (left.x - right.x).hypot(left.y - right.y)
}

#[cfg(test)]
mod tests {
    use std::time::{Duration, Instant};

    use super::{CursorBehavior, PerformanceMode, Point};
    use crate::PetState;

    #[test]
    fn chooses_local_movement_without_syncing_coordinates() {
        let mut behavior = CursorBehavior::new();
        let now = Instant::now();
        let pet = Point { x: 0.0, y: 0.0 };

        assert_eq!(
            behavior.update(pet, pet, now, PerformanceMode::Full).state,
            PetState::Idle
        );
        assert_eq!(
            behavior
                .update(
                    pet,
                    Point { x: 20.0, y: 0.0 },
                    now + Duration::from_millis(100),
                    PerformanceMode::Full
                )
                .state,
            PetState::Walk
        );
        assert_eq!(
            behavior
                .update(
                    pet,
                    Point { x: 500.0, y: 0.0 },
                    now + Duration::from_millis(200),
                    PerformanceMode::Full
                )
                .state,
            PetState::Run
        );
        assert_eq!(
            behavior
                .update(
                    pet,
                    Point { x: 1_000.0, y: 0.0 },
                    now + Duration::from_millis(300),
                    PerformanceMode::Reduced
                )
                .state,
            PetState::Walk
        );
    }
}
</file>

<file path="src-tauri/crates/zenpaws-pets/src/engine.rs">
use std::{collections::BTreeMap, time::Instant};

use thiserror::Error;
use uuid::Uuid;
use zenpaws_shared::{EventBus, ZenPawsEvent};

use crate::{PetInstance, PetRegistry, PetState, PetStateError};

/// Local pet runtime independent of chat and networking services.
pub struct PetEngine {
    bus: EventBus,
    instances: BTreeMap<Uuid, PetInstance>,
    registry: PetRegistry,
}

impl PetEngine {
    /// Creates an engine from validated installable pet packages.
    #[must_use]
    pub const fn new(registry: PetRegistry, bus: EventBus) -> Self {
        Self {
            bus,
            instances: BTreeMap::new(),
            registry,
        }
    }

    /// Creates one local instance for an installed manifest ID.
    ///
    /// # Errors
    ///
    /// Returns an error when the requested package is not installed.
    pub fn create_instance(&mut self, manifest_id: &str) -> Result<Uuid, PetEngineError> {
        if self.registry.get(manifest_id).is_none() {
            return Err(PetEngineError::UnknownManifest);
        }
        let instance = PetInstance::new();
        let id = instance.id();
        self.instances.insert(id, instance);
        Ok(id)
    }

    /// Updates a local instance and publishes only a permitted discrete event.
    ///
    /// # Errors
    ///
    /// Returns an error for an unknown instance or rate-limited state change.
    pub fn set_state(
        &mut self,
        instance_id: Uuid,
        state: PetState,
        now: Instant,
    ) -> Result<(), PetEngineError> {
        let instance = self
            .instances
            .get_mut(&instance_id)
            .ok_or(PetEngineError::UnknownInstance)?;
        if instance.set_state(state, now)?
            && let Some(sync_state) = state.sync_state()
        {
            self.bus.publish(ZenPawsEvent::PetStateChanged {
                pet_instance: instance_id,
                state: sync_state,
            });
        }
        Ok(())
    }
}

/// Pet runtime failures.
#[derive(Debug, Error)]
pub enum PetEngineError {
    #[error("pet manifest is not installed")]
    UnknownManifest,
    #[error("pet instance does not exist")]
    UnknownInstance,
    #[error("pet state update failed")]
    State(#[from] PetStateError),
}
</file>

<file path="src-tauri/crates/zenpaws-pets/src/manifest.rs">
use std::{
    fs,
    path::{Path, PathBuf},
};

use serde::Deserialize;
use thiserror::Error;

/// Data-driven description of one installable desktop pet.
#[derive(Clone, Debug, Deserialize, PartialEq)]
#[serde(rename_all = "camelCase")]
pub struct PetManifest {
    pub default_scale: f32,
    pub display_name: String,
    pub id: String,
    pub sounds: std::collections::BTreeMap<String, String>,
    pub states: std::collections::BTreeMap<String, SpriteState>,
}

/// Sprite-sheet metadata for one visible pet state.
#[derive(Clone, Debug, Deserialize, Eq, PartialEq)]
#[serde(rename_all = "camelCase")]
pub struct SpriteState {
    pub fps: u8,
    pub frame_count: u16,
    pub sprite: String,
}

impl PetManifest {
    /// Loads and validates a manifest and its required license file.
    ///
    /// # Errors
    ///
    /// Returns an error for unreadable, invalid, or unsafe pet assets.
    pub fn load(directory: impl AsRef<Path>) -> Result<Self, PetManifestError> {
        let directory = directory.as_ref();
        if !directory.join("LICENSE.txt").is_file() {
            return Err(PetManifestError::MissingLicense);
        }
        let manifest: Self = serde_json::from_slice(&fs::read(directory.join("manifest.json"))?)?;
        manifest.validate(directory)?;
        Ok(manifest)
    }

    fn validate(&self, directory: &Path) -> Result<(), PetManifestError> {
        if self.id.is_empty()
            || !self.id.chars().all(|character| {
                character.is_ascii_lowercase() || character.is_ascii_digit() || character == '-'
            })
            || self.display_name.is_empty()
            || self.default_scale <= 0.0
        {
            return Err(PetManifestError::InvalidMetadata);
        }
        for state in self.states.values() {
            let sprite = Path::new(&state.sprite);
            if state.frame_count == 0
                || state.fps == 0
                || sprite.is_absolute()
                || sprite
                    .components()
                    .any(|part| matches!(part, std::path::Component::ParentDir))
                || !asset_path(directory, sprite).is_file()
            {
                return Err(PetManifestError::InvalidSprite);
            }
        }
        Ok(())
    }
}

fn asset_path(directory: &Path, relative: &Path) -> PathBuf {
    directory.join(relative)
}

/// Manifest loading and validation failures.
#[derive(Debug, Error)]
pub enum PetManifestError {
    #[error("pet manifest I/O failed")]
    Io(#[from] std::io::Error),
    #[error("pet manifest JSON was invalid")]
    Json(#[from] serde_json::Error),
    #[error("pet manifest metadata was invalid")]
    InvalidMetadata,
    #[error("pet sprite metadata or file was invalid")]
    InvalidSprite,
    #[error("pet package is missing LICENSE.txt")]
    MissingLicense,
}
</file>

<file path="src-tauri/crates/zenpaws-pets/src/registry.rs">
use std::{collections::BTreeMap, fs, path::Path};

use thiserror::Error;

use crate::{PetManifest, PetManifestError};

/// Loaded data-driven pet packages keyed by immutable manifest ID.
#[derive(Debug, Default)]
pub struct PetRegistry {
    manifests: BTreeMap<String, PetManifest>,
}

impl PetRegistry {
    /// Loads every valid pet directory directly under the package root.
    ///
    /// # Errors
    ///
    /// Returns an error for unreadable directories, invalid manifests, or
    /// duplicate manifest IDs.
    pub fn load(root: impl AsRef<Path>) -> Result<Self, PetRegistryError> {
        let mut manifests = BTreeMap::new();
        for entry in fs::read_dir(root)? {
            let path = entry?.path();
            if !path.is_dir() {
                continue;
            }
            let manifest = PetManifest::load(&path)?;
            if manifests.insert(manifest.id.clone(), manifest).is_some() {
                return Err(PetRegistryError::DuplicateId);
            }
        }
        Ok(Self { manifests })
    }

    /// Returns one installed manifest by ID.
    #[must_use]
    pub fn get(&self, id: &str) -> Option<&PetManifest> {
        self.manifests.get(id)
    }

    /// Returns the number of installed pet packages.
    #[must_use]
    pub fn len(&self) -> usize {
        self.manifests.len()
    }

    /// Returns whether no pet packages are installed.
    #[must_use]
    pub fn is_empty(&self) -> bool {
        self.manifests.is_empty()
    }
}

/// Registry loading failures.
#[derive(Debug, Error)]
pub enum PetRegistryError {
    #[error("pet registry I/O failed")]
    Io(#[from] std::io::Error),
    #[error("pet manifest was invalid")]
    Manifest(#[from] PetManifestError),
    #[error("multiple pet packages use the same manifest ID")]
    DuplicateId,
}
</file>

<file path="src-tauri/crates/zenpaws-pets/src/state.rs">
use std::time::{Duration, Instant};

use serde::{Deserialize, Serialize};
use thiserror::Error;
use uuid::Uuid;
use zenpaws_shared::PetSyncState;

/// Discrete pet states; coordinates and animation frames stay local.
#[derive(Clone, Copy, Debug, Deserialize, Eq, PartialEq, Serialize)]
#[serde(rename_all = "snake_case")]
pub enum PetState {
    Away,
    Celebrate,
    Fight,
    Idle,
    Jump,
    Notification,
    Offline,
    Play,
    Run,
    Sleep,
    Typing,
    Walk,
}

impl PetState {
    /// Returns the synchronized representation when this state is network-visible.
    #[must_use]
    pub const fn sync_state(self) -> Option<PetSyncState> {
        match self {
            Self::Away => Some(PetSyncState::Away),
            Self::Celebrate => Some(PetSyncState::Celebrate),
            Self::Notification => Some(PetSyncState::Notification),
            Self::Offline => Some(PetSyncState::Offline),
            Self::Play => Some(PetSyncState::Play),
            Self::Sleep => Some(PetSyncState::Sleep),
            Self::Typing => Some(PetSyncState::Typing),
            Self::Fight | Self::Idle | Self::Jump | Self::Run | Self::Walk => None,
        }
    }
}

/// Local state and outbound synchronization policy for one pet instance.
#[derive(Debug)]
pub struct PetInstance {
    id: Uuid,
    last_broadcast: Option<Instant>,
    last_broadcast_state: Option<PetState>,
    state: PetState,
}

impl PetInstance {
    /// Creates a local pet in its idle state.
    #[must_use]
    pub fn new() -> Self {
        Self {
            id: Uuid::new_v4(),
            last_broadcast: None,
            last_broadcast_state: None,
            state: PetState::Idle,
        }
    }

    /// Returns the stable local instance ID.
    #[must_use]
    pub const fn id(&self) -> Uuid {
        self.id
    }

    /// Returns the current local state.
    #[must_use]
    pub const fn state(&self) -> PetState {
        self.state
    }

    /// Changes local state and reports whether a discrete event may be broadcast.
    ///
    /// # Errors
    ///
    /// Returns an error when a new state exceeds the two-per-second limit.
    pub fn set_state(&mut self, state: PetState, now: Instant) -> Result<bool, PetStateError> {
        self.state = state;
        if self.last_broadcast_state == Some(state) {
            return Ok(false);
        }
        if self
            .last_broadcast
            .is_some_and(|last| now.duration_since(last) < Duration::from_millis(500))
        {
            return Err(PetStateError::RateLimited);
        }
        self.last_broadcast = Some(now);
        self.last_broadcast_state = Some(state);
        Ok(true)
    }
}

impl Default for PetInstance {
    fn default() -> Self {
        Self::new()
    }
}

/// Pet state transition failures.
#[derive(Debug, Error, Eq, PartialEq)]
pub enum PetStateError {
    #[error("pet state broadcast exceeds two events per second")]
    RateLimited,
}

#[cfg(test)]
mod tests {
    use std::time::{Duration, Instant};

    use super::{PetInstance, PetState, PetStateError};

    #[test]
    fn deduplicates_and_rate_limits_broadcasts() {
        let mut pet = PetInstance::new();
        let now = Instant::now();
        assert!(
            pet.set_state(PetState::Typing, now)
                .expect("first state broadcasts")
        );
        assert!(
            !pet.set_state(PetState::Typing, now)
                .expect("same state deduplicates")
        );
        assert_eq!(
            pet.set_state(PetState::Sleep, now),
            Err(PetStateError::RateLimited)
        );
        assert!(
            pet.set_state(PetState::Sleep, now + Duration::from_millis(500))
                .expect("rate window passes")
        );
        assert!(PetState::Typing.sync_state().is_some());
        assert!(PetState::Walk.sync_state().is_none());
    }
}
</file>

<file path="src-tauri/crates/zenpaws-settings/src/identity.rs">
use std::{
    fs,
    path::{Path, PathBuf},
};

use serde::{Deserialize, Serialize};
use zenpaws_shared::PeerId;

use crate::SettingsError;

/// Persistent local identity required before the network service starts.
#[derive(Clone, Debug, Deserialize, Eq, PartialEq, Serialize)]
pub struct LocalIdentity {
    certificate_der: Vec<u8>,
    peer_id: PeerId,
    private_key_der: Vec<u8>,
    username: String,
}

impl LocalIdentity {
    /// Creates a persistable identity after validating the local display name.
    ///
    /// # Errors
    ///
    /// Returns an error for an empty, oversized, or control-character username.
    pub fn new(
        peer_id: PeerId,
        username: String,
        certificate_der: Vec<u8>,
        private_key_der: Vec<u8>,
    ) -> Result<Self, SettingsError> {
        if username.is_empty() || username.len() > 32 || username.chars().any(char::is_control) {
            return Err(SettingsError::InvalidUsername);
        }
        Ok(Self {
            certificate_der,
            peer_id,
            private_key_der,
            username,
        })
    }

    /// Returns the local peer identity.
    #[must_use]
    pub const fn peer_id(&self) -> PeerId {
        self.peer_id
    }

    /// Returns the local display name.
    #[must_use]
    pub fn username(&self) -> &str {
        &self.username
    }

    /// Returns the self-signed certificate DER.
    #[must_use]
    pub fn certificate_der(&self) -> &[u8] {
        &self.certificate_der
    }

    /// Returns the PKCS#8 private key DER.
    #[must_use]
    pub fn private_key_der(&self) -> &[u8] {
        &self.private_key_der
    }
}

/// Loads a persisted local identity when it exists.
///
/// # Errors
///
/// Returns an error when an existing identity file cannot be read or decoded.
pub fn load_local_identity(path: impl AsRef<Path>) -> Result<Option<LocalIdentity>, SettingsError> {
    let path = path.as_ref();
    if !path.exists() {
        return Ok(None);
    }
    Ok(Some(serde_json::from_slice(&fs::read(path)?)?))
}

/// Persists a local identity before opening the LAN listener.
///
/// # Errors
///
/// Returns an error when the destination cannot be created or written.
pub fn save_local_identity(
    path: impl Into<PathBuf>,
    identity: &LocalIdentity,
) -> Result<(), SettingsError> {
    let path = path.into();
    if let Some(parent) = path.parent() {
        fs::create_dir_all(parent)?;
    }
    let temporary = path.with_extension("tmp");
    fs::write(&temporary, serde_json::to_vec(identity)?)?;
    fs::rename(temporary, path)?;
    Ok(())
}

#[cfg(test)]
mod tests {
    use std::env;

    use zenpaws_shared::PeerId;

    use super::{LocalIdentity, load_local_identity, save_local_identity};

    #[test]
    fn persists_identity_material() {
        let peer_id = PeerId::new();
        let path = env::temp_dir().join(format!("zenpaws-identity-{}.json", peer_id.as_uuid()));
        let identity = LocalIdentity::new(peer_id, "owner".to_owned(), vec![1], vec![2])
            .expect("identity is valid");

        save_local_identity(&path, &identity).expect("identity saves");
        let loaded = load_local_identity(&path).expect("identity loads");

        assert_eq!(loaded, Some(identity));
        std::fs::remove_file(path).expect("test file is removable");
    }
}
</file>

<file path="src-tauri/crates/zenpaws-settings/src/lib.rs">
//! Persistent local settings used by the networking foundation.
//!
//! See `docs/12_SECURITY_MODEL.md`.

use std::{
    collections::HashMap,
    fs,
    path::{Path, PathBuf},
    sync::Mutex,
};

use serde::{Deserialize, Serialize};
use thiserror::Error;
mod identity;

pub use identity::{LocalIdentity, load_local_identity, save_local_identity};

use zenpaws_shared::{PeerId, PeerTrustStore, TrustStoreError, TrustedPeer};

/// JSON-backed TOFU certificate storage in the application data directory.
#[derive(Debug)]
pub struct JsonTrustStore {
    path: PathBuf,
    peers: Mutex<HashMap<PeerId, TrustedPeer>>,
}

impl JsonTrustStore {
    /// Opens existing trusted-peer data, or starts with an empty store.
    ///
    /// # Errors
    ///
    /// Returns an error when the existing file cannot be read or decoded.
    pub fn open(path: impl Into<PathBuf>) -> Result<Self, SettingsError> {
        let path = path.into();
        let peers = read_peers(&path)?;
        Ok(Self {
            path,
            peers: Mutex::new(peers),
        })
    }
}

impl PeerTrustStore for JsonTrustStore {
    fn trusted_peer(&self, peer_id: PeerId) -> Result<Option<TrustedPeer>, TrustStoreError> {
        let peers = self
            .peers
            .lock()
            .map_err(|_| TrustStoreError::Storage("trust store lock poisoned".to_owned()))?;
        Ok(peers.get(&peer_id).cloned())
    }

    fn trust_first_use(&self, peer: TrustedPeer) -> Result<(), TrustStoreError> {
        let mut peers = self
            .peers
            .lock()
            .map_err(|_| TrustStoreError::Storage("trust store lock poisoned".to_owned()))?;
        if let Some(existing) = peers.get(&peer.peer_id()) {
            if existing.pin() == peer.pin() {
                return Ok(());
            }
            return Err(TrustStoreError::PinMismatch);
        }

        let mut updated = peers.clone();
        updated.insert(peer.peer_id(), peer);
        write_peers(&self.path, &updated)
            .map_err(|error| TrustStoreError::Storage(error.to_string()))?;
        *peers = updated;
        drop(peers);
        Ok(())
    }
}

fn read_peers(path: &Path) -> Result<HashMap<PeerId, TrustedPeer>, SettingsError> {
    if !path.exists() {
        return Ok(HashMap::new());
    }
    let bytes = fs::read(path)?;
    Ok(serde_json::from_slice::<TrustFile>(&bytes)?.peers)
}

fn write_peers(path: &Path, peers: &HashMap<PeerId, TrustedPeer>) -> Result<(), SettingsError> {
    if let Some(parent) = path.parent() {
        fs::create_dir_all(parent)?;
    }
    let bytes = serde_json::to_vec(&TrustFile {
        peers: peers.clone(),
    })?;
    let temporary = path.with_extension("tmp");
    fs::write(&temporary, bytes)?;
    fs::rename(temporary, path)?;
    Ok(())
}

#[derive(Debug, Deserialize, Serialize)]
struct TrustFile {
    peers: HashMap<PeerId, TrustedPeer>,
}

/// JSON trust-store failures.
#[derive(Debug, Error)]
pub enum SettingsError {
    #[error("settings file I/O failed")]
    Io(#[from] std::io::Error),
    #[error("settings file is invalid")]
    Json(#[from] serde_json::Error),
    #[error("local username is invalid")]
    InvalidUsername,
}

#[cfg(test)]
mod tests {
    use std::env;

    use zenpaws_shared::{CertificatePin, PeerTrustStore, TrustedPeer};

    use super::JsonTrustStore;

    #[test]
    fn rejects_a_changed_certificate_pin() {
        let peer = zenpaws_shared::PeerId::new();
        let path = env::temp_dir().join(format!("zenpaws-trust-{}.json", peer.as_uuid()));
        let store = JsonTrustStore::open(&path).expect("store opens");
        let first = TrustedPeer::new(peer, vec![1], CertificatePin::new([1; 32]));
        let changed = TrustedPeer::new(peer, vec![2], CertificatePin::new([2; 32]));

        store
            .trust_first_use(first)
            .expect("first contact is trusted");
        assert!(store.trust_first_use(changed).is_err());

        std::fs::remove_file(path).expect("test file is removable");
    }
}
</file>

<file path="src-tauri/crates/zenpaws-settings/Cargo.toml">
[package]
name = "zenpaws-settings"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
zenpaws-shared = { path = "../zenpaws-shared" }

[lints]
workspace = true
</file>

<file path="src-tauri/crates/zenpaws-shared/src/trust.rs">
use serde::{Deserialize, Serialize};
use thiserror::Error;

use crate::PeerId;

/// SHA-256 digest of a peer certificate's DER encoding.
#[derive(Clone, Copy, Debug, Deserialize, Eq, PartialEq, Serialize)]
pub struct CertificatePin([u8; 32]);

impl CertificatePin {
    /// Creates a pin from a SHA-256 digest.
    #[must_use]
    pub const fn new(bytes: [u8; 32]) -> Self {
        Self(bytes)
    }

    /// Returns the raw SHA-256 digest.
    #[must_use]
    pub const fn as_bytes(self) -> [u8; 32] {
        self.0
    }
}

/// A certificate trusted for a stable peer identity.
#[derive(Clone, Debug, Deserialize, Eq, PartialEq, Serialize)]
pub struct TrustedPeer {
    certificate_der: Vec<u8>,
    peer_id: PeerId,
    pin: CertificatePin,
}

impl TrustedPeer {
    /// Creates a trusted-peer record from verified certificate material.
    #[must_use]
    pub const fn new(peer_id: PeerId, certificate_der: Vec<u8>, pin: CertificatePin) -> Self {
        Self {
            certificate_der,
            peer_id,
            pin,
        }
    }

    /// Returns the peer identity bound to this certificate.
    #[must_use]
    pub const fn peer_id(&self) -> PeerId {
        self.peer_id
    }

    /// Returns the stored certificate DER.
    #[must_use]
    pub fn certificate_der(&self) -> &[u8] {
        &self.certificate_der
    }

    /// Returns the stored certificate pin.
    #[must_use]
    pub const fn pin(&self) -> CertificatePin {
        self.pin
    }
}

/// Persistent TOFU pin storage used by the networking layer.
pub trait PeerTrustStore: Send + Sync {
    /// Returns the certificate previously trusted for a peer.
    ///
    /// # Errors
    ///
    /// Returns an error when persistent trust data cannot be read.
    fn trusted_peer(&self, peer_id: PeerId) -> Result<Option<TrustedPeer>, TrustStoreError>;

    /// Persists a first-use decision or rejects an unexpected pin change.
    ///
    /// # Errors
    ///
    /// Returns an error for a changed pin or failed persistent storage.
    fn trust_first_use(&self, peer: TrustedPeer) -> Result<(), TrustStoreError>;
}

/// TOFU pin-storage failures.
#[derive(Debug, Error)]
pub enum TrustStoreError {
    #[error("peer certificate pin changed")]
    PinMismatch,
    #[error("trust storage failed: {0}")]
    Storage(String),
}
</file>

<file path="src-tauri/crates/zenpaws-shared/Cargo.toml">
[package]
name = "zenpaws-shared"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
serde.workspace = true
thiserror.workspace = true
tokio.workspace = true
uuid.workspace = true

[lints]
workspace = true
</file>

<file path="src-tauri/crates/zenpaws-storage/src/lib.rs">
//! On-disk file storage: downloads, image/thumbnail cache, transfer
//! staging, log rotation, and the quotas that bound all of them.
//!
//! Implemented starting Phase 6 (Image System) and extended through Phase 7.
//! See `docs/26_STORAGE_STRATEGY.md` for the quotas and directory layout this
//! crate enforces.
//!
//! Intentionally empty at Phase 1 - this is an honest stub, not a
//! placeholder implementation, per `docs/00_PROJECT_CONSTITUTION.md`.
</file>

<file path="src-tauri/crates/zenpaws-storage/Cargo.toml">
[package]
name = "zenpaws-storage"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]

[lints]
workspace = true
</file>

<file path="src-tauri/crates/zenpaws-transfer/src/lib.rs">
//! Chunked file transfer: pause/resume/cancel, progress, SHA-256 integrity
//! validation. Never coupled to chat logic - see
//! `docs/08_FILE_TRANSFER_PROTOCOL.md`.
//!
//! Implemented in Phase 7 (File Transfer System).
//!
//! Intentionally empty at Phase 1 - this is an honest stub, not a
//! placeholder implementation, per `docs/00_PROJECT_CONSTITUTION.md`.
</file>

<file path="src-tauri/crates/zenpaws-transfer/Cargo.toml">
[package]
name = "zenpaws-transfer"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]

[lints]
workspace = true
</file>

<file path="src-tauri/src/commands/chat.rs">
#![allow(clippy::needless_pass_by_value, reason = "Tauri command IPC deserializes owned arguments and State")

use serde::Serialize;
use tauri::State;
use uuid::Uuid;
use zenpaws_database::{MessageCursor, MessageRecord};
use zenpaws_shared::{EventBus, ZenPawsEvent};

use crate::DatabaseState;

/// Frontend-safe message projection without database internals.
#[derive(Serialize)]
#[serde(rename_all = "camelCase")]
pub struct ChatMessage {
    pub author_id: String,
    pub body: String,
    pub created_at: i64,
    pub id: String,
    pub reply_to: Option<String>,
    pub room: String,
}

/// Keyset cursor accepted from the chat frontend.
#[derive(serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ChatCursor {
    pub created_at: i64,
    pub id: String,
}

/// Loads one descending keyset page of local room history.
#[tauri::command]
pub fn list_messages(
    database: State<'_, DatabaseState>,
    room: String,
    cursor: Option<ChatCursor>,
    limit: u32,
) -> Result<Vec<ChatMessage>, String> {
    let cursor = cursor
        .map(|cursor| {
            Ok::<MessageCursor, String>(MessageCursor {
                created_at: cursor.created_at,
                id: Uuid::parse_str(&cursor.id).map_err(|error| error.to_string())?,
            })
        })
        .transpose()?;
    let database = database.0.lock().map_err(|_| "database lock poisoned".to_owned())?;
    database
        .messages_before(&room, cursor.as_ref(), limit)
        .map(|messages| messages.iter().map(project_message).collect())
        .map_err(|error| error.to_string())
}

/// Searches the local FTS5 message index.
#[tauri::command]
pub fn search_messages(
    database: State<'_, DatabaseState>,
    query: String,
    limit: u32,
) -> Result<Vec<ChatMessage>, String> {
    let database = database.0.lock().map_err(|_| "database lock poisoned".to_owned())?;
    database
        .search_messages(&query, limit)
        .map(|messages| messages.iter().map(project_message).collect())
        .map_err(|error| error.to_string())
}

/// Persists one locally-authored message for the active peer identity.
#[tauri::command]
pub fn send_message(
    database: State<'_, DatabaseState>,
    room: String,
    body: String,
    reply_to: Option<String>,
) -> Result<ChatMessage, String> {
    if body.trim().is_empty() {
        return Err("message body cannot be empty".to_owned());
    }
    let author = super::network::local_peer_id()?;
    let created_at: i64 = std::time::SystemTime::now()
        .duration_since(std::time::UNIX_EPOCH)
        .map_err(|error| error.to_string())?
        .as_millis()
        .try_into()
        .map_err(|_| "system timestamp exceeds i64".to_owned())?;
    let message = MessageRecord {
        author,
        body,
        created_at,
        id: Uuid::new_v4(),
        lamport_counter: created_at,
        lamport_peer: author,
        reply_to: reply_to
            .map(|value| Uuid::parse_str(&value).map_err(|error| error.to_string()))
            .transpose()?,
        room,
    };
    database
        .0
        .lock()
        .map_err(|_| "database lock poisoned".to_owned())?
        .insert_message(&message)
        .map_err(|error| error.to_string())?;
    Ok(project_message(&message))
}

/// Adds a local peer reaction to a message.
#[tauri::command]
pub fn add_reaction(
    database: State<'_, DatabaseState>,
    message_id: String,
    emoji: String,
) -> Result<(), String> {
    if emoji.is_empty() || emoji.chars().count() > 16 {
        return Err("reaction emoji is invalid".to_owned());
    }
    let message_id = Uuid::parse_str(&message_id).map_err(|error| error.to_string())?;
    let peer_id = super::network::local_peer_id()?;
    database
        .0
        .lock()
        .map_err(|_| "database lock poisoned".to_owned())?
        .add_reaction(message_id, peer_id, &emoji)
        .map_err(|error| error.to_string())
}

/// Publishes ephemeral typing state for the active local peer.
#[tauri::command]
pub fn set_typing(
    bus: State<'_, EventBus>,
    room: String,
    is_typing: bool,
) -> Result<(), String> {
    let peer_id = super::network::local_peer_id()?;
    bus.publish(ZenPawsEvent::TypingChanged {
        peer_id,
        room,
        is_typing,
    });
    Ok(())
}

/// Returns acknowledgement status for a direct-message recipient.
#[tauri::command]
pub fn message_status(
    database: State<'_, DatabaseState>,
    message_id: String,
    peer_id: String,
) -> Result<ChatMessageStatus, String> {
    let message_id = Uuid::parse_str(&message_id).map_err(|error| error.to_string())?;
    let peer_id = Uuid::parse_str(&peer_id).map_err(|error| error.to_string())?;
    let status = database
        .0
        .lock()
        .map_err(|_| "database lock poisoned".to_owned())?
        .message_status(message_id, zenpaws_shared::PeerId::from_uuid(peer_id))
        .map_err(|error| error.to_string())?;
    Ok(ChatMessageStatus {
        delivered: status.delivered,
        read: status.read,
    })
}

/// Direct-message acknowledgement state for the frontend.
#[derive(Serialize)]
pub struct ChatMessageStatus {
    pub delivered: bool,
    pub read: bool,
}

/// Edits a locally visible message body.
#[tauri::command]
pub fn edit_message(
    database: State<'_, DatabaseState>,
    id: String,
    body: String,
) -> Result<(), String> {
    let id = Uuid::parse_str(&id).map_err(|error| error.to_string())?;
    let edited_at: i64 = std::time::SystemTime::now()
        .duration_since(std::time::UNIX_EPOCH)
        .map_err(|error| error.to_string())?
        .as_millis()
        .try_into()
        .map_err(|_| "system timestamp exceeds i64".to_owned())?;
    database
        .0
        .lock()
        .map_err(|_| "database lock poisoned".to_owned())?
        .edit_message(id, &body, edited_at)
        .map_err(|error| error.to_string())
}

/// Soft-deletes a locally visible message.
#[tauri::command]
pub fn delete_message(database: State<'_, DatabaseState>, id: String) -> Result<(), String> {
    let id = Uuid::parse_str(&id).map_err(|error| error.to_string())?;
    let deleted_at: i64 = std::time::SystemTime::now()
        .duration_since(std::time::UNIX_EPOCH)
        .map_err(|error| error.to_string())?
        .as_millis()
        .try_into()
        .map_err(|_| "system timestamp exceeds i64".to_owned())?;
    database
        .0
        .lock()
        .map_err(|_| "database lock poisoned".to_owned())?
        .delete_message(id, deleted_at)
        .map_err(|error| error.to_string())
}

fn project_message(message: &MessageRecord) -> ChatMessage {
    ChatMessage {
        author_id: message.author.as_uuid().to_string(),
        body: message.body.clone(),
        created_at: message.created_at,
        id: message.id.to_string(),
        reply_to: message.reply_to.map(|id| id.to_string()),
        room: message.room.clone(),
    }
}
</file>

<file path="src-tauri/src/commands/pets.rs">
#![allow(clippy::needless_pass_by_value, reason = "Tauri command IPC deserializes owned arguments and State")

use tauri::{AppHandle, Manager, WebviewUrl, WebviewWindowBuilder};
use uuid::Uuid;
use zenpaws_pets::PetRegistry;

/// Details returned after a transparent pet overlay is created.
#[derive(serde::Serialize)]
pub struct PetWindowStatus {
    pub label: String,
}

/// Opens one transparent, click-through pet overlay for an installed package.
#[tauri::command]
pub fn open_pet_window(app: AppHandle, manifest_id: String) -> Result<PetWindowStatus, String> {
    let assets = app
        .path()
        .resource_dir()
        .map_err(|error| error.to_string())?
        .join("assets")
        .join("pets");
    let registry = PetRegistry::load(assets).map_err(|error| error.to_string())?;
    if registry.get(&manifest_id).is_none() {
        return Err("requested pet package is not installed".to_owned());
    }

    let label = format!("pet-{manifest_id}-{}", Uuid::new_v4());
    let window = WebviewWindowBuilder::new(&app, &label, WebviewUrl::App("index.html".into()))
        .always_on_top(true)
        .decorations(false)
        .resizable(false)
        .skip_taskbar(true)
        .build()
        .map_err(|error| error.to_string())?;
    window
        .set_ignore_cursor_events(true)
        .map_err(|error| error.to_string())?;
    Ok(PetWindowStatus { label })
}

/// Closes only a pet-scoped overlay window.
#[tauri::command]
pub fn close_pet_window(app: AppHandle, label: String) -> Result<(), String> {
    if !label.starts_with("pet-") {
        return Err("window label is outside the pet scope".to_owned());
    }
    let window = app
        .get_webview_window(&label)
        .ok_or_else(|| "pet window does not exist".to_owned())?;
    window.close().map_err(|error| error.to_string())
}
</file>

<file path="src-tauri/rustfmt.toml">
edition = "2024"
max_width = 100
use_small_heuristics = "Default"
imports_granularity = "Crate"
group_imports = "StdExternalCrate"
</file>

<file path="biome.json">
{
  "$schema": "./node_modules/@biomejs/biome/configuration_schema.json",
  "vcs": {
    "enabled": true,
    "clientKind": "git",
    "useIgnoreFile": true
  },
  "files": {
    "includes": [
      "src/**",
      "vite.config.ts",
      "!**/node_modules",
      "!**/dist",
      "!**/target"
    ]
  },
  "assist": {
    "actions": {
      "source": {
        "organizeImports": "on"
      }
    }
  },
  "formatter": {
    "enabled": true,
    "indentStyle": "space",
    "indentWidth": 2,
    "lineWidth": 100
  },
  "javascript": {
    "formatter": {
      "semicolons": "always",
      "quoteStyle": "double",
      "trailingCommas": "all"
    }
  },
  "linter": {
    "enabled": true,
    "rules": {
      "suspicious": {
        "noExplicitAny": "error"
      },
      "correctness": {
        "useExhaustiveDependencies": "warn"
      },
      "preset": "recommended"
    }
  },
  "extends": [
    "ultracite/biome/core",
    "ultracite/biome/react",
    "ultracite/biome/tanstack",
    "ultracite/biome/vitest"
  ]
}
</file>

<file path="index.html">
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <link href="/vite.svg" rel="icon" type="image/svg+xml">
    <meta content="width=device-width, initial-scale=1.0" name="viewport">
    <title>ZenPaws</title>
  </head>

  <body>
    <div id="root"></div>
    <script src="/src/main.tsx" type="module"></script>
  </body>
</html>
</file>

<file path="MASTER_BUILD_PROMPT.md">
# MASTER_BUILD_PROMPT.md - ZenPaws

## Role

You are a senior software architect, senior Rust engineer, senior Tauri engineer,
senior React engineer, networking engineer, distributed systems engineer, desktop
application engineer, security engineer, and performance optimization specialist.

Your task is to build **ZenPaws**, a production-grade, cross-platform, LAN-only
peer-to-peer desktop messenger with interactive desktop pets, built on Tauri v2.

App identifier convention: `com.zenpaws.app` (used in `tauri.conf.json` and
platform bundle identifiers - confirm/replace with the real reverse-domain
identifier before Phase 1).

Technology stack:

- Tauri v2
- Rust
- Tokio
- React 19
- TypeScript
- Vite
- Zustand
- TanStack Query
- SQLite
- SQLite FTS5
- SQLite WAL mode

---

# Core Philosophy - `DietrichGebert/ponytail` Ruleset

This project follows the ponytail engineering discipline. It is a coding
discipline, not an architectural or pet-system reference - ponytail has no
rendering, window, or pet code. Before implementing anything, always evaluate
in order:

1. Does this feature need to exist?
2. Can an existing module solve it?
3. Can the Rust/TypeScript standard library solve it?
4. Can the OS or Tauri solve it natively?
5. Can an already-installed dependency solve it?
6. Can it be implemented with significantly less code?
7. Only then write new code - the minimum that correctly solves the problem.

Never introduce abstractions, services, dependencies, patterns, or layers
without a clear, stated need. Record any deliberate override of this ladder,
with justification, in `05_TECHNICAL_DECISIONS.md`.

**Non-negotiable exception:** do not sacrifice security, correctness,
reliability, or accessibility for simplicity. Laziness applies to avoiding
unnecessary abstraction - never to cutting a safety or correctness concern.

If desktop-pet visual/architectural inspiration is wanted from a *different*
named project (rendering, transparent overlay windows, animation, click-
through), name it explicitly with its GitHub URL before it's used as a
reference - that is unrelated to the ponytail ruleset above. Study technique
only; never copy code or art assets verbatim, since they may be licensed.

---

# Project Vision

Three independent systems sharing a common platform:

1. **Messenger** - LAN-based chat.
2. **Desktop Pet Platform** - interactive desktop pets.
3. **LAN Networking Platform** - peer discovery, synchronization, messaging,
   file transfer.

These systems remain loosely coupled via the Event Bus (see below). The pet
engine must remain functional even if chat functionality is disabled.

---

# Non-Goals

Explicit scope boundaries, to prevent drift during implementation:

- No internet/WAN relay, NAT traversal, or cloud component of any kind.
- No mobile clients.
- No multi-workspace / multi-server topology - a single flat LAN room plus
  DMs, not a Slack-style architecture.
- No encryption/identity guarantees beyond what's specified in the Security
  section - this is a LAN tool, not designed to resist a nation-state
  adversary on the same subnet, but it must resist casual snooping and
  spoofing by other devices on that LAN.

---

# Supported Platforms

Windows, Linux, macOS. Architecture must remain cross-platform. Avoid
platform-specific code unless unavoidable. When it's required: isolate it,
document it, and provide fallback behavior.

---

# Development Rules

- Never generate the entire project at once. Work phase-by-phase.
- Wait for explicit approval before starting the next phase.
- Never silently change architecture decisions.
- Never generate placeholder implementations unless explicitly requested.
  Avoid TODO-only code, fake implementations, mocked production features, or
  temporary architecture.
- If a requirement is ambiguous or two requirements conflict, stop and ask
  rather than silently choosing an interpretation.
- After every phase: update documentation, memory, backlog, and changelog,
  and commit that phase as one atomic git commit with a descriptive message
  (`phase-N: <summary>`) so any phase can be reverted independently.

---

# Development Phases

| Phase | Name |
|---|---|
| 0 | Documentation and Architecture |
| 1 | Project Foundation |
| 2 | Networking Foundation |
| 3 | Database Foundation |
| 4 | Pet Engine |
| 5 | Chat System |
| 6 | Image System |
| 7 | File Transfer System |
| 8 | Notification System |
| 9 | Performance Optimization |
| 10 | Testing and Hardening |

Never skip phases.

---

# Documentation Structure

```
docs/
00_PROJECT_CONSTITUTION.md
01_PROJECT_OVERVIEW.md
02_PRODUCT_REQUIREMENTS_DOCUMENT.md
03_PROJECT_SCOPE.md
04_SYSTEM_ARCHITECTURE.md
05_TECHNICAL_DECISIONS.md
06_NETWORK_ARCHITECTURE.md
07_MESSAGE_PROTOCOL.md
08_FILE_TRANSFER_PROTOCOL.md
09_DATABASE_SCHEMA.md
10_PET_ENGINE_SPECIFICATION.md
11_UI_UX_GUIDELINES.md
12_SECURITY_MODEL.md
13_PERFORMANCE_GUIDELINES.md
14_DEVELOPMENT_PHASES.md
15_AI_AGENT_RULES.md
16_PROJECT_MEMORY.md
17_CHANGELOG.md
18_BACKLOG.md
19_KNOWN_ISSUES.md
20_TESTING_STRATEGY.md
21_DEPLOYMENT_GUIDE.md
22_CONTRIBUTING_GUIDE.md
23_ASSET_LICENSES.md
24_API_REFERENCE.md
25_EVENT_BUS_ARCHITECTURE.md
26_STORAGE_STRATEGY.md
27_SYNC_REPLICATION_MODEL.md
```

`05_TECHNICAL_DECISIONS.md` must record concrete choices, not categories -
which mDNS crate, which SQLite binding, which hashing algorithm, which
conflict-resolution strategy - so later phases don't re-litigate them.

---

# Project Structure

This must follow Tauri v2's required layout - Tauri isn't flexible about this
part: the CLI locates the Rust project by finding `tauri.conf.json` next to
`Cargo.toml` inside `src-tauri/`, and expects `src-tauri/src/main.rs` +
`lib.rs`, `capabilities/`, and `icons/` at fixed locations. Modularity
(network/database/pets/etc.) is achieved with a **Cargo workspace inside
`src-tauri/`**, not with invented top-level folders Tauri doesn't recognize.

```
zenpaws/
├── package.json
├── vite.config.ts
├── index.html
├── src/                          # React frontend (Vite root)
│   ├── main.tsx
│   ├── app/                      # routing/shell
│   ├── features/                 # chat/, pets/, transfers/, settings/
│   ├── stores/                   # Zustand
│   ├── queries/                  # TanStack Query
│   ├── components/
│   └── shared/
│
└── src-tauri/                    # Rust project - Tauri-required layout
    ├── Cargo.toml                # workspace root
    ├── Cargo.lock
    ├── build.rs                  # tauri_build::build()
    ├── tauri.conf.json           # app id, windows, bundle config
    ├── icons/
    ├── capabilities/             # per-window permission scoping (see Security)
    │   ├── main-window.json
    │   └── pet-window.json
    ├── src/
    │   ├── main.rs                # entrypoint only - do not modify; calls app_lib::run()
    │   ├── lib.rs                 # tauri::Builder setup, plugin registration
    │   └── commands/              # thin #[tauri::command] handlers only -
    │                               # delegate to the crates below, no business logic here
    └── crates/                   # internal workspace crates - the actual modularity
        ├── zenpaws-shared/        # common types, error handling, the Event Bus
        ├── zenpaws-network/       # discovery, transport, peer/session mgmt
        ├── zenpaws-database/      # schema, migrations, queries (SQLite/WAL/FTS5)
        ├── zenpaws-pets/          # pet state machine, asset loading, window mgmt
        ├── zenpaws-transfer/      # chunked file transfer service
        ├── zenpaws-notifications/ # native + in-app notification dispatch
        ├── zenpaws-storage/       # on-disk file storage, quotas, cache
        └── zenpaws-settings/      # user preferences, persistence
```

Each `zenpaws-*` crate is a normal library crate with its own `Cargo.toml`,
added as a path dependency of the `src-tauri` binary crate. `commands/`
handlers stay thin - they translate a Tauri IPC call into a call on the
relevant crate and back, so business logic is unit-testable without spinning
up a Tauri runtime. Cross-crate communication goes through the Event Bus
defined in `zenpaws-shared`, not direct crate-to-crate coupling, preserving
the loose-coupling requirement from the Project Vision section.


---

# Networking

LAN only. No cloud, no internet relay, no external servers.

- **Discovery:** mDNS/Bonjour primary, UDP broadcast fallback.
- **Identity:** UUID + username + avatar. IP is a transport detail only,
  never identity (DHCP can reassign it).
- **Transport:** TCP with a binary protocol / binary streaming. Avoid
  WebSocket unless Phase 0 documents a specific, strong justification for it.
- **Resilience:** auto-reconnect, heartbeat, retry with backoff, explicit
  connection-state machine (connecting / connected / degraded / disconnected).
- Avoid unnecessary polling and port scanning. Minimize LAN traffic.
- **Protocol versioning:** every message envelope includes a protocol
  version field, independent of the replication message-versioning below, so
  future protocol changes don't silently break peers on an older build.
- **Delivery semantics:** given the hostless replication model (no central
  server), define explicitly in `07_MESSAGE_PROTOCOL.md` what "sent,"
  "delivered," and "read" mean - e.g. delivered = acknowledged by at least
  one directly-connected peer, read = acknowledged by the specific
  recipient. Don't leave this implicit; a P2P mesh has no default answer.

---

# Replication Model

Hostless replication. There is no server. Every peer stores shared-room
history, metadata, and local settings. Peers synchronize missing messages
after reconnecting.

Implement and document in `27_SYNC_REPLICATION_MODEL.md`:

- Message versioning and the synchronization protocol.
- **A named conflict-resolution mechanism** - e.g. last-write-wins with
  logical/Lamport clocks, or a CRDT - not just "conflict handling" as a
  category. State which one and why before Phase 3 implementation begins.

---

# Event Bus Architecture

All major modules communicate through an event bus rather than direct
coupling - e.g. `Network → EventBus → Chat`, `Network → EventBus → Pets`,
`Network → EventBus → Notifications`, `Network → EventBus → Transfers`.
Document the event taxonomy in `25_EVENT_BUS_ARCHITECTURE.md`.

---

# Messaging Features

Shared room + direct messages. Support: text, timestamps, edit, delete,
reply, emoji reactions, mentions, typing indicators, message status, search,
copy, context menus.

Must support 100,000+ messages via pagination, virtualization, and indexing.

---

# Database

SQLite with WAL mode, FTS5, prepared statements, migrations.

Never edit an existing migration - always create a new one.

---

# Image Features

Ctrl+V paste, clipboard image, drag from browser/desktop/file explorer,
multiple upload, preview, zoom, fullscreen, save, lazy loading. Images may
auto-transfer. Generate thumbnail + metadata via a dedicated
`ThumbnailService`; display thumbnail first, load originals lazily.

---

# File Features

Supported: PDF, ZIP, DOCX, TXT, CSV, MP4, images, arbitrary files.

Upload flow: generate UUID filename, store metadata in SQLite (`fileId`,
`messageId`, `senderId`, `mimeType`, `size`, `checksum`, `createdAt`,
`originalFilename`, `storedFilename`).

- **Checksum algorithm: SHA-256**, used consistently for both file-transfer
  integrity checks and any content-addressed storage.
- **Upload size limit:** document a concrete maximum (e.g. a specific MB
  figure appropriate to LAN bandwidth) in `05_TECHNICAL_DECISIONS.md` -
  "upload limits" must resolve to an actual number before Phase 7, not stay
  a placeholder.

Non-image files: never auto-transfer content, only metadata. Receiver sees a
download button; on click, requests the file, sender streams it, receiver
saves it to the downloads folder or a configured location.

---

# File Transfer

Dedicated `TransferService`, never coupled to chat logic. Chunked transfer
with pause/resume/cancel, progress reporting, and SHA-256 checksum/integrity
validation before marking complete.

---

# Desktop Pet System

First-class feature. Transparent, always-on-top windows with click-through
mode. Data-driven - no hardcoded pets; installable without code changes.

Initial pets: cat, dog, fox, rabbit, slime, ghost, parrot.

```
assets/pets/<pet>/
  manifest.json
  sprites/
  sounds/
  LICENSE.txt
```

**States:** idle, walk, run, jump, sleep, typing, notification, celebrate,
fight, play, away, offline.

**Behaviors:** chase cursor, react to rapid cursor movement, interact with
other pets, celebrate on messages, display notifications, idle behaviors.

**Future (not this build):** one pet per LAN user.

---

# Pet Synchronization

State synchronization only. Never synchronize coordinates, frame positions,
or per-frame animation state. Synchronize discrete events only: typing,
playing, sleeping, celebrating, notification, away, offline.

**Rate limit: maximum 2 pet-state broadcasts per second per pet**, with
debounce, deduplication, and rate limiting implemented explicitly - this
matters at the target scale of up to 50 concurrent peers.

---

# Notifications

Native OS notifications + pet speech-bubble notifications. Clicking a
notification opens the relevant chat. Privacy-first: no message content in
notification previews by default.

---

# Settings

Username, avatar, theme (light/dark/system), notifications, download
location, auto-download, startup behavior, font size, pet settings,
performance mode.

---

# System Features

System tray, background operation, auto-start, window-state persistence.
Clipboard: copy/paste text and images.

---

# Security

- Filename sanitization, path traversal protection, UUID-based file
  identifiers, SHA-256 checksum validation, upload size limits (see File
  Features).
- **Transport security:** TLS with TOFU (Trust On First Use) peer-identity
  pinning - since IP isn't identity, this is how a UUID+username claim gets
  validated on first contact and re-verified on reconnect.
- Document the threat model and identity-verification flow explicitly in
  `12_SECURITY_MODEL.md`: what this protects against (spoofing, casual
  snooping by other LAN devices) and what it explicitly does not (see
  Non-Goals).

## Tauri v2 Capability Scoping (per window)

Tauri v2 replaces v1's single global allowlist with per-window capability
files in `src-tauri/capabilities/`. This must be used for real least-
privilege separation, not left at defaults:

- **Main window** (`main-window.json`): the full command surface it
  legitimately needs - filesystem access scoped to the app's data/downloads
  directories, dialog, clipboard, notification, tray.
- **Pet windows** (`pet-window.json`): a deliberately minimal capability
  set - window positioning/transparency/click-through and the specific IPC
  commands pets need (state updates, asset loading). No filesystem,
  network, or dialog access from a pet window's webview context, since pet
  windows render less-trusted, frequently-updated asset content.

Document the exact permission list per window (not just "least privilege" as
a principle) in `12_SECURITY_MODEL.md`, and revisit it whenever a new
`#[tauri::command]` is added - a command is only reachable from a window if
that window's capability file explicitly grants it.

---

# Storage Strategy

Implement quotas and document limits in `26_STORAGE_STRATEGY.md` for: image
cache, thumbnail cache, temporary in-progress transfers, and logs. Prevent
uncontrolled storage growth on long-running installs.

---

# Performance Mode

Two runtime modes:

- **Full Mode** - maximum animations and interactions.
- **Performance Mode** - reduced animation rate, reduced effects, lower pet
  FPS, reduced resource usage.

---

# Performance Targets

Target hardware: dual-core Intel i3-equivalent CPU, 4GB RAM, HDD supported,
integrated GPU.

| Metric | Target |
|---|---|
| Idle RAM | < 200 MB |
| Idle CPU | < 2% |
| Pet engine | < 1% CPU per active pet |
| 100,000-message history | Smooth scrolling |
| Large image history | Lazy loaded |

---

# Testing

Write `20_TESTING_STRATEGY.md` **before Phase 1 begins**, not retrofitted
per phase - define the unit, integration, replication, LAN multi-device, and
performance testing approach up front so later phases build to it rather
than around it. No phase is complete without its testing documentation
updated accordingly.

---

# AI Memory Rules

After every phase, update `16_PROJECT_MEMORY.md`, `17_CHANGELOG.md`,
`18_BACKLOG.md`, `19_KNOWN_ISSUES.md` with: completed work, pending work,
architecture decisions, tradeoffs, risks. Always continue from memory files;
never rely solely on prompt context.

---

# Output Rules

Start with **Phase 0 only**. Phase 0 must include: documentation structure,
folder structure (the actual Tauri v2 layout - `src/` + `src-tauri/` with its
internal Cargo workspace), architecture diagrams, technology decisions,
networking decisions, replication decisions (including the named conflict-
resolution mechanism), event bus design, security decisions (including the
transport security / TOFU flow and the per-window capability files for
main-window vs. pet-window), and database decisions.

Do not generate implementation code until Phase 0 is reviewed and approved.

At the end of every phase, provide:

- Definition of Done
- A brief self-review against the ponytail ladder (what was skipped as
  unnecessary, what reused an existing dependency, what needed genuinely new
  code and why)
- Open Questions
- Risks
- Next Phase Plan

Wait for approval before proceeding.
</file>

<file path="tsconfig.node.json">
{
  "compilerOptions": {
    "composite": true,
    "skipLibCheck": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true,
    "strict": true,
    "strictNullChecks": true,
    "noImplicitAny": true
  },
  "include": ["vite.config.ts"]
}
</file>

<file path="vite.config.ts">
import react from "@vitejs/plugin-react";
import { defineConfig, loadEnv } from "vite";

export default defineConfig(({ mode }) => {
  const host = loadEnv(mode, ".", "").TAURI_DEV_HOST;

  return {
    clearScreen: false,
    plugins: [react()],
    server: {
      hmr: host
        ? {
            host,
            port: 1421,
            protocol: "ws",
          }
        : undefined,
      host,
      port: 1420,
      strictPort: true,
      watch: { ignored: ["**/src-tauri/**"] },
    },
  };
});
</file>

<file path="docs/05_TECHNICAL_DECISIONS.md">
# 05 - Technical Decisions

Every dependency here was checked against its current documentation before
being chosen - not assumed from training data - because package names and
APIs move (e.g. `framer-motion` renamed to `motion` with a `motion/react`
import path). Version numbers are what was current at decision time; pin
exact versions in `Cargo.lock`/`bun.lock`, don't hand-edit them
later without re-checking.

## Frontend

| Concern | Choice | Why |
|---|---|---|
| Framework | React 19 | Given by the brief. |
| Animation | **`motion`** (Bun-managed), import from `motion/react` | This is the same project as Framer Motion - it renamed and moved its import path. Using the old `framer-motion` package name would install a deprecated alias. Hybrid engine (WAAPI + JS fallback) gives 120fps GPU-accelerated animation with a small footprint, which matters directly for the low-end-hardware target. |
| State | Zustand v5 | Already specified; minimal boilerplate, no `any` needed for typed stores. |
| Server-state cache | TanStack Query v5 | Already specified; used for data that's conceptually "fetched" from the Rust backend via Tauri commands, even though it's local IPC not HTTP. |
| Virtualization | `@tanstack/react-virtual` | Same ecosystem as TanStack Query, avoids a second virtualization dependency for the 100k-message list requirement. |
| Linting/formatting | **Biome** (`@biomejs/biome`, schema `2.5.4`+) | One Rust binary replacing ESLint + Prettier; ~35x faster formatting, single config file (`biome.json`). `linter.rules.suspicious.noExplicitAny` is set to `error` to enforce the no-`any` rule mechanically, not just by convention. |
| Type checking | TypeScript, `strict: true` | `noImplicitAny`, `strictNullChecks`, etc. all on - see `tsconfig.json`. |

## Backend (Rust)

| Concern | Choice | Why |
|---|---|---|
| Async runtime | Tokio | Given by the brief; industry standard, what Tauri v2 itself is built on. |
| LAN discovery | `mdns-sd` | Pure-Rust mDNS/DNS-SD, no forced async runtime dependency but has a Tokio-compatible async interface, actively maintained. Preferred over `libmdns` (responder-only, no discovery) and `simple-mdns`/`rtc-mdns` (smaller communities, more manual protocol handling) for this project's need to both advertise and discover. |
| Database access | `rusqlite` (bundled SQLite) | Chosen over `sqlx` and over Tauri's official `tauri-plugin-sql`. Reasons: (1) FTS5 virtual tables and WAL pragmas are easiest to control directly against `rusqlite`'s connection API; (2) `tauri-plugin-sql` exposes a raw-SQL-execution surface to the frontend over IPC, which is a larger attack surface than needed - this project's database access goes through typed Rust query functions only, never raw SQL from the frontend; (3) per the ponytail ladder, we don't need `sqlx`'s compile-time query checking against a running DB for a single-file embedded SQLite use case. |
| Serialization / wire format | `serde` + `bincode` | Binary framing per `07_MESSAGE_PROTOCOL.md`; smaller and faster to (de)serialize than JSON for the message/gossip traffic this app generates continuously. |
| Checksums | `sha2` (SHA-256) | Standard, audited, used consistently for file-transfer integrity and any content-addressed storage. |
| TLS | `rustls` + `rcgen` (self-signed cert generation for TOFU) | `rustls` avoids a system OpenSSL dependency (relevant for the low-end/varied-OS target); `rcgen` generates the self-signed identity certs each peer needs for TOFU pinning. |
| Async TLS I/O | `tokio-rustls` | Direct Tokio stream integration for the existing `rustls` configuration; avoids introducing a second TLS stack. |
| Discovery fallback encoding | `base64` | Public certificate DER is split into bounded mDNS TXT strings; Base64 provides safe text transport without custom encoding code. |
| Error handling | `thiserror` (library crates) + `anyhow` (binary/command layer) | Standard split: typed errors in `zenpaws-*` crates, ergonomic error propagation in the thin command layer. |
| Logging | `tracing` | Structured, async-aware, works well with Tokio. |
| Linting | Clippy, `[workspace.lints.clippy]` in the workspace `Cargo.toml`: `all = "warn"`, `pedantic = "warn"`, `nursery = "warn"`, with `module_name_repetitions` and `must_use_candidate` allowed (noisy, low-value for this codebase's naming conventions). CI runs `cargo clippy --all-targets -- -D warnings`. |
| Formatting | `rustfmt`, default width, `cargo fmt --check` in CI. |
| Test runner | `cargo-nextest` (dev-only tool, not a dependency) | Faster, clearer parallel output than `cargo test`; used in CI per `20_TESTING_STRATEGY.md`. |

## Tauri plugins (official, `tauri-apps/plugins-workspace`)

Registered only as each feature phase needs them - not all up front, per the
ponytail ladder's "does this need to exist yet":

| Plugin | Registered in | Purpose |
|---|---|---|
| `tauri-plugin-single-instance` | Phase 1 | Prevents two ZenPaws instances presenting duplicate peer identities from the same machine. |
| `tauri-plugin-window-state` | Phase 1 | Window position/size persistence (explicit requirement). |
| `tauri-plugin-dialog` | Phase 6/7 | Native file save/open dialogs for images and file transfer. |
| `tauri-plugin-notification` | Phase 8 | Native OS notifications. |
| `tauri-plugin-autostart` | Phase 1 settings groundwork, wired to a user toggle in Phase 8 | Startup-behavior setting. |
| `tauri-plugin-clipboard-manager` | Phase 6 | Copy/paste text and images. |

`tauri-plugin-global-shortcut` was considered and deliberately **not**
included - no requirement calls for a global hotkey, and adding it now would
violate the ladder's first rung ("does this need to exist at all").

## Replication & conflict resolution

- **Conflict resolution: last-write-wins using Lamport logical clocks**
  `(peer_uuid, logical_counter)`, tie-broken by `peer_uuid` ordering. Chosen
  over a CRDT because messages are simple field-level edits (text, deleted
  flag), not concurrently-merged structured data - a CRDT would be solving a
  problem this app doesn't have, per the ladder. Documented in full in
  `27_SYNC_REPLICATION_MODEL.md`.
- **Message envelope**: 1-byte protocol version + `bincode`-serialized
  `serde` enum, length-prefixed (u32 LE) over TCP. See
  `07_MESSAGE_PROTOCOL.md`.

## Concrete limits (previously left as placeholders - now decided)

| Limit | Value |
|---|---|
| Max file size (any attachment) | 2 GB, chunked transfer |
| Image auto-transfer threshold | 25 MB per image before it requires manual download like other files |
| Pet state broadcast rate | 2/sec/pet (already specified in the brief) |
| SHA-256 used for | File-transfer integrity, content-addressed storage keys |
</file>

<file path="docs/09_DATABASE_SCHEMA.md">
# 09 - Database Schema

SQLite, WAL mode, FTS5, prepared statements only (no string-concatenated
SQL, ever - this is also a security requirement, see `12_SECURITY_MODEL.md`).
Accessed exclusively through `zenpaws-database`'s typed query functions;
never raw SQL from the frontend (see the `rusqlite` decision in
`05_TECHNICAL_DECISIONS.md`).

## Migrations

Managed as sequential, never-edited files (`migrations/0001_init.sql`,
`0002_...`). **Never edit an existing migration** - always add a new one,
even to fix a mistake in an earlier one. This is what keeps every peer's
schema reconstructable from history.

## Core tables (initial shape - refined in Phase 3 implementation)

```sql
CREATE TABLE peers (
    uuid TEXT PRIMARY KEY,
    username TEXT NOT NULL,
    avatar_path TEXT,
    pubkey_fingerprint BLOB NOT NULL,   -- TOFU pin, see 12_SECURITY_MODEL.md
    last_seen_at INTEGER NOT NULL
);

CREATE TABLE messages (
    id TEXT PRIMARY KEY,                -- UUID
    room TEXT NOT NULL,                 -- 'shared' or a DM peer UUID
    author_uuid TEXT NOT NULL REFERENCES peers(uuid),
    body TEXT NOT NULL,
    reply_to TEXT REFERENCES messages(id),
    lamport_peer TEXT NOT NULL,         -- Lamport clock, see 27_SYNC_REPLICATION_MODEL.md
    lamport_counter INTEGER NOT NULL,
    created_at INTEGER NOT NULL,
    edited_at INTEGER,
    deleted_at INTEGER
);

CREATE VIRTUAL TABLE messages_fts USING fts5(
    body, content='messages', content_rowid='rowid'
);

CREATE TABLE reactions (
    message_id TEXT NOT NULL REFERENCES messages(id),
    peer_uuid TEXT NOT NULL REFERENCES peers(uuid),
    emoji TEXT NOT NULL,
    PRIMARY KEY (message_id, peer_uuid, emoji)
);

CREATE TABLE message_acks (
    message_id TEXT NOT NULL REFERENCES messages(id),
    peer_uuid TEXT NOT NULL REFERENCES peers(uuid),
    kind TEXT NOT NULL CHECK (kind IN ('delivered', 'read')),
    acked_at INTEGER NOT NULL,
    PRIMARY KEY (message_id, peer_uuid, kind)
);

CREATE TABLE files (
    id TEXT PRIMARY KEY,                -- fileId (UUID)
    message_id TEXT NOT NULL REFERENCES messages(id),
    sender_uuid TEXT NOT NULL REFERENCES peers(uuid),
    mime_type TEXT NOT NULL,
    size_bytes INTEGER NOT NULL,
    checksum_sha256 BLOB NOT NULL,
    original_filename TEXT NOT NULL,
    stored_filename TEXT NOT NULL,      -- UUID-based, sanitized
    created_at INTEGER NOT NULL
);

CREATE TABLE settings (
    key TEXT PRIMARY KEY,
    value TEXT NOT NULL                 -- JSON-encoded
);
```

## Applied migrations

- `0001_init.sql` creates the documented core tables, FTS5 virtual table, and indexes.
- `0002_messages_fts.sql` adds FTS synchronization triggers. Existing migration files were not edited.
- `0003_peer_certificates.sql` stores the public certificate associated with a peer TOFU pin.

## Indexing

- `messages(room, created_at)` - the hot path for scrollback pagination.
- `messages(lamport_peer, lamport_counter)` - sync/conflict resolution
  lookups (`27_SYNC_REPLICATION_MODEL.md`).
- FTS5's own index covers search; no separate search index needed.

## Why these choices satisfy the 100k-message requirement

Pagination queries against `messages(room, created_at)` with `LIMIT`/keyset
pagination (not `OFFSET`, which degrades at scale) plus the virtualized list
on the frontend (`@tanstack/react-virtual`) are what make 100,000+ messages
scroll smoothly - this is a combined DB + UI concern, cross-reference
`13_PERFORMANCE_GUIDELINES.md`.
</file>

<file path="src/app/app-shell.tsx">
import { motion } from "motion/react";
import type { ReactNode } from "react";
import { ChatShell } from "../features/chat/chat-shell";
import { useAppStatus } from "../hooks/use-app-status";

/**
 * The visible Phase 1 shell: confirms the Rust backend is reachable over
 * IPC. Replaced by real chat/pet UI starting Phase 2+ - this file's only
 * job right now is to prove the wiring works, using the same stack
 * (Motion, TanStack Query) the real UI will use, so Phase 2 isn't the first
 * time any of it runs.
 */
export function AppShell(): ReactNode {
  const { data, isLoading, isError } = useAppStatus();

  if (!(isLoading || isError)) {
    return <ChatShell />;
  }

  return (
    <motion.main
      animate={{ opacity: 1 }}
      initial={{ opacity: 0 }}
      style={styles.main}
      transition={{ duration: 0.3 }}
    >
      <h1 style={styles.heading}>ZenPaws</h1>
      <p style={styles.status}>{renderStatus(isLoading, isError, data?.version)}</p>
    </motion.main>
  );
}

function renderStatus(isLoading: boolean, isError: boolean, version: string | undefined): string {
  if (isLoading) {
    return "Connecting to backend...";
  }
  if (isError) {
    return "Backend unreachable - check the Rust process.";
  }
  return `Backend connected - v${version ?? "unknown"}`;
}

const styles = {
  heading: { fontSize: "2rem", margin: 0 },
  main: {
    alignItems: "center",
    display: "flex",
    flexDirection: "column" as const,
    fontFamily: "system-ui, sans-serif",
    gap: "0.5rem",
    height: "100vh",
    justifyContent: "center",
  },
  status: { color: "#666", margin: 0 },
};
</file>

<file path="src-tauri/crates/zenpaws-database/src/acks.rs">
use rusqlite::params;
use thiserror::Error;
use uuid::Uuid;
use zenpaws_shared::PeerId;

use crate::Database;

/// Receipt kinds allowed by the hostless chat protocol.
#[derive(Clone, Copy, Debug, Eq, PartialEq)]
pub enum MessageAckKind {
    Delivered,
    Read,
}

/// Aggregate acknowledgement state for one message and recipient.
#[derive(Clone, Copy, Debug, Eq, PartialEq)]
pub struct MessageStatus {
    pub delivered: bool,
    pub read: bool,
}

impl MessageAckKind {
    const fn as_sql(self) -> &'static str {
        match self {
            Self::Delivered => "delivered",
            Self::Read => "read",
        }
    }
}

impl Database {
    /// Returns delivery and read state for one recipient.
    ///
    /// # Errors
    ///
    /// Returns an error when acknowledgement data cannot be queried.
    pub fn message_status(
        &self,
        message_id: Uuid,
        peer_id: PeerId,
    ) -> Result<MessageStatus, MessageAckError> {
        let mut statement = self.connection.prepare(
            "SELECT kind FROM message_acks WHERE message_id = ?1 AND peer_uuid = ?2",
        )?;
        let rows = statement.query_map(
            params![message_id.to_string(), peer_id.as_uuid().to_string()],
            |row| row.get::<_, String>(0),
        )?;
        let mut status = MessageStatus { delivered: false, read: false };
        for kind in rows {
            match kind?.as_str() {
                "delivered" => status.delivered = true,
                "read" => status.read = true,
                _ => {}
            }
        }
        Ok(status)
    }

    /// Stores an idempotent delivery or read acknowledgement.
    ///
    /// # Errors
    ///
    /// Returns an error for an unknown message, failed persistence, or a read
    /// acknowledgement for the shared room.
    pub fn acknowledge_message(
        &self,
        message_id: Uuid,
        peer_id: PeerId,
        kind: MessageAckKind,
        acked_at: i64,
    ) -> Result<(), MessageAckError> {
        let room: String = self.connection.query_row(
            "SELECT room FROM messages WHERE id = ?1",
            [message_id.to_string()],
            |row| row.get(0),
        )?;
        if kind == MessageAckKind::Read && room == "shared" {
            return Err(MessageAckError::SharedRoomRead);
        }
        self.connection.execute(
            "INSERT OR IGNORE INTO message_acks (message_id, peer_uuid, kind, acked_at)
             VALUES (?1, ?2, ?3, ?4)",
            params![
                message_id.to_string(),
                peer_id.as_uuid().to_string(),
                kind.as_sql(),
                acked_at
            ],
        )?;
        Ok(())
    }
}

/// Message acknowledgement failures.
#[derive(Debug, Error)]
pub enum MessageAckError {
    #[error("shared-room messages do not have read receipts")]
    SharedRoomRead,
    #[error("message acknowledgement database operation failed")]
    Sqlite(#[from] rusqlite::Error),
}
</file>

<file path="src-tauri/crates/zenpaws-database/Cargo.toml">
[package]
name = "zenpaws-database"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
rusqlite.workspace = true
thiserror.workspace = true
uuid.workspace = true
zenpaws-shared = { path = "../zenpaws-shared" }

[lints]
workspace = true
</file>

<file path="src-tauri/crates/zenpaws-pets/Cargo.toml">
[package]
name = "zenpaws-pets"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
uuid.workspace = true
zenpaws-shared = { path = "../zenpaws-shared" }

[lints]
workspace = true
</file>

<file path=".zed/settings.json">
{
  "format_on_save": "on",
  "formatter": "language_server",
  "lsp": {
    "typescript-language-server": {
      "settings": {
        "typescript": {
          "preferences": {
            "includePackageJsonAutoImports": "on"
          }
        }
      }
    }
  },
  "languages": {
    "JavaScript": {
      "code_actions_on_format": {
        "source.fixAll.biome": true,
        "source.organizeImports.biome": true
      },
      "formatter": {
        "language_server": {
          "name": "biome"
        }
      }
    },
    "TSX": {
      "code_actions_on_format": {
        "source.fixAll.biome": true,
        "source.organizeImports.biome": true
      },
      "formatter": {
        "language_server": {
          "name": "biome"
        }
      }
    },
    "TypeScript": {
      "code_actions_on_format": {
        "source.fixAll.biome": true,
        "source.organizeImports.biome": true
      },
      "formatter": {
        "language_server": {
          "name": "biome"
        }
      }
    }
  }
}
</file>

<file path="src-tauri/crates/zenpaws-database/src/lib.rs">
//! `SQLite` persistence, migrations, WAL mode, and typed database operations.
//!
//! See `docs/09_DATABASE_SCHEMA.md`.

use std::{fs, path::Path};

use rusqlite::{Connection, OpenFlags, params};
use thiserror::Error;
use zenpaws_shared::PeerId;

mod acks;
mod messages;
mod trust;

pub use acks::{MessageAckError, MessageAckKind, MessageStatus};
pub use messages::{MessageCursor, MessageRecord};
pub use trust::DatabaseTrustStore;

const MIGRATIONS: [(&str, &str); 3] = [
    ("0001_init", include_str!("../migrations/0001_init.sql")),
    (
        "0002_messages_fts",
        include_str!("../migrations/0002_messages_fts.sql"),
    ),
    (
        "0003_peer_certificates",
        include_str!("../migrations/0003_peer_certificates.sql"),
    ),
];

/// A typed `SQLite` database connection owned by the database crate.
pub struct Database {
    connection: Connection,
}

impl Database {
    /// Opens a database, enables required `SQLite` pragmas, and applies migrations.
    ///
    /// # Errors
    ///
    /// Returns an error when `SQLite` cannot open, configure, migrate, or validate
    /// the local database.
    pub fn open(path: impl AsRef<Path>) -> Result<Self, DatabaseError> {
        let path = path.as_ref();
        if let Some(parent) = path.parent() {
            fs::create_dir_all(parent)?;
        }
        let connection = Connection::open_with_flags(
            path,
            OpenFlags::SQLITE_OPEN_CREATE | OpenFlags::SQLITE_OPEN_READ_WRITE,
        )?;
        Self::configure(&connection)?;
        Self::migrate(&connection)?;
        Ok(Self { connection })
    }

    /// Creates an isolated in-memory database for tests.
    ///
    /// # Errors
    ///
    /// Returns an error when `SQLite` configuration or migration fails.
    pub fn open_in_memory() -> Result<Self, DatabaseError> {
        let connection = Connection::open_in_memory()?;
        Self::configure(&connection)?;
        Self::migrate(&connection)?;
        Ok(Self { connection })
    }

    /// Upserts peer metadata while preserving prepared-statement boundaries.
    ///
    /// # Errors
    ///
    /// Returns an error if peer metadata cannot be written.
    pub fn upsert_peer(
        &self,
        peer_id: PeerId,
        username: &str,
        fingerprint: &[u8],
        last_seen_at: i64,
    ) -> Result<(), DatabaseError> {
        self.connection.execute(
            "INSERT INTO peers (uuid, username, pubkey_fingerprint, last_seen_at)
             VALUES (?1, ?2, ?3, ?4)
             ON CONFLICT(uuid) DO UPDATE SET
               username = excluded.username,
               pubkey_fingerprint = excluded.pubkey_fingerprint,
               last_seen_at = excluded.last_seen_at",
            params![
                peer_id.as_uuid().to_string(),
                username,
                fingerprint,
                last_seen_at
            ],
        )?;
        Ok(())
    }

    /// Upserts one JSON-encoded application setting.
    ///
    /// # Errors
    ///
    /// Returns an error if the setting cannot be written.
    pub fn set_setting(&self, key: &str, value: &str) -> Result<(), DatabaseError> {
        self.connection.execute(
            "INSERT INTO settings (key, value) VALUES (?1, ?2)
             ON CONFLICT(key) DO UPDATE SET value = excluded.value",
            params![key, value],
        )?;
        Ok(())
    }

    /// Returns a JSON-encoded application setting when it exists.
    ///
    /// # Errors
    ///
    /// Returns an error if the setting query fails.
    pub fn setting(&self, key: &str) -> Result<Option<String>, DatabaseError> {
        let value =
            self.connection
                .query_row("SELECT value FROM settings WHERE key = ?1", [key], |row| {
                    row.get(0)
                });
        match value {
            Ok(value) => Ok(Some(value)),
            Err(rusqlite::Error::QueryReturnedNoRows) => Ok(None),
            Err(error) => Err(DatabaseError::Sqlite(error)),
        }
    }

    /// Returns the applied migration count.
    ///
    /// # Errors
    ///
    /// Returns an error if migration metadata cannot be queried.
    pub fn migration_count(&self) -> Result<u32, DatabaseError> {
        let count =
            self.connection
                .query_row("SELECT COUNT(*) FROM schema_migrations", [], |row| {
                    row.get::<_, u32>(0)
                })?;
        Ok(count)
    }

    fn configure(connection: &Connection) -> Result<(), DatabaseError> {
        connection.execute_batch(
            "PRAGMA foreign_keys = ON;
             PRAGMA journal_mode = WAL;
             PRAGMA synchronous = NORMAL;",
        )?;
        connection.busy_timeout(std::time::Duration::from_secs(5))?;
        Ok(())
    }

    fn migrate(connection: &Connection) -> Result<(), DatabaseError> {
        connection.execute_batch(
            "CREATE TABLE IF NOT EXISTS schema_migrations (
                 name TEXT PRIMARY KEY,
                 applied_at INTEGER NOT NULL
             );",
        )?;
        for (name, sql) in MIGRATIONS {
            let applied = connection.query_row(
                "SELECT EXISTS(SELECT 1 FROM schema_migrations WHERE name = ?1)",
                [name],
                |row| row.get::<_, bool>(0),
            )?;
            if !applied {
                let transaction = connection.unchecked_transaction()?;
                transaction.execute_batch(sql)?;
                transaction.execute(
                    "INSERT INTO schema_migrations (name, applied_at) VALUES (?1, unixepoch())",
                    [name],
                )?;
                transaction.commit()?;
            }
        }
        Ok(())
    }
}

/// Database failures exposed by typed database functions.
#[derive(Debug, Error)]
pub enum DatabaseError {
    #[error("database path I/O failed")]
    Io(#[from] std::io::Error),
    #[error("`SQLite` operation failed")]
    Sqlite(#[from] rusqlite::Error),
}

#[cfg(test)]
mod tests;
</file>

<file path="src-tauri/crates/zenpaws-database/src/messages.rs">
use rusqlite::{params, types::Type};
use uuid::Uuid;
use zenpaws_shared::PeerId;

use crate::{Database, DatabaseError};

/// A persisted chat message independent of frontend representation.
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct MessageRecord {
    pub author: PeerId,
    pub body: String,
    pub created_at: i64,
    pub id: Uuid,
    pub lamport_counter: i64,
    pub lamport_peer: PeerId,
    pub reply_to: Option<Uuid>,
    pub room: String,
}

/// Stable keyset cursor for descending message history.
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct MessageCursor {
    pub created_at: i64,
    pub id: Uuid,
}

impl Database {
    /// Inserts one message through a prepared statement.
    ///
    /// # Errors
    ///
    /// Returns an error when foreign keys or message constraints fail.
    pub fn insert_message(&self, message: &MessageRecord) -> Result<(), DatabaseError> {
        self.connection.execute(
            "INSERT INTO messages (
                id, room, author_uuid, body, reply_to, lamport_peer,
                lamport_counter, created_at
             ) VALUES (?1, ?2, ?3, ?4, ?5, ?6, ?7, ?8)",
            params![
                message.id.to_string(),
                message.room,
                message.author.as_uuid().to_string(),
                message.body,
                message.reply_to.map(|id| id.to_string()),
                message.lamport_peer.as_uuid().to_string(),
                message.lamport_counter,
                message.created_at,
            ],
        )?;
        Ok(())
    }

    /// Returns one room page using a keyset cursor rather than `OFFSET`.
    ///
    /// # Errors
    ///
    /// Returns an error when the query or row decoding fails.
    pub fn messages_before(
        &self,
        room: &str,
        cursor: Option<&MessageCursor>,
        limit: u32,
    ) -> Result<Vec<MessageRecord>, DatabaseError> {
        let limit = i64::from(limit.clamp(1, 200));
        let mut messages = Vec::new();
        if let Some(cursor) = cursor {
            let mut statement = self.connection.prepare(
                "SELECT id, room, author_uuid, body, reply_to, lamport_peer,
                        lamport_counter, created_at
                 FROM messages
                 WHERE room = ?1
                   AND (created_at < ?2 OR (created_at = ?2 AND id < ?3))
                 ORDER BY created_at DESC, id DESC
                 LIMIT ?4",
            )?;
            let rows = statement.query_map(
                params![room, cursor.created_at, cursor.id.to_string(), limit],
                row_to_message,
            )?;
            messages.extend(rows.collect::<Result<Vec<_>, _>>()?);
        } else {
            let mut statement = self.connection.prepare(
                "SELECT id, room, author_uuid, body, reply_to, lamport_peer,
                            lamport_counter, created_at
                     FROM messages
                     WHERE room = ?1
                     ORDER BY created_at DESC, id DESC
                     LIMIT ?2",
            )?;
            let rows = statement.query_map(params![room, limit], row_to_message)?;
            messages.extend(rows.collect::<Result<Vec<_>, _>>()?);
        }
        Ok(messages)
    }

    /// Edits a message body and records the edit timestamp.
    ///
    /// # Errors
    ///
    /// Returns an error when the message update fails.
    pub fn edit_message(&self, id: Uuid, body: &str, edited_at: i64) -> Result<(), DatabaseError> {
        self.connection.execute(
            "UPDATE messages SET body = ?1, edited_at = ?2 WHERE id = ?3",
            params![body, edited_at, id.to_string()],
        )?;
        Ok(())
    }

    /// Marks a message deleted while retaining replication history.
    ///
    /// # Errors
    ///
    /// Returns an error when the message update fails.
    pub fn delete_message(&self, id: Uuid, deleted_at: i64) -> Result<(), DatabaseError> {
        self.connection.execute(
            "UPDATE messages SET deleted_at = ?1 WHERE id = ?2",
            params![deleted_at, id.to_string()],
        )?;
        Ok(())
    }

    /// Adds one unique emoji reaction through a prepared statement.
    ///
    /// # Errors
    ///
    /// Returns an error when reaction persistence fails.
    pub fn add_reaction(
        &self,
        message_id: Uuid,
        peer_id: PeerId,
        emoji: &str,
    ) -> Result<(), DatabaseError> {
        self.connection.execute(
            "INSERT OR IGNORE INTO reactions (message_id, peer_uuid, emoji) VALUES (?1, ?2, ?3)",
            params![message_id.to_string(), peer_id.as_uuid().to_string(), emoji],
        )?;
        Ok(())
    }

    /// Searches locally indexed message text.
    ///
    /// # Errors
    ///
    /// Returns an error when the FTS query or row decoding fails.
    pub fn search_messages(
        &self,
        query: &str,
        limit: u32,
    ) -> Result<Vec<MessageRecord>, DatabaseError> {
        let mut statement = self.connection.prepare(
            "SELECT m.id, m.room, m.author_uuid, m.body, m.reply_to, m.lamport_peer,
                    m.lamport_counter, m.created_at
             FROM messages_fts
             JOIN messages AS m ON m.rowid = messages_fts.rowid
             WHERE messages_fts MATCH ?1
             ORDER BY m.created_at DESC, m.id DESC
             LIMIT ?2",
        )?;
        let rows = statement.query_map(
            params![query, i64::from(limit.clamp(1, 100))],
            row_to_message,
        )?;
        Ok(rows.collect::<Result<Vec<_>, _>>()?)
    }
}

fn row_to_message(row: &rusqlite::Row<'_>) -> rusqlite::Result<MessageRecord> {
    let parse_uuid = |index| {
        row.get::<_, String>(index).and_then(|value| {
            Uuid::parse_str(&value).map_err(|error| {
                rusqlite::Error::FromSqlConversionFailure(index, Type::Text, Box::new(error))
            })
        })
    };
    Ok(MessageRecord {
        id: parse_uuid(0)?,
        room: row.get(1)?,
        author: PeerId::from_uuid(parse_uuid(2)?),
        body: row.get(3)?,
        reply_to: row
            .get::<_, Option<String>>(4)?
            .map(|value| Uuid::parse_str(&value))
            .transpose()
            .map_err(|error| {
                rusqlite::Error::FromSqlConversionFailure(4, Type::Text, Box::new(error))
            })?,
        lamport_peer: PeerId::from_uuid(parse_uuid(5)?),
        lamport_counter: row.get(6)?,
        created_at: row.get(7)?,
    })
}
</file>

<file path="src-tauri/crates/zenpaws-pets/src/lib.rs">
//! Data-driven pet manifests, local state, and synchronized-state rate limits.
//!
//! See `docs/10_PET_ENGINE_SPECIFICATION.md`.

mod behavior;
mod engine;
mod manifest;
mod registry;
mod state;

pub use behavior::{BehaviorDecision, CursorBehavior, PerformanceMode, Point};
pub use engine::{PetEngine, PetEngineError};
pub use manifest::{PetManifest, PetManifestError};
pub use registry::{PetRegistry, PetRegistryError};
pub use state::{PetInstance, PetState, PetStateError};
</file>

<file path="src-tauri/crates/zenpaws-shared/src/lib.rs">
//! Shared types and the cross-crate event bus.
//!
//! See `docs/25_EVENT_BUS_ARCHITECTURE.md`.

use std::sync::Arc;

use serde::{Deserialize, Serialize};
use tokio::sync::broadcast;
use uuid::Uuid;

mod trust;

pub use trust::{CertificatePin, PeerTrustStore, TrustStoreError, TrustedPeer};

/// Stable peer identity independent of a peer's current IP address.
#[derive(Clone, Copy, Debug, Deserialize, Eq, Hash, PartialEq, Serialize)]
#[serde(transparent)]
pub struct PeerId(Uuid);

impl PeerId {
    /// Creates a fresh peer identity.
    #[must_use]
    pub fn new() -> Self {
        Self(Uuid::new_v4())
    }

    /// Returns the underlying UUID.
    #[must_use]
    pub const fn as_uuid(self) -> Uuid {
        self.0
    }

    /// Creates an identity from a persisted or received UUID.
    #[must_use]
    pub const fn from_uuid(uuid: Uuid) -> Self {
        Self(uuid)
    }
}

impl Default for PeerId {
    fn default() -> Self {
        Self::new()
    }
}

/// A state transition visible to other modules through the event bus.
#[derive(Clone, Copy, Debug, Deserialize, Eq, PartialEq, Serialize)]
pub enum ConnectionState {
    Connecting,
    Connected,
    Degraded,
    Disconnected,
}

/// Discrete network-visible pet states. Animation data remains local.
#[derive(Clone, Copy, Debug, Deserialize, Eq, PartialEq, Serialize)]
pub enum PetSyncState {
    Away,
    Celebrate,
    Notification,
    Offline,
    Play,
    Sleep,
    Typing,
}

/// Cross-module events. Feature-specific payloads remain in their owner crate.
#[derive(Clone, Debug, Deserialize, Eq, PartialEq, Serialize)]
pub enum ZenPawsEvent {
    PeerStateChanged {
        peer_id: PeerId,
        state: ConnectionState,
    },
    PeerTrustViolation {
        peer_id: PeerId,
    },
    PetStateChanged {
        pet_instance: Uuid,
        state: PetSyncState,
    },
    TypingChanged {
        peer_id: PeerId,
        room: String,
        is_typing: bool,
    },
}

/// In-process fanout for decoupled backend modules.
#[derive(Clone, Debug)]
pub struct EventBus {
    sender: Arc<broadcast::Sender<ZenPawsEvent>>,
}

impl EventBus {
    /// Creates a bus with a bounded event history for slow subscribers.
    #[must_use]
    pub fn new(capacity: usize) -> Self {
        let (sender, _) = broadcast::channel(capacity);
        Self {
            sender: Arc::new(sender),
        }
    }

    /// Publishes an event to all active subscribers.
    pub fn publish(&self, event: ZenPawsEvent) {
        let _ = self.sender.send(event);
    }

    /// Subscribes to future events.
    #[must_use]
    pub fn subscribe(&self) -> broadcast::Receiver<ZenPawsEvent> {
        self.sender.subscribe()
    }
}

#[cfg(test)]
mod tests {
    use super::{ConnectionState, EventBus, PeerId, ZenPawsEvent};

    #[tokio::test]
    async fn publishes_to_subscribers() {
        let bus = EventBus::new(1);
        let mut receiver = bus.subscribe();
        let event = ZenPawsEvent::PeerStateChanged {
            peer_id: PeerId::new(),
            state: ConnectionState::Connected,
        };

        bus.publish(event.clone());

        assert_eq!(receiver.recv().await.expect("event is available"), event);
    }
}
</file>

<file path="src-tauri/src/commands/mod.rs">
//! Thin Tauri command handlers - IPC translation only.
//!
//! Every command here validates input, delegates to the relevant
//! `zenpaws-*` crate, and maps its result to a serializable type. See
//! `docs/04_SYSTEM_ARCHITECTURE.md` and `docs/15_AI_AGENT_RULES.md`.

pub mod chat;
pub mod network;
pub mod pets;

use serde::Serialize;

/// Phase 1 smoke-test command verifying the frontend↔backend IPC bridge
/// compiles and works end-to-end. Not a feature - remove or repurpose once
/// Phase 2 adds the first real command and this is no longer needed as a
/// wiring check.
#[derive(Debug, Serialize)]
pub struct AppStatus {
    pub name: &'static str,
    pub version: &'static str,
}

#[tauri::command]
pub const fn app_status() -> AppStatus {
    AppStatus {
        name: "ZenPaws",
        version: env!("CARGO_PKG_VERSION"),
    }
}
</file>

<file path="src-tauri/src/commands/network.rs">
#![allow(clippy::needless_pass_by_value, reason = "Tauri command IPC deserializes owned arguments and State")

use std::{
    net::SocketAddr,
    sync::{Arc, OnceLock},
    time::Duration,
};

use serde::Serialize;
use tauri::{AppHandle, Manager, State};
use zenpaws_database::DatabaseTrustStore;
use zenpaws_network::{NetworkService, TlsIdentity};
use zenpaws_settings::{LocalIdentity, load_local_identity, save_local_identity};
use zenpaws_shared::{EventBus, PeerId, PeerTrustStore};

struct NetworkRuntime {
    service: Arc<NetworkService>,
    shutdown: tokio::sync::watch::Sender<bool>,
}

static NETWORK_RUNTIME: OnceLock<NetworkRuntime> = OnceLock::new();

/// Details returned after the local LAN listener begins.
#[derive(Debug, Serialize)]
pub struct NetworkStatus {
    pub peer_id: String,
    pub tcp_port: u16,
}

/// Starts the local encrypted LAN listener exactly once.
#[tauri::command]
pub async fn start_network(
    app: AppHandle,
    database: State<'_, crate::DatabaseState>,
    username: String,
) -> Result<NetworkStatus, String> {
    if let Some(runtime) = NETWORK_RUNTIME.get() {
        return status_for(&runtime.service);
    }

    let data_dir = app
        .path()
        .app_data_dir()
        .map_err(|error| error.to_string())?;
    let path = data_dir.join("network-identity.json");
    let (identity, tls_identity) = load_or_create_identity(&path, username)?;
    let last_seen_at = std::time::SystemTime::now()
        .duration_since(std::time::UNIX_EPOCH)
        .map_err(|error| error.to_string())?
        .as_millis()
        .try_into()
        .map_err(|_| "system timestamp exceeds i64".to_owned())?;
    database
        .0
        .lock()
        .map_err(|_| "database lock poisoned".to_owned())?
        .upsert_peer(
            identity.peer_id(),
            identity.username(),
            &tls_identity.pin().as_bytes(),
            last_seen_at,
        )
        .map_err(|error| error.to_string())?;
    let service = NetworkService::bind(
        SocketAddr::from(([0, 0, 0, 0], 0)),
        identity.peer_id(),
        identity.username().to_owned(),
        &tls_identity,
        EventBus::new(256),
        Duration::from_secs(30),
    )
    .await
    .map_err(|error| error.to_string())?;
    let trust_store: Arc<dyn PeerTrustStore> = Arc::new(DatabaseTrustStore::new(Arc::clone(&database.0)));
    let service = Arc::new(service);
    let status = status_for(&service)?;

    let (shutdown, receiver) = tokio::sync::watch::channel(false);
    NETWORK_RUNTIME
        .set(NetworkRuntime {
            service: Arc::clone(&service),
            shutdown,
        })
        .map_err(|_| "network service started concurrently".to_owned())?;
    tokio::spawn(Arc::clone(&service).run_inbound(receiver.clone()));
    tokio::spawn(Arc::clone(&service).run_discovery(Arc::clone(&trust_store), receiver.clone()));
    tokio::spawn(service.run_udp(trust_store, receiver));
    Ok(status)
}

/// Stops LAN discovery and listener lifecycle tasks.
#[tauri::command]
pub fn stop_network() -> Result<(), String> {
    let runtime = NETWORK_RUNTIME
        .get()
        .ok_or_else(|| "network service is not running".to_owned())?;
    runtime
        .shutdown
        .send(true)
        .map_err(|error| error.to_string())?;
    runtime
        .service
        .shutdown()
        .map_err(|error| error.to_string())
}

pub(super) fn local_peer_id() -> Result<PeerId, String> {
    NETWORK_RUNTIME
        .get()
        .map(|runtime| runtime.service.local_peer_id())
        .ok_or_else(|| "network service is not running".to_owned())
}

fn status_for(service: &NetworkService) -> Result<NetworkStatus, String> {
    let address = service.local_addr().map_err(|error| error.to_string())?;
    Ok(NetworkStatus {
        peer_id: service.local_peer_id().as_uuid().to_string(),
        tcp_port: address.port(),
    })
}

fn load_or_create_identity(
    path: &std::path::Path,
    username: String,
) -> Result<(LocalIdentity, TlsIdentity), String> {
    if let Some(identity) = load_local_identity(path).map_err(|error| error.to_string())? {
        let tls = TlsIdentity::from_der(
            identity.certificate_der().to_vec(),
            identity.private_key_der().to_vec(),
        );
        return Ok((identity, tls));
    }

    let peer_id = PeerId::new();
    let tls = TlsIdentity::generate(format!("zenpaws-{}.local", peer_id.as_uuid()))
        .map_err(|error| error.to_string())?;
    let identity = LocalIdentity::new(
        peer_id,
        username,
        tls.certificate().as_ref().to_vec(),
        tls.private_key_der(),
    )
    .map_err(|error| error.to_string())?;
    save_local_identity(path, &identity).map_err(|error| error.to_string())?;
    Ok((identity, tls))
}
</file>

<file path="src-tauri/.gitignore">
# Generated by Cargo
# will have compiled files and executables
/target/

# Generated by Tauri
# will have schema files for capabilities auto-completion
/gen/schemas
</file>

<file path="src-tauri/build.rs">
fn main() {
    tauri_build::build();
}
</file>

<file path=".gitignore">
# Node
node_modules/
dist/
dist-ssr/
*.local

# Vite
.vite/

# Rust / Tauri
src-tauri/target/
src-tauri/gen/

# App data (never commit a real SQLite DB or downloaded files)
*.sqlite
*.sqlite-wal
*.sqlite-shm

# Editors / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/

# Env
.env
.env.local
</file>

<file path="README.md">
# ZenPaws

A LAN-only, peer-to-peer desktop messenger with interactive desktop pets,
built with Tauri v2, Rust, React 19, TypeScript, Vite, and **Bun**.

## Status

Phase 0 (architecture) is complete. Phase 1 (project foundation) has created
the Tauri/React workspace and is awaiting its first local Bun and Rust
verification. No product feature is implemented yet; see
`docs/14_DEVELOPMENT_PHASES.md` and `docs/16_PROJECT_MEMORY.md`.

## Prerequisites

- [Bun](https://bun.sh/) 1.3.14 or newer
- [Rust](https://www.rust-lang.org/tools/install) via `rustup` (edition 2024)
- Tauri v2 platform prerequisites: WebView2 on Windows, platform build tools
  on Linux, or Xcode Command Line Tools on macOS

## Start development

```bash
bun install --frozen-lockfile
bun run tauri dev
```

The initial lockfile must be generated once with `bun install` when Bun has
network access. Commit the resulting `bun.lock`; do not replace it with a
Node-package-manager lockfile.

## Quality gates

```bash
bun run verify
cd src-tauri && cargo fmt --check && cargo clippy --all-targets -- -D warnings
```

## Layout

- `src/`: React frontend
- `src-tauri/`: Tauri binary crate and its required configuration
- `src-tauri/crates/`: internal Rust library crates; implementation begins in
  the feature phase that owns each concern
- `docs/`: architecture, operating rules, and project memory
</file>

<file path="tsconfig.json">
{
  "compilerOptions": {
    "target": "ES2022",
    "useDefineForClassFields": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noImplicitAny": true,
    "strictNullChecks": true,
    "strictFunctionTypes": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": ["src"]
}
</file>

<file path="docs/14_DEVELOPMENT_PHASES.md">
# 14 - Development Phases

| Phase | Name | Status |
|---|---|---|
| 0 | Documentation and Architecture | ✅ Complete |
| 1 | Project Foundation | Complete |
| 2 | Networking Foundation | Complete |
| 3 | Database Foundation | Complete |
| 4 | Pet Engine | Complete |
| 5 | Chat System | In progress |
| 6 | Image System | Not started |
| 7 | File Transfer System | Not started |
| 8 | Notification System | Not started |
| 9 | Performance Optimization | Not started |
| 10 | Testing and Hardening | Not started |

Never skip phases. Status is changed only with the corresponding updates to
`16_PROJECT_MEMORY.md`, `17_CHANGELOG.md`, `18_BACKLOG.md`, and
`19_KNOWN_ISSUES.md`.

## Definition of Done (applies to every phase)

1. The code and docs cover that phase only; later-phase concerns remain
   deferred.
2. Project memory, changelog, backlog, and known issues are updated.
3. The applicable Bun and Cargo quality gates have passed and their results
   are recorded.
4. The phase is one atomic git commit (`phase-N: <summary>`).
5. A ponytail-ladder self-review states what was not built, what was reused,
   and what genuinely required new code.

After this checklist, record Definition of Done, Open Questions, Risks, and
Next Phase Plan, then wait for approval before a subsequent phase.
</file>

<file path="docs/16_PROJECT_MEMORY.md">
# 16 - Project Memory

Always resume from this file rather than re-deriving intent from chat history.

## Phase 0 — Documentation and Architecture (complete)

- ZenPaws is a LAN-only, hostless desktop messenger plus independently
  functioning desktop-pet platform. The app identifier presently configured
  is `com.krishna.zenpaws`; confirm ownership before a public release.
- Replication uses Lamport-clock last-write-wins, tie-broken by peer UUID.
- Transport and discovery decisions were implemented in Phase 2: TCP, `rustls`
  TLS with TOFU pinning, `mdns-sd`, and bounded UDP broadcast fallback.
- The planned SQLite binding is `rusqlite`, with WAL and FTS5, in Phase 3.
- Constraints fixed: SHA-256, a 2 GB file maximum, a 25 MB image
  auto-transfer threshold, and two pet-state broadcasts/second/pet.
- Owner-approved receipt semantics: delivered means acknowledgement from at
  least one directly connected peer; read acknowledgements are DMs only.

## Phase 1 — Project Foundation (complete)

- Rebuilt the foundation around Bun: `packageManager` pins Bun 1.3.14;
  scripts and all contributor instructions use `bun`, never a Node package
  manager runner. `bun.lock` is intentionally absent until the first Bun
  installation can generate it; it must be committed when generated.
- The required Tauri v2 layout is in place: React in `src/`, Tauri package
  and Cargo workspace root in `src-tauri/`, internal `zenpaws-*` crates in
  `src-tauri/crates/`, per-window capabilities, and a thin IPC command.
- Phase-1 Rust dependencies are limited to Tauri, its two necessary plugins,
  and serde for the IPC smoke-test response. Future networking, storage, and
  database dependencies were deliberately not preinstalled.
- The initial shell uses a real `app_status` IPC command to exercise the
  frontend/backend bridge. It is foundation wiring, not a product feature.
- Added a first-party ZenPaws application icon set and restrictive production
  CSP; development CSP permits only the Vite development origin and IPC.
- The project owner confirmed the Phase 1 Clippy remediation and marked this
  phase complete. Phase 2 remains blocked on the delivery and read decision.

## Phase 2 - Networking Foundation (complete)

- Owner approved direct-message-only read acknowledgements. Shared-room
  messages have no read receipt or per-peer seen state.
- Added the first real networking foundation: a typed shared event bus, stable
  peer identity type, explicit connection state machine, and bounded binary
  protocol framing with a protocol version and 1 MiB control-frame limit.
- Added only the dependencies used by this work: `tokio`, `serde`, `uuid`,
  `thiserror`, `bincode`, and `mdns-sd`. The `mdns-sd` 0.18.2 API was checked
  before use: `ServiceDaemon::new`, `browse`, `register`, and async receiver
  events are its documented interfaces. Added `rustls`, `rcgen`, and `sha2`
  for real self-signed TLS identity generation, SHA-256 certificate pins, and
  client configurations that trust only an explicitly stored peer certificate.
- Added `JsonTrustStore` in `zenpaws-settings`. It persists first-use peer
  certificates, accepts reconnects with the same SHA-256 pin, and rejects a
  changed pin instead of silently replacing it. `TlsIdentity::trust_peer_certificate`
  uses that shared trust-store boundary to create a TLS client configuration
  only after the first-use record or pin match succeeds. Added `tokio-rustls`
  and verified a loopback TCP connection is upgraded with the pinned TLS
  configuration. Added a bounded exponential reconnect backoff with reset on
  successful reconnect; it has no polling loop or port scanning behavior.
- Added the post-TLS application handshake. It exchanges and validates UUID
  and username using the framed protocol only after TLS succeeds; a discovery
  UUID mismatch is rejected.
- Added the UDP broadcast fallback with a bounded 1 KiB, versioned discovery
  datagram. It is only a fallback for filtered mDNS networks, never a port
  scanner or polling mechanism.
- Added `NetworkService` to own the TCP listener, mDNS advertisement, TLS
  accept path, post-TLS handshake, and inbound peer lifecycle. It uses the
  configured heartbeat deadline rather than a polling task.
- Added persistent local identity material in `zenpaws-settings`: peer UUID,
  validated username, self-signed certificate DER, and PKCS#8 private key DER.
  `TlsIdentity` can restore those bytes without regenerating a certificate.
- Added `NetworkService::connect_peer` for the outbound lifecycle. It applies
  the stored TOFU certificate decision, opens pinned TLS, runs the application
  handshake, validates the discovered UUID, and creates peer state.
- Added the `start_network` Tauri command. It loads or creates the persisted
  local identity, binds and advertises the service once, and starts the bounded
  inbound lifecycle task. Root Tauri compilation remains blocked in this
  sandbox by the missing Linux `gdk-3.0` development package.
- mDNS advertisements now carry the public certificate as bounded Base64 TXT
  chunks. Resolved peer metadata reconstructs the certificate, endpoint, UUID,
  and username for the first-use TOFU decision and pinned outbound connection.
  `start_network` now owns the JSON trust store and starts the mDNS-driven
  outbound connection task alongside inbound acceptance. Duplicate discovery
  events are deduplicated per peer. Failed outbound connections use bounded
  retry backoff; a TOFU pin mismatch emits `PeerTrustViolation` and does not retry.
- Added `stop_network`, which signals the inbound and discovery tasks through a
  Tokio watch channel and shuts down the mDNS daemon. The runtime remains
  single-start for the current app process to preserve one local identity.
- The UDP fallback now runs in the app lifecycle. It broadcasts the bounded
  advertisement at most once every 30 seconds, receives peer advertisements,
  and routes them through the same deduplicated TOFU and TLS connection flow.

## Phase 3 - Database Foundation (complete)

- `rusqlite` documentation was checked before use. `Connection::open_with_flags` supports explicit read-write/create flags; the `bundled` feature supplies a controlled SQLite build with FTS5 support.
- Added immutable migrations `0001_init.sql`, `0002_messages_fts.sql`, and `0003_peer_certificates.sql`. Existing migration files were never edited.
- Implemented WAL mode, foreign keys, bounded busy timeout, typed peer and settings writes, keyset pagination, FTS5 search, and disk-backed migration coverage.
- Added `DatabaseTrustStore`, which persists certificate DER and SHA-256 pins in SQLite and rejects changed pins. The network runtime now uses it.
- Tauri setup opens the app-data `zenpaws.sqlite` behind an `Arc<Mutex<Database>>`. Root Tauri compilation still requires the unavailable GTK development package in this sandbox.


## Phase 5 - Chat System (in progress)

- Owner approved transition after Phase 4. Start from typed database message APIs, the event bus, and the owner-approved DM-only read receipt policy.
</file>

<file path="docs/17_CHANGELOG.md">
# 17 - Changelog

## Phase 4 - Pet Engine

- Added data-driven pet manifest and registry validation, local state and cursor behavior, discrete sync event handling, and transparent pet-window commands.
- Added the pet asset prompt kit and generated review-pending sprite references and strips outside the phase commit.

## Phase 3 - Database Foundation

- Added bundled SQLite through `rusqlite`, WAL configuration, migrations, FTS5, typed settings, keyset pagination, and disk-backed migration verification.
- Added SQLite-backed TOFU certificate storage and wired it into the network runtime.

## Phase 2 - Networking Foundation

- Implemented mDNS discovery with bounded UDP broadcast fallback.
- Implemented versioned binary frames, encrypted TCP, post-TLS UUID and username handshake, heartbeat state transitions, and bounded reconnect backoff.
- Implemented persistent local TLS identity and JSON-backed TOFU peer certificate pin storage.
- Added Tauri start and stop network commands, inbound accept lifecycle, and mDNS/UDP outbound lifecycle.
- Recorded DM-only read receipt semantics.

## Phase 1 — Project Foundation (complete)

- Established the Tauri v2 + React project structure and internal Cargo
  workspace with the eight planned module crates.
- Made Bun the sole JavaScript package manager interface in metadata, scripts,
  README, and contributor instructions.
- Kept dependencies honest: removed unused opener and lint-wrapper packages,
  and removed future-phase Rust dependencies from the Phase 1 build graph.
- Added Vite Tauri development configuration, strict TypeScript, Biome,
  application shell, TanStack Query provider, and a thin Rust IPC smoke test.
- Added single-instance and window-state plugins, initial per-window
  capability files, production/development CSPs, and native icon assets.
- Created the project git repository and committed this phase as one atomic
  foundation commit.
- Restored Ultracite as the configured frontend lint and formatting wrapper.
- Restored the documented Rust minimum version of 1.97.
- Corrected the root crate Clippy findings: documentation markdown, the
  unnecessary panic in application startup, and the const command function.

## Phase 0 — Documentation and Architecture

- Added the project constitution and architecture/specification documents
  (`docs/00` through `docs/27`).
</file>

<file path="docs/18_BACKLOG.md">
# 18 - Backlog

## Deferred to later phases

- Phases 6/7: dialog and clipboard plugins, storage and transfer logic.
- Phase 8: notification and autostart plugins.
- Phase 5: detailed visual design tokens against real screens.
- Review generated pet sprite alpha, frame boundaries, and commercial-use provenance before committing production asset packages.

## Non-goals

WAN relay, mobile clients, multi-workspace topology, and one pet per LAN user
are outside this build.
</file>

<file path="docs/19_KNOWN_ISSUES.md">
# 19 - Known Issues

## Root Tauri package verification in this sandbox

The networking, shared, settings, and database crates pass their tests and Clippy with
warnings denied. Full root-package Tauri compilation is blocked in this Linux
sandbox because the system development package providing `gdk-3.0.pc` is not
installed. Verify the Tauri commands on a host with the Linux Tauri WebKit and
GTK prerequisites.

## Capability validation pending

Validate the generated Tauri capability schema during the first successful
`bun run tauri dev`. Pet windows are not created until Phase 4.

## Pet assets pending owner review

Generated pet references and sprite strips are not yet production assets. Their alpha transparency, frame alignment, actual frame counts, and provider commercial-use terms must be reviewed before `manifest.json`, `LICENSE.txt`, and asset commits are created.
</file>

<file path="docs/20_TESTING_STRATEGY.md">
# 20 - Testing Strategy

Written before Phase 1 implementation begins, per `00_PROJECT_CONSTITUTION.md`'s
process rules - later phases build to this, not around it.

## Layers

| Layer | Tool | Scope |
|---|---|---|
| Rust unit tests | `cargo test` / `cargo nextest run` | Pure logic inside each `zenpaws-*` crate - Lamport clock ordering, checksum verification, manifest parsing, etc. |
| Rust integration tests | `cargo nextest run`, loopback TCP between two in-process peers | Handshake, TOFU pinning, message round-trip, sync-on-reconnect - without real hardware. |
| Frontend unit/component tests | Vitest + React Testing Library | Component behavior, hooks, pure utils - no `any` in test code either. |
| Replication tests | Rust integration tests with 3+ simulated peers on loopback | Conflict resolution (concurrent edits), late-join catch-up sync. |
| LAN multi-device tests | Manual checklist (below) | Real mDNS discovery, real NAT-free LAN conditions, cross-OS pairing (Windows↔Linux↔macOS). |
| Performance tests | `criterion` (Rust benches), manual profiling on target-spec hardware | Verifies the budgets in `13_PERFORMANCE_GUIDELINES.md`. |

## CI gates (from Phase 1 onward)

Every commit: `biome check .`, `cargo clippy --all-targets -- -D warnings`,
`cargo fmt --check`, `tsc -b --noEmit`, `cargo nextest run`, `vitest run`.
A phase is not done if any of these fail.

## Manual LAN multi-device checklist (used starting Phase 2, finalized Phase 10)

- [ ] Two peers on the same LAN discover each other within a few seconds.
- [ ] A peer joining after messages exist catches up via sync, per
      `27_SYNC_REPLICATION_MODEL.md`.
- [ ] TOFU mismatch (simulated by regenerating a peer's key) surfaces a
      warning, not a silent failure.
- [ ] A large file transfer (near the 2GB limit) survives a pause/resume
      cycle and passes checksum verification.
- [ ] Idle CPU/RAM on target-spec hardware stays within the budgets in
      `13_PERFORMANCE_GUIDELINES.md` with 5+ peers connected.
- [ ] Cross-OS pairing: Windows↔Linux, Windows↔macOS, Linux↔macOS all work.

## What's explicitly not covered

Load testing at the full 50-peer target is a Phase 9/10 concern once
there's a real build to test - not simulated abstractly here.

## Phase 2 coverage

- Unit tests cover framed envelopes, retry caps, connection degradation, TOFU
  pin changes, local identity persistence, UDP loopback discovery, pinned TLS
  loopback, post-TLS handshakes, and network-service task sharing.
- Manual LAN verification remains required for mDNS and UDP broadcast across
  actual devices because multicast and broadcast behavior is OS and network
  dependent.

## Phase 3 coverage

- Database tests cover migrations, WAL mode, migration reuse on disk, typed writes, FTS5 search, settings persistence, and database-backed TOFU pin rejection.

## Phase 4 coverage

- Pet tests cover local state deduplication, rate limiting, and local-only versus synchronized state mapping.
- Desktop-host testing must verify transparent and click-through pet overlays on Windows, Linux, and macOS.
</file>

<file path="package.json">
{
  "name": "zenpaws",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "bun@1.3.14",
  "scripts": {
    "dev": "vite",
    "build": "bun run typecheck && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "check": "ultracite check",
    "fix": "ultracite fix",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.node.json",
    "verify": "bun run check && bun run typecheck"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.101.2",
    "@tanstack/react-virtual": "^3.14.6",
    "@tauri-apps/api": "^2.11.1",
    "@tauri-apps/plugin-window-state": "^2.4.1",
    "motion": "^12.42.2",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "zustand": "^5.0.14"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.4",
    "@tauri-apps/cli": "^2",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^4.6.0",
    "typescript": "~5.8.3",
    "vite": "^7.0.4",
    "ultracite": "7.9.4"
  }
}
</file>

<file path="src-tauri/src/lib.rs">
//! `ZenPaws` Tauri application setup.
//!
//! This stays a thin shell: plugin registration and window/event wiring
//! only. Business logic lives in the `zenpaws-*` crates under `crates/`,
//! reached through `commands/`. See `docs/04_SYSTEM_ARCHITECTURE.md`.
//!
//! Only the plugins Phase 1 actually needs are registered here. See
//! `docs/05_TECHNICAL_DECISIONS.md`.

mod commands;

use std::sync::{Arc, Mutex};

use tauri::{Emitter, Manager};

/// Application-owned database state. Commands borrow it through Tauri state.
pub struct DatabaseState(pub Arc<Mutex<zenpaws_database::Database>>);

/// Runs the desktop application.
///
/// # Errors
///
/// Returns a `tauri::Error` if application setup or the runtime fails.
#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() -> tauri::Result<()> {
    tauri::Builder::default()
        .setup(|app| {
            let path = app.path().app_data_dir()?.join("zenpaws.sqlite");
            app.manage(DatabaseState(Arc::new(Mutex::new(
                zenpaws_database::Database::open(path)?,
            ))));
            let bus = zenpaws_shared::EventBus::new(256);
            let mut events = bus.subscribe();
            let handle = app.handle().clone();
            tauri::async_runtime::spawn(async move {
                while let Ok(event) = events.recv().await {
                    let _ = handle.emit("zenpaws://event", event);
                }
            });
            app.manage(bus);
            Ok(())
        })
        .plugin(tauri_plugin_single_instance::init(|app, _args, _cwd| {
            if let Some(window) = app.get_webview_window("main") {
                let _ = window.set_focus();
            }
        }))
        .plugin(tauri_plugin_window_state::Builder::default().build())
        .invoke_handler(tauri::generate_handler![
            commands::app_status,
            commands::network::start_network,
            commands::network::stop_network,
            commands::pets::open_pet_window,
            commands::pets::close_pet_window,
            commands::chat::list_messages,
            commands::chat::search_messages,
            commands::chat::send_message,
            commands::chat::add_reaction,
            commands::chat::set_typing,
            commands::chat::message_status,
            commands::chat::edit_message,
            commands::chat::delete_message
        ])
        .run(tauri::generate_context!())
}
</file>

<file path="src-tauri/tauri.conf.json">
{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "ZenPaws",
  "version": "0.1.0",
  "identifier": "com.krishna.zenpaws",
  "build": {
    "beforeDevCommand": "bun --bun run dev",
    "devUrl": "http://localhost:1420",
    "beforeBuildCommand": "bun --bun run build",
    "frontendDist": "../dist"
  },
  "app": {
    "windows": [
      {
        "label": "main",
        "title": "ZenPaws",
        "width": 1100,
        "height": 720,
        "minWidth": 720,
        "minHeight": 480,
        "resizable": true,
        "center": true
      }
    ],
    "security": {
      "csp": "default-src 'self'; img-src 'self' asset: http://asset.localhost; style-src 'self' 'unsafe-inline'; script-src 'self'; connect-src ipc: http://ipc.localhost",
      "devCsp": "default-src 'self' http://localhost:1420; img-src 'self' asset: http://asset.localhost data:; style-src 'self' 'unsafe-inline' http://localhost:1420; script-src 'self' 'unsafe-inline' http://localhost:1420; connect-src ipc: http://ipc.localhost ws://localhost:1420 http://localhost:1420"
    },
    "trayIcon": {
      "iconPath": "icons/icon.png",
      "tooltip": "ZenPaws"
    }
  },
  "bundle": {
    "active": true,
    "targets": "all",
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/icon.icns",
      "icons/icon.ico"
    ]
  }
}
</file>

<file path="src-tauri/src/main.rs">
// Entry point only - Tauri's convention is to keep this file untouched and
// put actual app setup in `lib.rs` so the same entry point can be shared
// with a future mobile target. See docs/04_SYSTEM_ARCHITECTURE.md.
#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

fn main() -> tauri::Result<()> {
    zenpaws_lib::run()
}
</file>

<file path="src-tauri/Cargo.toml">
[package]
name = "zenpaws"
version = "0.1.0"
edition = "2024"
rust-version = "1.97"
description = "ZenPaws - LAN peer-to-peer desktop messenger with desktop pets"

[workspace]
members = ["crates/*"]

[workspace.package]
version = "0.1.0"
edition = "2024"
rust-version = "1.97"

[workspace.dependencies]
base64 = "0.22"
bincode = "1.3.3"
mdns-sd = "0.18.2"
rusqlite = { version = "0.38", features = ["bundled"] }
rcgen = "0.14.6"
rustls = "0.23"
sha2 = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
tokio-rustls = "0.26.2"
tokio = { version = "1", features = ["io-util", "macros", "net", "rt", "sync", "time"] }
uuid = { version = "1", features = ["serde", "v4"] }

# See docs/05_TECHNICAL_DECISIONS.md for why each of these was chosen.
[workspace.lints.rust]
unused_must_use = "warn"
unused_qualifications = "warn"

[workspace.lints.clippy]
all = { level = "warn", priority = -1 }
pedantic = { level = "warn", priority = -1 }
nursery = { level = "warn", priority = -1 }
module_name_repetitions = "allow"
must_use_candidate = "allow"

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "zenpaws_lib"
crate-type = ["staticlib", "cdylib", "rlib"]
path = "src/lib.rs"

[[bin]]
name = "zenpaws"
path = "src/main.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-single-instance = "2"
tauri-plugin-window-state = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid.workspace = true
tokio.workspace = true

zenpaws-shared = { path = "crates/zenpaws-shared" }
zenpaws-network = { path = "crates/zenpaws-network" }
zenpaws-database = { path = "crates/zenpaws-database" }
zenpaws-pets = { path = "crates/zenpaws-pets" }
zenpaws-transfer = { path = "crates/zenpaws-transfer" }
zenpaws-notifications = { path = "crates/zenpaws-notifications" }
zenpaws-storage = { path = "crates/zenpaws-storage" }
zenpaws-settings = { path = "crates/zenpaws-settings" }

[lints]
workspace = true
</file>

</files>
